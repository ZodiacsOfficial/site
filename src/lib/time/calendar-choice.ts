/**
 * The birth forms offer the Julian (Old Style) calendar only for a date
 * written before this day: Greece, the last European country to change, did
 * so in 1923. A module of its own, so the forms' first bundle carries this
 * constant and not the calendar code that loads with the control.
 */
export const CALENDAR_CHOICE_BEFORE = '1924-01-01';
