---
import Base from '../../../layouts/Base.astro';
import SignIcon from '../../../components/SignIcon.astro';
import Faq from '../../../components/Faq.astro';
import CollectBand from '../../../components/CollectBand.astro';
import { SIGNS, SIGN_SLUGS, elementLabel, signBySlug, signDates, signName } from '../../../lib/signs';
import { localizePath } from '../../../lib/i18n';
import { spanishGuideFor } from '../../../data/es-guides';
import { pairSlug } from '../../../lib/compat';

export function getStaticPaths() {
  return SIGN_SLUGS.map((sign) => ({ params: { sign } }));
}

const sign = signBySlug(Astro.params.sign!);
const guide = spanishGuideFor(sign);
const idx = SIGNS.findIndex((s) => s.slug === sign.slug);
const prev = SIGNS[(idx + 11) % 12];
const next = SIGNS[(idx + 1) % 12];
const easeful = {
  aries: ['leo', 'sagittarius', 'gemini'],
  taurus: ['virgo', 'capricorn', 'cancer'],
  gemini: ['libra', 'aquarius', 'aries'],
  cancer: ['scorpio', 'pisces', 'taurus'],
  leo: ['aries', 'sagittarius', 'libra'],
  virgo: ['taurus', 'capricorn', 'cancer'],
  libra: ['gemini', 'aquarius', 'leo'],
  scorpio: ['cancer', 'pisces', 'virgo'],
  sagittarius: ['aries', 'leo', 'aquarius'],
  capricorn: ['taurus', 'virgo', 'scorpio'],
  aquarius: ['gemini', 'libra', 'sagittarius'],
  pisces: ['cancer', 'scorpio', 'capricorn'],
}[sign.slug]!;

const jsonLd = [
  {
    '@type': 'Article',
    headline: guide.title,
    description: guide.description,
    url: `https://zodiacs.org/es/${sign.slug}/`,
    datePublished: '2026-07-07T03:45:11+07:00',
    dateModified: '2026-07-07T03:45:11+07:00',
    author: { '@type': 'Organization', name: 'Zodiacs.org', url: 'https://zodiacs.org/' },
    publisher: { '@id': 'https://zodiacs.org/#org' },
    mainEntityOfPage: { '@type': 'WebPage', '@id': `https://zodiacs.org/es/${sign.slug}/` },
    image: `https://zodiacs.org/assets/og/v2/sign/${sign.slug}.png`,
    inLanguage: 'es',
    about: { '@type': 'Thing', name: `${signName(sign, 'es')} (astrología)` },
  },
  {
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Zodiacs.org en español', item: 'https://zodiacs.org/es/' },
      { '@type': 'ListItem', position: 2, name: 'Signos del zodiaco', item: 'https://zodiacs.org/es/#signos' },
      { '@type': 'ListItem', position: 3, name: signName(sign, 'es'), item: `https://zodiacs.org/es/${sign.slug}/` },
    ],
  },
  {
    '@type': 'FAQPage',
    inLanguage: 'es',
    mainEntity: guide.faq.map(({ q, a }) => ({
      '@type': 'Question',
      name: q,
      acceptedAnswer: { '@type': 'Answer', text: a },
    })),
  },
];
---
<Base
  title={`${guide.title} | Zodiacs.org`}
  description={guide.description}
  path={`/es/${sign.slug}/`}
  image={`/assets/og/v2/sign/${sign.slug}.png`}
  imageAlt={`${signName(sign, 'es')} — guía de signo en Zodiacs.org`}
  type="article"
  locale="es"
  jsonLd={jsonLd}
>
  <article style={`--sign:${sign.hue}`}>
    <header class="guide-es-hero container">
      <SignIcon sign={sign.slug} size={168} loading="eager" class="guide-es-hero__icon" />
      <div>
        <span class="eyebrow">{sign.glyph} · {elementLabel(sign.element, 'es')} · {signDates(sign, 'es')}</span>
        <h1 class="display">{signName(sign, 'es')}</h1>
        <p>{guide.intro[0]}</p>
        <div class="guide-es-hero__ctas">
          <a class="btn btn--primary" href={localizePath('es', '/birth-chart/')}><span>Obtén tu carta natal gratis</span><span class="orb">↗</span></a>
          <a class="btn btn--ghost" href={localizePath('es', '/compatibility/')}><span>Revisar compatibilidad</span></a>
        </div>
      </div>
    </header>

    <div class="container container--narrow guide-es-body">
      <div class="reveal">
        <CollectBand signName={signName(sign, 'es')} slug={sign.slug} locale="es" />
      </div>

      <div class="prose reveal">
        {guide.intro.slice(1).map((p) => <p>{p}</p>)}
        {guide.sections.map((section) => (
          <>
            <h2>{section.heading}</h2>
            {section.body.map((p) => <p>{p}</p>)}
          </>
        ))}
      </div>

      <section class="guide-es-panel shell tinted reveal" style={`--sign:${sign.hue}`}>
        <div class="core tinted guide-es-panel__core">
          <h2>Compatibilidad rápida</h2>
          <p>
            La química de Sol a Sol es solo el comienzo. Estos signos suelen
            fluir con {signName(sign, 'es')}; para una lectura real, compara las dos cartas.
          </p>
          <div class="guide-es-panel__chips">
            {easeful.map((slug) => {
              const s = signBySlug(slug);
              return (
                <a class="chip" href={`/compatibility/${pairSlug(sign.slug, s.slug)}/`} style={`--sign:${s.hue}`}>
                  <SignIcon sign={s.slug} size={18} class="chip__icon" /> {signName(sign, 'es')} y {signName(s, 'es')}
                </a>
              );
            })}
          </div>
        </div>
      </section>

      <section class="reveal">
        <Faq items={guide.faq} heading={`${signName(sign, 'es')}, preguntas frecuentes`} />
      </section>
    </div>

    <nav class="container guide-es-nav reveal" aria-label="Más signos">
      <a class="guide-es-nav__link" href={localizePath('es', `/${prev.slug}/`)} style={`--sign:${prev.hue}`}>
        <span class="mono--label">← Anterior</span>
        <span><SignIcon sign={prev.slug} size={22} /> {signName(prev, 'es')}</span>
      </a>
      <a class="guide-es-nav__link guide-es-nav__link--next" href={localizePath('es', `/${next.slug}/`)} style={`--sign:${next.hue}`}>
        <span class="mono--label">Siguiente →</span>
        <span><SignIcon sign={next.slug} size={22} /> {signName(next, 'es')}</span>
      </a>
    </nav>
  </article>
</Base>

<style>
  .guide-es-hero {
    display: grid;
    gap: clamp(22px, 5vw, 44px);
    align-items: center;
    padding-block: clamp(28px, 6vw, 68px) clamp(22px, 5vw, 42px);
  }
  @media (min-width: 760px) {
    .guide-es-hero { grid-template-columns: auto minmax(0, 1fr); }
  }
  .guide-es-hero :global(.guide-es-hero__icon) {
    width: clamp(118px, 16vw, 168px);
    filter: drop-shadow(0 18px 44px color-mix(in oklab, var(--sign) 30%, transparent));
  }
  .guide-es-hero h1 {
    margin: 12px 0 14px;
    font-size: var(--text-hero);
    line-height: 0.96;
  }
  .guide-es-hero p {
    margin: 0;
    max-width: 58ch;
    color: var(--muted);
    font-size: var(--text-lg);
    line-height: 1.6;
    text-wrap: pretty;
  }
  .guide-es-hero__ctas { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 24px; }
  .guide-es-body { display: grid; gap: clamp(28px, 5vw, 46px); }
  .guide-es-panel__core { padding: clamp(22px, 4vw, 32px); display: grid; gap: 12px; }
  .guide-es-panel h2 { margin: 0; font-size: var(--text-xl); }
  .guide-es-panel p { margin: 0; max-width: 58ch; color: var(--text-2); line-height: 1.65; }
  .guide-es-panel__chips { display: flex; flex-wrap: wrap; gap: 8px; }
  .guide-es-nav {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    padding-block: clamp(34px, 6vw, 70px) 0;
  }
  .guide-es-nav__link {
    display: grid;
    gap: 7px;
    min-width: 0;
    color: var(--text);
    text-decoration: none;
  }
  .guide-es-nav__link span:last-child {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-weight: 600;
  }
  .guide-es-nav__link--next { text-align: right; justify-items: end; }
</style>
