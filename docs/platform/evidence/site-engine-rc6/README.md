# Engine rc.6 site precision adoption evidence

These executed records form the site evidence carrier for the prepared rc.6 adoption candidate. The root integrator copied and verified the actual Node 22/24 records and independent public-consumer result before committing this carrier. The site still runs rc.5 at this evidence-only commit; source integration and full acceptance follow separately. No scratch placeholder identifies a release, publication or deployment.

The exact anonymous archive is [zodiacs-engine-0.1.1-rc.6.tgz](https://raw.githubusercontent.com/ZodiacsOfficial/sdk/51129a197cd3f2a2a8c966fb797ea4da1e147b3d/artifacts/zodiacs-engine-0.1.1-rc.6.tgz), SHA-256 `09c3e63432f8ba2e9df05af137c42f65ab039740a207a89418d9e6470ea3db3e`. Source commit is SDK `fb57af7a2cd7c30983cc8fb655183d5a11f9cf30`; artifact carrier is `51129a197cd3f2a2a8c966fb797ea4da1e147b3d`. The anonymous read returned HTTP 200 and the expected 36,591 bytes, with 23 regular archive members. This remains an unpublished candidate under the SDK publication hold.

The site applies the delivered second/millisecond wall-time comparison and millisecond offset conversion correction, without changing the local-date endpoint policy or receipt runtime. Author tests reproduce 23 failures before correction, then pass 183 focused tests on Node 22.23.2 and 24.19.0. Separate #427 companion tests preserve 16 deliberately inconsistent legacy contexts rejected before natal calculation and add 16 current-context one-call positives. The two patches and their exact bases/hashes are retained in the author implementation records.

[node22-parity.json](node22-parity.json) and [node24-parity.json](node24-parity.json) are fresh author executions of the actual rc.6 artifact and frozen site code. Each proves all 16 previously unavailable historical receipts become valid through the unchanged frozen receipt helper, nine civil controls remain valid, and 96 rc.5/rc.6 chart comparisons for identical explicit UTC inputs preserve numerical values apart from the intended engine-version field. Correcting a previously misresolved historical input may intentionally change its instant, flags and resulting chart. These are finite host-IANA and package-parity checks, not independent astronomical or historical certification. Source labels explicitly distinguish author checks from independent review.

Actual Chrome 152 executes the same 16/9/96 cases offline. Thirteen network/storage/cookie observer negative controls are exercised, then all observed call counts remain zero during the calculation/resolution checks. Its three served assets, source and bundle hashes, actual results, screenshot and cleanup are retained in the author record. Intl timezone lookups are expected for civil resolution.

The fresh public-consumer record uses only public root/geo/receipt APIs and actual archive declarations, with Node 22 and Node 24 execution. Its attribution, exact archive files, consumer setup, commands, TypeScript results and any raw errors are separate from the author matrix; no rc.5 record is relabeled as rc.6.

Signed fixed-offset receipt syntax and exact-pole coverage remain declared limitations. The separate local-date endpoint interval/policy defects are explicitly unresolved by this precision adoption. Previous artifacts, archived rc.1 TypeDoc, starter rc.3, optional ownership SDK, account/storage formats, daily content and approved site design are preserved. Production build, full-suite integration, immutable evidence-carrier finalization, publication authority and deployment remain the root integrator's responsibilities.
