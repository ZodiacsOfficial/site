# Root C-014 integrated acceptance

The final source consists of the exact independently accepted 12-file C-014
freeze plus the separately accepted two catalog-count tests and two metadata
files. All 16 file identities are verified against the reviewed records. Base
is 3845a04bcea822da3c30a18848ef3d8d133b7b66. No root product-source correction was
needed after the author freeze. CI and actual remote preview remain separate.

Node 22.23.2 / Chrome 152 acceptance passes: normal build, check (1,044 files,
zero errors/warnings and 11 existing hints), 5,024 tests / 417 files, 35 scoped
native date groups, 20 existing Moon ownership groups, 17 existing chart ownership
groups, and exact-base scope with eight precisely named protected files.
All 18 fresh approved-page captures are byte-identical to the base. Final render
source receipt is e664d1d35eeae647eff10cc775f058d16e179bea4f0006313a91f3f0d4a3b3b7.
Existing bundle budgets pass; the engine remains 23.3 KB within 25 KB.
The default 35-case native driver does not claim a new baseline equality run;
original author and independent same-runtime numerical parity are separate.

Actual normal-build pages pass 24 groups across six locales, chart/Moon and
1280/390 widths. Real offline Apia selection and native form edits verify an
ordinary successful chart, skipped-date focused refusal, removed old results/
actions/context, preserved typed date/time/place and successful corrected-date
recovery. Two English desktop controls preserve known-time gap behavior. All
accepted refusal states fit the viewport. No saved profile, mutation attempt,
unhandled page error or unexpected console error is observed. Each accepted
case retains exactly its one sanitized expected refusal diagnostic. This is
normal feature-off evidence; signed-in/capture behavior has the separate combined
fixture. Root visually inspects Russian chart and French Moon mobile refusals.

Original failures remain preserved. The first full suite passes 5,022 tests and
fails two stale exact-count assertions: adding one reviewed message changes 418
to 419 keys. Independent replay reproduces those failures, then the two corrected
constants and matching test title pass while all parity/interpolation assertions
remain unchanged. The scope allowance's extra two test paths have separate
independent acceptance; the prior six-path review was not silently broadened.
The build and 18 captures are freshly repeated because the source receipt also
covers these test files. Original pre-correction logs and receipts remain.

The first actual-page driver passes 12 Moon cases but stops 12 chart cases at an
incorrect shared error-on-edit expectation. Actual unchanged Chart handlers keep
the prior error until the next submit; Moon clears its error on edit. Source-proof
hashes and the original driver/results/screenshots remain. Only the affected
chart cases rerun with retained-error/input-focus assertions, then error-free
successful retry: all 12 pass. Thus the 24 accepted cases come from two explicit
executions, not a fictional single 24/24 run. Product and built bytes remain
unchanged. All raw records, executed drivers, native bundles, screenshots and
source inputs are in raw.tar.gz with verified ordinary members.

The selected-instant witness does not prove a date empty, complete date coverage,
Sun/Moon certainty, astronomical validity for every representable year, provider
authorization, deployed behavior or publication. Retained endpoint/certainty
counterexamples and SDK #5's release hold remain separate open requirements.
