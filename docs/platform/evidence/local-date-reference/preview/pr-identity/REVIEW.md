# Independent PR #435 identity at f803d254

Accepted for this bounded identity review. Fresh GitHub metadata before and after full changed-file pagination reports the open draft **“Reject unknown-time references outside the selected local date”** at head `f803d2543ad81343b22d49326b8b46d0e2ea03a0`, base `3845a04bcea822da3c30a18848ef3d8d133b7b66`. Both refs and SHAs stayed fixed. The immutable local Git merge base equals that declared PR base.

All **314** changed paths were retrieved in four pages (100, 100, 100, 14), without duplicates. Every filename and status matches the local diff, and every API blob ID matches the local tree. The verifier independently recomputed all 314 Git blob SHA-1 values from the exact contents and recorded each byte length and SHA-256. There are **293 additions and 21 modifications**: **298 documentation paths and 16 other files**.

The sixteen non-documentation paths are exactly the original twelve-file C-014 freeze and the separately reviewed four-file catalog-count/CI/scope supplement. All sixteen original byte lengths and SHA-256 hashes match. The C-014 identity is pinned to `328ea15bd192573894639dab699058cb83185e0c61e907dde7f275305cfe48c2`; the four-file identity matches the previously sealed review copy at `869d5042d7f355ffb881604e0db7614644cef6db6d51614d14a99a9b4243a400`. The integration patch retains SHA-256 `50330a5cc5865f44872f607795b1135bf5c98a0a1ffd06113ccf6cfcebff5922`.

The scope allowance is exactly the reviewed eight sorted, unique locale catalog/test paths, pinned to the actual `3845a04b…` base; its SHA-256 remains `2f26d0a3b85e651e1b248733adae5a818c213885c227af08b98a699acc440797`. The workflow differs from that base by precisely the four reviewed lines adding the five-minute local-date-reference native step. Its SHA-256 remains `5574577652bc6002884647804e6b99fa45c05840accbb3ce442620f5d32732a0`. Existing triggers, read-only permissions, setup, dependency/browser pins and always-uploaded artifact path remain unchanged. Package manifests, BirthFields, the browser helper, scope guard and bundle-report script have identical base/head blobs.

This is fresh remote evidence for PR #435. The previous #434 verifier was reused as implementation scaffolding only; no previous API response substituted for these reads. All three read-only GitHub requests succeeded with empty stderr. No setup failure, remote mutation, credential capture, browser run, test run or production access occurred. GitHub authentication remained internal to `gh`; no credential headers were requested or stored.

This establishes source identity and bounded wiring/scope, not remote CI success, browser behavior, full-suite results, numerical correctness, publication or deployment. Documentation contents are identity-matched, not substantively reviewed in this subtask. Root remains the integrator and the parent owns preview review. Raw API responses, exact source extracts, per-file identities, immutable local diffs, verifier output and the command ledger are retained beside this report.
