# C-014 draft #435: independent preview acceptance

**Passed: 10 actual preview groups** at source `f803d2543ad81343b22d49326b8b46d0e2ea03a0`. The preview is [zodiacs-qg1besfdu-zodiacsofficial.vercel.app](https://zodiacs-qg1besfdu-zodiacsofficial.vercel.app), deployment `dpl_HAJ4JzQd5wyNRPicr2YX6cLDqi7u`. Fresh provider reads before and after report READY, target null, Git source and PR 435 at that exact commit. This verifies a draft preview, not production or publication.

Eight groups use EN/RU × Chart/Moon × 1280/390 with actual remote pages, remote modules, native form controls and the deployed GeoNames Apia option. Ordinary unknown-time December 29 computes; editing to the skipped December 30 clears prior result/actions/context; submission produces the dedicated localized focused refusal without another chart-computed event; correcting to December 31 recovers successfully. Chart retains its refusal message until retry and Moon clears it on edit, following the source-reviewed existing behavior. Four keyboard downloads parse as rc.6 receipts for unknown-time Pacific/Apia noon on December 31 with no houses. Two nested explicit 08:30 controls preserve known-time calculations on December 30. The other two groups preserve no-city UTC Moon and public positions-only input; the latter emits no chart-computed event or receipt and requests no full, chart-adapter or receipt module.

The checks observed 71 distinct deployed JavaScript modules and two actual city-data payloads, all preserved with exact byte identities. There were 819 request events and 782 complete remote 200 response bodies; these totals are not a claim that every requested resource completed. Seventy chart-context clear events were plain Events with no detail and the global context already absent. All sampled profiles stayed absent. No uncaught page errors, API requests or non-GET requests were observed in the test contexts. Eight expected generic refusal console diagnostics and ten deliberately blocked configured Plausible GETs are retained; no unexpected console errors or request failures remain after matching those blocks. Eighteen screenshots and four receipt files were rehashed. I visually inspected RU Chart mobile refusal and RU Moon mobile recovery; the alert is readable and the result recovers without horizontal overflow in the measured controls.

The existing Moon notice asserts that phase is unaffected by the missing hour. Its EN/RU text and the result-rendering/caption source slices are byte-identical to the preceding source. The actual strings are retained in the raw Moon records and separately labeled in `moon-baseline-copy-proof.json`. **This acceptance does not certify that certainty claim, whole-date coverage or complete Moon/Sun candidates.** C-014 establishes a representative membership witness; refusal does not prove the date is empty. The parent is handling the remaining certainty issue separately.

An independent child refreshed [draft PR #435](https://github.com/ZodiacsOfficial/site/pull/435) metadata and all four changed-file pages: 314 files, 298 documentation paths plus exactly 16 reviewed files. Every path/status/API blob ID matches immutable local Git, with Git blob SHA-1 recomputed from contents and SHA-256 retained. All original twelve frozen files and the four-file integration supplement match their reviewed identities. The eight-path allowance remains pinned to base `3845a04bcea822da3c30a18848ef3d8d133b7b66`. The main reviewer read that report and rehashed all 40 child records. Documentation identity was checked, not every document's substantive claims; this review does not claim remote CI success.

No component, SDK, resolver, date, loader or request-result data was substituted. The only page injection is passive observation of chart-computed/context-clear events. No numerical-call counts are claimed. The page uses the deployed build while a separate 23-file immutable source snapshot anchors interpretation and identity. No feature flag, signed-in account, backend lifecycle or nonempty capture-shell assumption was made.

The only new failure was the review driver's initial syntax collision, caught before browser work and retained with its original bytes. The corrected driver then passed all ten groups in one browser execution; original earlier local normal-page failures remain in their separate audit. Temporary preview access was deleted after exact URL/query/encoded-value scanning of 187 raw files and 94 decompressed archive members, with zero matches. All browser contexts closed. The auth bootstrap used a separate unobserved context, so this is not a zero-bootstrap-network or server-side access-revocation claim. No source edit, new deployment, merge, publication, production operation or SDK release was performed by this reviewer.
