# Independent C-014 scope allowance and CI wiring review

No blocker found in the bounded two-file metadata proposal after replacing only the explicit base marker with the actual final chart commit 3845a04bcea822da3c30a18848ef3d8d133b7b66. This is source/guard/wiring acceptance, not a remote CI-success, publication or deployment claim.

The sealed author proposal manifest is fad14ed213ce43396bc626b5b2b2b6aa3a3b8884891aca4fa985bc948ede7603; all 18 payload hashes were independently verified. Workflow patch SHA256 1f26431e837a4cbb73e689f31313f59316431259fa9f8d3b227891f9b7818eed and template patch aa1526ee88beb117b1c5d4fe74541a8a5865b410adb699817c3b61ecbae09583 are unchanged. The final materialized allowance SHA256 is 7b7bcb5bb33d19790c06d427c665c9e3dd709fdaed057c4d880c1aeebc6046d4; the exact final JSON is included. The template itself is deliberately invalid for the final comparison and was independently tested to reject.

## Exact final-base scope

The actual Git object resolves to 3845a04bcea822da3c30a18848ef3d8d133b7b66. Its workflow bytes match the proposal's input SHA256 286ca48156fb294f2af629fdf973d921565c5601142396a4d31fe5f1d95633a9. An owned detached sparse clone of that exact object received the unmodified accepted C014 patch plus only the two proposed metadata files. git apply --check and apply succeeded; all twelve C014 files were verified against their accepted freeze identities. The actual unmodified scope CLI reports PASS with 14 changed paths and exactly six protected paths covered. git diff --check passes. No root or author workspace was modified.

The six sorted catalogs are en/es/fr/it/pt/ru. Removing exactly the single added localDateReferenceError line from each accepted frozen catalog restores the exact final-base catalog bytes. Thus this allowance's reviewed content is one dedicated localized inability message per catalog. There are no locale test, Registry, SDK, Learn, guide, global navigation/style or Astrofolio exceptions. The prior eight-path historical receipt allowance is replaced, not accumulated.

Eight independently executed guard controls cover exact admission, wrong-base rejection, extra/missing protected paths, duplicate/unsorted paths, inactive allowance when its own file is unchanged, and retained rename/deletion protection. The final-base run is separate from the earlier observed-base run at 295bbfbe3728b3d6f57ff665dc0b08feb765bc41; both records are retained and only the former is used for current acceptance. The actual guard hash is 53c8452a2350eda3fb6541066846a88ad2111df46c803db807e91212c02c282d.

The guard is an exact-base/path exception, not proof of authorization or semantic content. Root's approved C014 policy supplies authorization; the accepted twelve-file freeze and the exact added-line comparisons supply content evidence. Once the allowance file leaves a future compared diff, it supplies no exemption. A changed comparison base requires a new correct validation; this report does not permit silently broadening the list.

## CI wiring

Removing only the four-line Unknown-time local date reference step restores the exact entire prior workflow. The resulting workflow hash is 5574577652bc6002884647804e6b99fa45c05840accbb3ce442620f5d32732a0. Independent YAML checks confirm the step follows Chart-linked prompt result ownership and precedes Engine accuracy vectors, has a five-minute timeout and runs OUT_DIR=tests/visual/artifacts/local-date-reference node tests/local-date-reference-drive.mjs.

The frozen driver passes Node22 syntax checking. Read-only inspection confirms repository-relative source root, OUT_DIR support, existing portable Chromium helper and stable arguments, local fixture/server/browser ownership, declared loader/city/probe boundaries, failure exit and cleanup. Without REFERENCE_BASELINE_DIR, CI runs candidate controls and records no baseline equality. No new dependency, secret, provider authority or backend operation is required.

Existing checkout/Node22/npm-ci/pinned Chromium steps, action pins, triggers and contents:read permission remain byte-identical. The existing if:always artifact upload covers tests/visual/artifacts/ for 90 days. Candidate bundle, source identities, result records and available failure screenshots use that output path. Early setup failure or abrupt timeout can still prevent a complete result file; CI logs and already-created artifacts remain the applicable evidence. This review did not execute Linux or remote CI.

One reviewer harness run initially asserted a guessed predecessor label, Post-chart context ownership. The actual unchanged source labels it Chart-linked prompt result ownership. The original failing script/log are retained; only that assertion was corrected after reading the source. The final byte/YAML/source checks pass without a product or proposal change.

All records are SHA256 hashed/sealed, not cryptographically signed. The combined C012+C014 actual-page flow has separate independent evidence; this metadata review does not replace numerical, caller, production, release or external-adoption checks.
