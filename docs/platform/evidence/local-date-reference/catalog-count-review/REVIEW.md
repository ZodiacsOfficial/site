# Independent C-014 catalog-count and scope supplement

Accepted for this bounded supplement. I copied the exact four-file root integration patch `50330a5cc5865f44872f607795b1135bf5c98a0a1ffd06113ccf6cfcebff5922` into a new isolated checkout at base `3845a04bcea822da3c30a18848ef3d8d133b7b66`, together with the original twelve frozen C-014 files. All sixteen captured files and the original source/patch were rehashed after verification and remain exact.

The two existing affected test files were executed with the actual frozen catalogs. Their original base versions produce **24 passed / 2 failed**, specifically the two assertions expecting 418 keys while receiving 419. Their exact supplemental versions produce **26 passed / 0 failed**. The retained byte-level comparison proves that the only test changes are the two `toHaveLength(418)` constants becoming 419, plus the corresponding English test title. Every existing key-parity and interpolation assertion remains byte-identical. This corrects the stale count without discarding the assertions that ensure catalog alignment.

The workflow SHA-256 `5574577652bc6002884647804e6b99fa45c05840accbb3ce442620f5d32732a0` is byte-identical to the previously reviewed four-line C-014 native step. No new CI behavior, permission, secret, trigger, installation or browser change is introduced by this supplement. The two count-test SHA-256 values are:

- `src/lib/i18n/index.test.ts`: `d14f7e38407738910c6e43c552475901bfea8358d3be31438666ddce0527c513`
- `src/lib/i18n/ui/ru.test.ts`: `5af680fd9c0905b3090a6f660c1381828ef9a39838ebba59eb9f383d6b60260d`

The allowance SHA-256 is `2f26d0a3b85e651e1b248733adae5a818c213885c227af08b98a699acc440797`. Its exact base, version and ID are unchanged from the prior six-path proposal. The protected list adds precisely these two test paths to the six catalogs; it is sorted, unique and contains eight exact file paths. The explanatory authorization text explicitly describes the two count updates and retains the conservative date-refusal and publication limits. This review covers the requested eight-path supplement separately; the earlier six-path acceptance was not treated as approval of the additional paths.

The actual unchanged repository scope CLI, run against this isolated checkout, passes **16 changed paths / 8 protected paths**. Replacing only the allowance with the exact prior six-path version fails with “does not exactly match the protected diff.” Running the current allowance against the prior PR base fails with “pinned to a different base.” After restoring the exact candidate allowance, the CLI passes again. The guard implementation is byte-identical to the base, and the final Git diff contains only the twelve original C-014 files and these four supplemental files.

The original product twelve-file freeze, its native driver and its previous independent review remain unchanged. This subtask adds no product, date, numerical, endpoint, browser, account, provider or publication changes. It does not rerun or claim the full suite, native UI, remote CI or deployment. Root remains the integrator and owns those gates. Existing installed dependencies were physically copied from a prior owned fixture; there was no network install. All source mutations were confined to the independent checkout for the original-test and six-path negative controls, then restored and rehashed. No root/author source, shared documentation or remote resource was modified.

`result.json`, the raw Vitest JSON/logs, actual scope CLI logs, `commands.json`, exact original/final files, frozen identities and verifier scripts contain the evidence. `runtime-and-final-provenance.json` records the exact runtime and dependency versions. The retained delivery excludes the full fixture checkout, Git object references and installed dependencies.
