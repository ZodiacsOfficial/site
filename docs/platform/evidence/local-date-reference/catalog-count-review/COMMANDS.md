# Execution and evidence notes

`assemble.py` performed a local read-only `git clone --shared --no-checkout` from the shared repository into the independent `site/` fixture, then `git checkout --detach 3845a04bcea822da3c30a18848ef3d8d133b7b66` there. Raw output and exit success are in `assembly-git.log`; `assembly.log` records completion. The clone reads immutable local Git objects via alternates; it writes neither root Git metadata nor root source. Checked-out files, exact freeze overlays and existing installed `node_modules` are physically separate files. No fetch, remote operation or package installation occurred.

`python3 run-review.py > run-review.log 2>&1` completed with exit 0. Exact subprocess arguments, working directory, UTC start, observed/expected exit code and raw output path are retained in `commands.json`. The two original tests intentionally exit 1; the exact candidate tests exit 0. The actual scope CLI positive exits 0, prior-six-path and wrong-base controls intentionally exit 1, and the restored final positive exits 0. No harness setup failure occurred in these runs.

Other read-only preparation inspected the exact four-file patch/identity, relevant original test and scope-guard source, package metadata, the current repository instructions and parent's sealed prior six-path/workflow evidence. It did not treat parent results as locally executed tests. After all runs, an independent read of the original source directories confirmed all sixteen provided source hashes and the supplemental patch still matched their captures. The report uses only the 26 locally executed affected tests and real local CLI outcomes; root's broader 81-test or full-suite results are not counted as this review's execution.
