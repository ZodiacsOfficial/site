# Reproduction record

All activity used /private/tmp/zodiacs-platform-date-downstream-combined-review and an owned site/ checkout.

1. Read exact c7 P3 fixture, preview/browser helpers, city selector/index and field sources into preparation/. Read the repository CLAUDE.md. The Supabase skill was consulted for the synthetic-client boundary; its mandatory public [changelog](https://supabase.com/changelog) and [getSession documentation](https://supabase.com/docs/reference/javascript/auth-getsession) were checked. Markdown changelog retrieval was unsupported by the web reader, so the public HTML changelog was used. No live Supabase task, schema, auth settings or client dependency was changed.
2. `python3 assemble.py > assembly.log 2>&1` archived exact c7 and verified/copied both frozen overlays. Initial Python 3.9 unsupported extractall filter failed before extraction; assembly-initial-error.log is retained. Only the empty owned site/ directory was removed before the corrected assembler succeeded. Existing node_modules was physically copied; no package download/install.
3. `/private/tmp/zodiacs-platform-runtime/node_modules/node/bin/node build.mjs > build-summary.log 2>&1` invoked the existing Astro build and assistant bundle builder with the explicit synthetic P3 environment. Both exit 0; raw logs retained. No simultaneous author build was used or edited.
4. Parent's exact Freeze 2 acceptance/authorization was recorded in MAIN-ACCEPTED.txt before browser execution. `/private/tmp/zodiacs-platform-runtime/node_modules/node/bin/node --check combined-page-drive.mjs` passed.
5. `/private/tmp/zodiacs-platform-runtime/node_modules/node/bin/node combined-page-drive.mjs > native-run.log 2>&1` initially exited 1 on the incorrect recovered-state assertion. Its complete six artifacts remain in initial-run/ with an unchanged manifest. Actual startup attempted POST was intercepted, not issued externally.
6. Root explicitly selected retaining that startup attempt and measuring zero further mutations; actual derive-state/sync source was read. Only the independent driver was refined. The same command then exited 0 on the unchanged product source/build, with four passing checkpoints. Native process/loopback access was approved; no external mutations were allowed.
7. Rehashed all 11,187 combined source records after build and native runs: zero changes. Hashed every dist regular file and archived only the 76 selected browser artifacts with Python's regular-file tar writer, then reopened and hash-verified every member. Sealed source/driver/runtime/build/city/request identities and both browser outcomes without including the whole generated site or node_modules.

An early broad text search produced truncated pre-existing evidence JSON; it was replaced with bounded markdown/path reads. A mistaken components/BirthFields.tsx lookup was corrected to the actual islands/BirthFields.tsx. Neither affected source or acceptance. No failing test run or attempted mutation was discarded.
