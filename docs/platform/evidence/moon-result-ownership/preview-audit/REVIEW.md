Moon preview delivery is internally consistent with its exact source and current PR 430 head. All 28 manifest records (2099209 bytes) match supplied SHA-256 fc873f343183390e8328ad8a8ab0e4779861643745b6c2917253d79f33ac4d0c.

The recorded 12 actual-page cases cover six locales at 1280 and 390 pixels. Each records successful result/focus, date/time edit clearing and recovery, no saved profile, matching local result text and matching screenshot bytes. All 624 observed same-origin responses are 200 with hashes; all 646 observed requests are GET. Twelve attempts to load the configured Plausible script are explicitly blocked. The same Moon module SHA-256 737f8ed5c5e9a19c9b3b238c4f99996ddcc98396522df7bdf8b691599bf5dcbe appears in all 12 cases.

The before/after READY preview metadata refers to commit 4227bb0758d4b34cbd761a3cddd273cbea6c2c1b, matching the source snapshot and current open draft PR 430. The exact Moon component SHA-256 0661eb17ee4bdcd32f2d7d0e4311691036bb5335c0ce14176b1e1234f3f976bb is the independently reviewed two-file implementation. GitHub lists 328 changed files in four pages, including evidence, the Moon component/driver, four workflow lines and screenshot provenance. All remote file names and non-document blob IDs match local immutable commit objects. The ordinary PR diff endpoint failed because it exceeds 300 files; this is preserved and does not invalidate the complete paginated comparison.

This is evidence validation, not another browser run or a new numerical oracle. Delayed-loader races, profile authority and production deployment are not inferred from these preview checks. The original result-comparison and scope qualifications are accurate. The runtime helper imported from the root checkout currently matches its pinned archived helper hash. The temporary access directory is absent; no credentials were requested or read. No root or product files were changed.
