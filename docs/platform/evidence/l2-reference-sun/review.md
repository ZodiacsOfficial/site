# Bounded review and corrected failure

A real delegated consumer reviewer implemented its isolated Today/email/Profile files
and focused tests. Root remains accountable for all integration and delivery.
A subsequent bounded reviewer pass found no defect in ownership/reset/receipt handling,
and proposed exercising the real forecast snapshot codec. The active/quiet × known/
unknown matrix now uses valid UUIDs and actual snapshot creation plus JSON round-trip
parsing; numerical inputs and contact receipt strings are unchanged.

The native chart fixture caught an actual initial-render regression in root's new code:
cross-locale automatic-name recognition called `t` for every locale on an English page.
Browser error was `Missing client UI catalog for es`; no chart form rendered. Root
replaced those cross-locale lookups with a small prefix map matching the pre-existing
Sun-name recognition pattern. The client catalog guard remains unchanged. A focused
parity/recognition test and actual six-language browser runs now pass. The failed run
was stopped after repeated initial-render failure; no failing candidate was published.

The first acceptance-capture attempt correctly refused a build whose fingerprint had
been invalidated by a subsequent focused test edit. Final capture followed the frozen,
rebuilt source. The first corrected-runtime capture passed eighteen views: fourteen PNGs matched prior bytes, four retained
identical dimensions with under 0.9% raw pixel differences. The final catalog-test-linked refresh passes eighteen views with fifteen PNGs matching prior bytes and three changed. Hosted visual/performance
budgets remain required and unchanged. No numerical or whole-date completeness claim
is inferred from screenshot evidence.
