# C016 normal-page evidence audit

Accepted as a bounded audit of the root's recorded local acceptance. No new browser execution, numerical calculation, product edit or release action was performed. One evidence-summary wording correction is required and agreed by root below; it does not change source or acceptance gates.

The audited source package is `docs/platform/evidence/reference-captions/root-integration`, manifest SHA256 `f2219750f04a88c1b69c05b1c59b0c4dd44a0cab30f43b07aebf33b94a451273`. All fourteen top-level records rehash correctly. All 340 raw original records map to 321 distinct, regular, content-addressed archive objects whose exact bytes verify. There are no additional nonregular, directory, xattr or AppleDouble members. The original evidence remains unchanged.

## Normal pages

`audit-result.json` records 114 independently executed evidence checks. The single actual normal-page execution contains precisely six locales × two modes × two widths, for 24 passing groups. Each separate group JSON equals its row in the combined report. The 48 recorded screenshots have matching hashes and PNG dimensions of 1280×1000 or 390×1000. All 104 distinct served-file byte hashes match the archived normal build assets, including the actual twelve HTML pages, component/engine modules and two city-data files. This audit rehashes the screenshots; it does not claim a new visual inspection.

The 46 source records captured before and after execution are exactly equal to the bound source manifest. All are unchanged in the final 51-record root snapshot. All 33 author source files in that final snapshot match the independently accepted frozen identity `c055e55985284904cb82e39fa1eca53a035aa337a3a49a752240fcd5c7d21644`. The final five additional records are a later snapshot domain, not extra source changes during the page run. Root's metadata/full-suite sequencing remains explicitly qualified in its original records.

I read the actual driver and independently parsed thirty expected literal properties from the six archived catalog source files, without executing the expected-output bundle or injecting anything into a page. Recorded Moon tool states show the correct unknown UTC and local reference captions visibly outside closed disclosures; supplied UTC retains its existing conversion notice; supplied local time has an empty caption. Clock/place edits remove the earlier result. Khartoum unknown reference and supplied 13:00 display the same phase, sign and illumination. The actual served city data contains one matching Khartoum/Africa-Khartoum row and matches the recorded selected city. Chart Moon mode has the reference label only for unknown time and the birth label for supplied time, with an equal phase suffix. These are recorded display comparisons, not a numerical oracle or complete-date proof.

All 1,586 observed browser request records match the server request multiset: local GETs with status 200, no attempted external/API/non-GET requests in these measured anonymous flows. Group records have no page or console errors. All recorded profile values are null; all recorded state errors and overflow flags are clear. The 84 successful-result focus snapshots are H2 headings. These are finite recorded focus/overflow observations, not a fresh accessibility test or permission for account operations. Owned contexts, browser and server cleanup are recorded.

The preflight and executed drivers differ by exactly one access: `row.requests.filter(r=>r.method()...)` becomes `row.requests.filter(r=>r.method...)`, because these rows already store a method string. The executed driver hash equals the report's hash. The root retains the preflight source and explicitly describes correction before its only run. No failed browser run or product change is inferred from this unexecuted preflight defect. Other root evidence-assembly qualifications remain intact.

## Sharing summary corrigendum

This audit supersedes only the causal phrase “differs ... because” in the sealed root README's stale-image paragraph. The supported statements are separate: the regenerated sharing sample differs from the tracked sample; the tracked artifact is an August 20 artifact rather than a fresh 9d180c9 rendering, and the preceding C015 sharing run recorded `zoomEvidence: null`. Those facts do not identify the cause of the pixel differences.

Root has agreed to carry that distinction in the canonical checkpoint while preserving the original sealed README. No normal-page image-bit-equivalence claim follows from the stale sample. The unchanged known-time rendering source and the actual fresh same-browser paired baseline/candidate PNG evidence are separate supporting records. The independently scoped child sharing audit supplies the exact source/log/image provenance reconciliation and records any limitations of its fresh Git reads; its report is linked through the supplemental manifest rather than represented as another browser run.

The main numerical/caller and consumer image/copy/privacy reviews, root unit/build/native gates, generator/scope/CI review and eventual hosted preview remain separate evidence. This audit does not broaden their scope or claim deployment, publication, merge or external adoption.
