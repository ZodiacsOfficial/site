## C-016 — Make current reference captions match their available evidence

Preserve the admitted calculation, its actual UTC, numerical positions, full
phase angle/illumination, existing lite phase name and portable receipt. Correct
the displayed claim instead of changing a reference instant to make an old noon
caption true. A Khartoum unknown-time request on 2000-01-15 resolves noon to
13:00 local while remaining within the date. Valid public positions tokens also
carry no date, clock or zone; examples calculated at06:17 and18:43 UTC currently
receive a12:00 OG caption. Do not add sensitive provenance to that wire merely
to justify a caption. Generic reference-position wording is sufficient.

Choose the coherent23-source current-result scope in the retained preparation,
rather than a Moon-only correction that leaves the demonstrated export/preview
claims or a global clock sweep across unrelated products. Explicitly include
only the no-angle settings/description prose in chart-preview-model.ts and
chart-preview.ts; no route, response shape, parameter, header, privacy rule or
image algorithm change is authorized by that inclusion. Include the exact
PT/FR/IT share-card counterpart keys required by the existing translation contract.
Root owns regeneration of the existing additions manifest and exact-base scope/CI
metadata. All23 product paths and actual tests must receive frozen-source review.

In MoonPhaseTool, remove its private alternate-sign metadata, day-endpoint
resolution and endpoint Moon samples. Retain primary reference resolution,
C-014 admission, first Moon/Sun evaluations, phase algorithms and result ownership.
Show the caption captured with the result visibly, including the truthful
known-time/no-city UTC caption. Unknown-time output must say phase and sign refer
to a reference moment and may differ at other times; it must not assert an actual
crossing, exhaustive alternatives or date-wide phase invariance. Keep exact
longitude in the existing disclosure and preserve layout classes/styles.

ChartCalculator changes only its unknown-time Moon-mode phase label. Its shared
result/Inspector notice must not invent a local clock or zone. Correct the
six-locale chart help, Moon help, share-card time note, technical-sheet settings/
opted-in provenance, and Tour/Lens no-time copy. The Tour must not guarantee all
other planetary signs across an unknown date. Correct each Moon-page FAQ answer
and the additional English paragraph, preserving the existing structure and
interpretive framing. Hidden birth details stay hidden; known-time and existing
resolved/reference UTC provenance stay unchanged.

Prefer retiring the now-unused moonChangedNotice key in favor of a clearly named
reference-phase label, with all uses migrated and420 keys retained. Descriptive
one-for-one internal caption-key renames are permitted only with complete use
coverage. Do not repurpose misleading names or weaken parity/interpolation tests.
Preserve every other locale property. A necessary additional key must be explicitly
recorded with its count-test effect rather than silently changing that contract.

Compare primary values separately from intentionally removed alternate metadata;
do not normalize numerical bytes or introduce tolerances. Require actual caller,
native visible-caption, real image-download, clockless Inspector and valid
non-noon OG-token controls, known-time/hidden-detail positives, refusal/recovery
and stale-result ownership. The lite/full phase-category discrepancy is an
existing separate algorithm boundary and must not be silently changed here.
Broader home/methodology/compatibility/transit/return/wallet clock prose, complete
date coverage and reference-Sun personalization remain separate. No SDK, receipt
schema, positions codec, canonical Registry/Astrofolio, account or publication
change follows from this decision.
