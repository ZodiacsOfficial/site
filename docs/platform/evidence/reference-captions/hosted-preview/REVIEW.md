# Exact C016 hosted preview acceptance

Accepted source9912e37bee3f20de691b6bed8165ff36189d1eca, draft#438.
Vercel preview dpl_42iL979pobWWCgELp1X4vAmtL953 remains READY at the same source
before and after execution. This is a preview, not production deployment.

Root executes14 actual hosted groups: six locales × Moon tool/Chart Moon mode at
390px, plus both English modes at1280px. All pass on Node22.23.2 and actual Chrome
152.0.7977.84. The driver adapts the previously accepted local normal-page driver;
its original is retained. No product build, source patch, numerical recalculation
comparison or independent reviewer execution is claimed here. Prior unchanged
source numerical, image, unit and local-browser evidence is reused.

Controls cover visible unknown UTC/local reference captions outside collapsed
disclosures, preserved supplied-time UTC caption, empty supplied-local caption,
actual offline Khartoum historical reference versus13:00 supplied time, reference/
birth phase labels, and edit clearing. No site response, clock, engine or source
is injected. Expected catalogs are compiled only for assertions. Actual served
city files match the accepted build data. All46 source records stay unchanged.
Root visually inspects the English desktop and Russian phone local-reference
screenshots; the notice is visible and fits in both.

All14 group records,28 screenshots and72 retained served HTML/JS/JSON bodies
rehash. There are910 observed requests;14 external Plausible GETs are blocked,
zero non-GET/API attempts,892 responses. Four200 image bodies are unavailable
(three app icons and one Taurus image); these are retained as unavailable, not
silently omitted or used for image-equivalence claims. All observed JS and city
bodies are captured. Every group records no page error, no saved profile and
no overflow. Anonymous measured flows do not prove backend/account behavior.

Preview-access bootstrap uses a separate disposable context and is unobserved.
All contexts/browser close. Exact private-string scan finds no leaked access
value; the temporary private file/directory is deleted and in-memory tool values
cleared. No server-side revocation claim is made.

The complete driver, its original, expected-output bundle, response/source rows,
group JSON, screenshots and ordinary captured bodies are retained in a neutral
content-addressed archive. Source equivalence is tied to the accepted freeze and
actual paginated PR path/blob verification. The required hosted CI remains a
separate gate; its initial performance failure and single justified retry are
recorded in the CI evidence, without weakening thresholds.
