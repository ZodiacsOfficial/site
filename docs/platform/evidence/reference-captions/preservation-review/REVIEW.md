# C016 independent numerical, caller and ownership review

Accepted within the frozen C016 scope. No product defect was found in these independent controls. The removed endpoint/alternate metadata is recorded separately; the calculated reference values, supplied-time behavior and portable envelopes remain exactly equal to the preceding source in the executed comparisons. This is bounded source and native fixture acceptance, not a production deployment, complete-date proof or astronomical accuracy oracle.

The exact baseline is `9d180c9f1a2f66893ccd6d73fcda106cb3894674`. The reviewed 33-file author identity is `c055e55985284904cb82e39fa1eca53a035aa337a3a49a752240fcd5c7d21644`; patch SHA256 is `0371de7d739a8526f342e49c3fdb623fa646d8d405a91db4f7623cba5b835b36`. All 33 copied and author-frozen source hashes were checked before execution and again at sealing. They comprise 23 product/copy files and ten tests/drivers. No reviewer changes were made to root or author source, build output, account data or external services.

## Independent executions

`parity.json` records 24 exact lookup comparisons: twelve inputs on Node 22.23.2 and the same twelve on Node 24.19.0. Each pair executes the actual contiguous `lookup` and illumination function spans from the original component, with explicit controlled lexical state and actual rc.6 full ephemeris, lite phase, resolver and membership functions. The primary JSON retains phase, full angle, illumination and longitude in its original order. The first actual Moon/Sun calls retain the exact UTC and numeric values. No rounding, tolerance, sorting or field removal is used for these primary comparisons. This extracted-function check is distinct from the rendered native component check below.

The inputs cover known and unknown UTC, Khartoum's shifted noon, Toronto's cross-midnight date, Juneau's repeated date, unknown skipped Apia refusal, retained known-time Apia gap policy, Paramaribo's historical seconds, Lord Howe, and both 1800/2199 UI endpoints. Each runtime produces eleven reference results and one refusal before numerical work. Khartoum retains `2000-01-15T10:00:00.000Z`, which actually formats to 13:00 local; Paramaribo retains `1911-01-01T15:40:52.000Z`. The candidate makes only the two primary longitude calls and one selected-city resolution. The old extra endpoint resolutions and Moon samples disappear. The skipped unknown date still performs zero longitude work. Runtime ICU/timezone versions are retained; this does not promise equality between different runtimes or all historical zones.

The owned Chrome 152.0.7977.83 fixture executes the complete actual Preact MoonPhaseTool and ChartCalculator, actual engine/resolver/rc.6 receipt code, and native form events. There are fifteen distinct accepted candidate groups across two executions: the initial run passed fourteen of fifteen, and a single corrected Russian-language assertion passed on the one-group rerun. The baseline passed fifteen of fifteen. These are not presented as one green fifteen-group candidate run.

The groups cover shifted-noon, repeated and ordinary dates; old alternate-sign output; unknown and supplied-time UTC; Russian unknown-local → known-local → known-UTC caption ownership; an edited new run sharing one pending loader promise; city removal and unmount during that promise; controlled primary failure and recovery; skipped-date refusal and recovery; and known/unknown Chart modes. The candidate notice is actually visible, not a hidden context string. Known selected-city results still have no caption; removing the city yields the existing localized UTC-conversion notice. The former alternate-sign heading is removed. Old delayed work does not calculate or restore a caption after invalidation; new work uses the new clock. Refusal and failure clear the prior result and retain focus/recovery behavior.

Eighteen native display/chart comparisons preserve the primary Moon calls, rendered phase, illumination, longitude and exact SVG disc string across paired states. Chart Moon-mode keeps its actual phase value and changes only the unknown-time label; known time remains “Moon phase at birth.” The full Chart known/unknown Khartoum results and immutable envelope JSON strings match exactly without stripping any fields. These two current envelope controls supplement the entire-component source reconstruction; they are not a fresh exhaustive receipt/save/profile-race suite.

The native fixture declares synthetic city rows, a controlled loader promise and pass-through call observations. Chart imports are observed through an explicit AST wrapper; their actual modules and calculator receipt execute. A share-artifact side effect is controlled, so this main review makes no actual image-download claim. It does not replace Moon `setResult` with an observer. The separate consumer reviewer owns real Canvas/download/server output. The fixture omits the complete Astro theme/assets and does not certify production layout. Native requests were fulfilled in the owned browser route: baseline 107, candidate 101, rerun three GETs; zero external-origin/non-GET attempts, zero page errors and no saved-profile writes. Each main execution retains the two expected console errors for the controlled primary failure and genuine skipped-date refusal. The rerun has none. Contexts/browser were closed; no server was started and no network request was issued.

## Source and architecture

`source-review.json` records 48 checks. The entire ChartCalculator reconstructs byte-for-byte from the baseline with only the approved comment and one phase-label expression. The entire MoonPhaseTool reconstructs from the approved endpoint/alternate removal, caption-key changes and visible captured-caption movement. Unchanged named functions are separately hashed. This verifies that input ownership, profile fences, saves, receipt generation, known-time calculation and dynamic import declarations were not changed by this slice.

The native baseline/candidate each compile the same 212 input identities: 200 unchanged bytes and twelve changed source inputs, with no added or removed input. The complete source/frozen changes, selected build inputs and transformed observation copies are preserved in the archive mapping. This is the actual finite fixture graph, not a claim about every production split chunk. No dependency installation or new runtime was used; existing dependencies were read through an isolated symlink and their executed input bytes are captured.

The exact 33-file diff and native driver were read. Tests retain the new reference semantics and existing numerical/known-time/privacy assertions. The separate metadata/generator review is already sealed under `/private/tmp/zodiacs-platform-reference-caption-metadata-review/delivery`, manifest SHA256 `1517480331f5da4715d52b46fce3e3ca41c3d25b81920ad1f81a08be5164d499`; it independently ran the unchanged generator and 23 metadata/negative-scope controls. This main report does not substitute that for root's actual CLI scope, CI or normal build gates.

## Preserved failures and limits

The initial numerical harness used CommonJS for a top-level-await entry and failed to build. The attempted Node executions therefore reported missing bundles. The preserved correction changes only the harness to ESM with explicit SSR definition; both actual runtime pairs subsequently passed. Read-path mistakes are retained in `preparation-read-errors.log`.

The initial native candidate's Russian assertion wrongly required the literal Latin acronym `UTC`. Both baseline and candidate correctly say “Время читается как всемирное — добавьте место рождения, чтобы пересчитать ваши часы.” The original driver/result and failed parity attempt are immutable. `native-harness-correction.json` identifies the narrow assertion correction and rerun selection. Only that affected group was rerun; no product bytes or numerical expectation changed. The author's separately retained test/setup failures were read and remain author evidence, not independent reviewer executions.

The root reports that a tracked sharing PNG came from an older August artifact rather than a fresh 9d180c9 rendering. This main review does not use that file as a current numerical or image baseline. The independent consumer's fresh same-browser baseline/candidate image comparison is the relevant bounded image evidence, supplied separately.

The primary remains one admitted reference, not a complete-date Sun/Moon candidate set. The existing lite phase category and full-engine angle remain separate algorithms; no newly discovered disagreement is claimed in these twelve inputs. The reference-Sun daily/save boundary, other noon wording outside C016, and complete interval activation remain open. The portable request convention `reference: 'local-noon'` remains unchanged even where resolver policy selects a shifted clock. Known-time gap/fold policy is preserved. Account deletion and real authenticated saved-write behavior were not activated or rerun here.

Root remains accountable for integration, normal full-site verification, release classification and any approved draft delivery. No publication, deployment, merge or external adoption is asserted by this review.
