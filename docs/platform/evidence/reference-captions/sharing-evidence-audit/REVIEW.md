# C016 root sharing-evidence supplement

The bounded sharing-evidence audit is accepted with two explicit qualifications below. No product or browser was changed or rerun. Parent separately audits the 24 normal-page groups.

Root manifest SHA256 `f2219750f04a88c1b69c05b1c59b0c4dd44a0cab30f43b07aebf33b94a451273` matches the supplied identity. All 14 top-level records verify, including the full raw archive hash. Ten selected sharing/driver records were read from their exact regular archive members, and their lengths and SHA256 values checked before writing only to this owned scratch. The selected member map is retained. This is not a second audit of all normal-page archive payloads.

Eighteen scoped assertions pass (`audit-result.json`):

- The prior C015 log has `zoomEvidence: null`, matches C015's actual delivered `sharing.log` byte-for-byte, and has SHA256 `76a234688c57358de502fd5a42a710a7cdf27cd707a91eb59a7c0305ae377119`.
- The current T17 log has the completed full/positions/partial/native-sharing transcript, including 1800×2400 full-chart PNG, 14 positions rows, moon/rising partial modes and prepared native image sharing. It differs from the prior success transcript only in elapsed duration and optional zoom capture path. The retained exact frozen driver writes this final transcript only after its final browser-error assertion; the log supports the reported existing gate pass. This is evidence reconciliation, not an independent browser re-execution or a real native OS share operation.
- The exact driver condition returns null without `T17_SHARE_EVIDENCE=1`. With that option it copies the actual completed PNG through Sharp to 594×792. Current log points at the expected tracked output path; archived old and current images have those dimensions, valid PNG signatures and different bytes.
- Root's archived baseline and candidate share-card modules match this reviewer's separately accepted baseline/candidate copies. The entire module reconstructs byte-for-byte after reversing exactly two permitted caption strings and one comment. No drawing/route/privacy implementation change is hidden in this module comparison.
- The present tracked sample matches the archived baseline bytes, SHA256 `dfbd85ebf67b822d8510b92dff2dc8ccd02671e26abf93da7afca2c1ba6e0fa7`. The new archived sample is `fdac569d1f89b4e844d3e7b4b21b57d9e8a4426f3ca3307321b9ecf32a841c38`. It was retained rather than accepted as an unrelated tracked artifact replacement.

Historical qualification: root's retained disposition records last change `84a775d3b0bac6834971911867aef9f05bce4fe3`, timestamp `2026-08-20T19:32:20+07:00`. Fresh independent local Git reads did not complete: exact blob `git show` timed out after 30 seconds, and bounded no-output history attempts were interrupted. The read-only escalation also produced no output. These attempts are retained in `git-attempts.json`; the August history remains root-recorded provenance, not a successfully reproduced independent Git result. The independent proof of prior `zoomEvidence:null`, byte-restored tracked image and exact source comparison does not depend on that date claim.

Wording qualification: root README says the image differs “because” the tracked sample is old. The evidence establishes a mismatch and absence of a fresh prior capture; it does not isolate the cause of the pixel difference. Root accepted phrasing those as separate facts in its subsequent checkpoint. The original sealed evidence is left unchanged. The stronger inference that the mismatch demonstrates a C016 regression or normal-image equality is unsupported and is not made here.

The retained pixel comparison is diagnostic only. Its original Node version was not recorded; this audit neither recalculates nor accepts its threshold as an image gate. Fresh paired known-time PNG equality belongs to the separately sealed independent consumer fixture (`6ac061cfde7b0755cb752f303910f516681b673369ac544996f383236acd2334`) and any separately attributed author execution. It is not inferred from these older/newer normal-page images. No numeric, image equivalence, publication or deployment claim is added.
