# C016 root local integration acceptance

Root applies exact Freeze1 c055e55985284904cb82e39fa1eca53a035aa337a3a49a752240fcd5c7d21644 (patch0371de7d739a8526f342e49c3fdb623fa646d8d405a91db4f7623cba5b835b36) above9d180c9f1a2f66893ccd6d73fcda106cb3894674:23 product/copy sources and10 scoped tests. All33 starting/base and final bytes are checked. Root subsequently generates the one-line English-default manifest change with the unchanged generator and applies the separately independently accepted two-file CI/13-path exact-base allowance. Final51-source snapshot includes these and selected unchanged runtime boundaries. This local acceptance remains separate from independent product signoff, draft delivery, hosted CI/preview, human review, publication, production and adoption.

Normal build passes, including23.3/25KB engine and other unchanged budgets. Astro check reports1,047files,0errors,0warnings,11hints. Following fresh captures, the full suite passes5,091tests in420files. All18 approved captures are byte-identical to delivered9d18; the fresh build receipt is bcc0855247a0910f539eca86209a549fbd0423b13917bddb8db91e694679a339. After the metadata-only integration,136 affected workflow/scope/generator tests and the actual exact-base scope CLI pass. No full-suite/generator/fingerprint expectation is weakened.

The frozen native caption driver passes34groups, local-date driver35 and Moon-ownership driver20. Root omits each optional historical baseline parameter; root does not claim new paired-baseline numerical/image equality from those runs. That comparison belongs to the separately executed author and independent review. Native fixture loader/place/state instrumentation remains explicit in the retained drivers and reports.

Actual normal-build acceptance passes24groups in one execution: six locales × Moon tool/Chart Moon mode ×1280/390. The driver loads actual built pages and real offline Khartoum city data, with no source, engine, clock or response substitution. It checks unknown UTC and selected-city captions outside closed disclosures, visible unchanged supplied-UTC wording, supplied-local empty caption, phase-label distinction, primary displayed Khartoum reference versus supplied13:00 values, and immediate result removal after clock/place changes. Forty-eight screenshots and all104 distinct served files are retained and byte-verified after execution. There are1,586 local GET request records, zero attempted external/non-GET/API requests, zero page errors, and no saved profile in measured flows. This is finite DOM/focus/overflow behavior; it is not exhaustive accessibility/browser/numerical certification. Root visually inspects English1280 and Russian390 local-reference screenshots.

A separate agent's preflight source sanity check found that the unexecuted normal-page driver treated a recorded method string as a function. Root preserves normal-pages.preflight1.mjs and changes only that field access before the first run. All24 actual cases pass; no failed browser case is hidden by this correction.

The complete existing t17 built-page sharing gate passes actual full/partial/positions/native-sharing behavior. The explicit T17_SHARE_EVIDENCE=1 option also captures a594×792 copy of the prepared1800×2400 chart sheet. This differs from the tracked sample because that sample dates to84a775d3 (August20) and the previous C015 run recorded zoomEvidence:null. It is not a valid fresh9d18 normal-image baseline. Both images, diagnostic pixel comparison, exact prior log and the entire before/after sharing source are retained. The full module reconstructs from only three frozen no-time comment/string changes; known-time drawing code is unchanged. No normal-page image bit-equality claim is made from the stale sample; actual paired known-time PNG equality remains separately attributed to the author/reviewer fixtures. Root archives the newly generated sample and restores only that owned tracked output to exact9d18, avoiding an unrelated artifact update. The initial diagnostic command did not record its Node version; its pixel metrics are diagnostic, not a new acceptance threshold.

The initial archive-input collection used a wrong capture-manifest path and failed after source/served-byte verification. Its error is retained; the retry uses the actual driver output docs/acceptance/phase1/screenshots/manifest.json. No product or test behavior changed. The Moon ownership command's original log destination was accidentally named /private/tmp/zodiacs-platform-reference-caption-root-acceptance-root-acceptance-unused.log; the completed original is copied byte-for-byte to native-moon-ownership.log. These are evidence assembly qualifications, not product failures.

Root also applies the relevant React skill checklist to these Preact components: no added hooks/effects/listeners or new render-time loading; existing revision guards and focused success/refusal remain; caption state is captured with its result rather than derived from edited fields; the existing live-status notice is visible while exact longitude remains disclosed. This is integrator source review, not a claimed React migration or separate reviewer.

No engine/receipt/positions codec, provider, account, global styles/navigation, canonical Registry, ownership SDK or Astrofolio implementation changes. The known lite/full phase-category discrepancy and broader discovery copy remain separate follow-ups.
