# Independent C016 integration metadata review

Accepted for the exact frozen 33-file C016 source at base `9d180c9f1a2f66893ccd6d73fcda106cb3894674`. This is separate metadata/generator acceptance, not product acceptance, CI completion or publication approval.

The proposed two-file integration patch is `a0c52aa23c1464858abada3db1b5bbfb4a8cedd5db7e409fb04016b84197d637`. Both before files exactly match immutable base Git contents and both after hashes match the proposal identity. The exact frozen source set contains 23 product/copy files and 10 scoped tests/drivers. Its actual protected-path intersection is precisely the listed thirteen paths: six UI catalogs, four locale Moon pages and the three PT/FR/IT share-card counterpart files. The earlier test-count allowances are removed. The unchanged scope guard hash is `53c8452a2350eda3fb6541066846a88ad2111df46c803db807e91212c02c282d`.

Twenty-three independently executed checks pass. The real exported guard rejects wrong base, missing actual changes, duplicate/unsorted/unprotected allowance entries, additional SDK/Registry/locale/test paths, and protected source paths in rename/deletion records. Omitting the changed allowance restores all thirteen protected violations. These are actual pure guard calls and parser controls, not an executed repository CLI diff; root owns that integration check.

The workflow changes only the four-line `Current reference captions` step before Engine accuracy. Its five-minute timeout and `OUT_DIR=tests/visual/artifacts/reference-captions` agree with the exact frozen driver `8eed49f7d612defe35b223e9fc9d3598ab2c30fd317ef23d1cc6ad1ae185f97f`. That driver derives the repository root from its own path, accepts OUT_DIR, uses the existing Chromium helper, and makes failures nonzero. The optional source-baseline environment variable is not supplied by CI, so CI executes its candidate-only mode; baseline numerical/image equality is separate author/reviewer evidence. Existing Node22 setup, dependency lock/install, pinned Playwright Chromium installation, contents-read permission, trigger scope and all surrounding workflow bytes remain unchanged. The output belongs to the unchanged always-uploaded `tests/visual/artifacts/` tree with 90-day retention. No CI success claim is made.

The separate generated-record proposal `cdcebdd27255dd9e71c89e98c87975b75724f160cc84404010ffd317ed83efe5` is also accepted. The unchanged generator `7cae770860a39d5423c8f4c6e5cad4df35261b41d55e1fdad51e622a55494e82` was executed with pinned Node22 and type stripping in a disposable exact-base source copy overlaid only with the frozen files. It wrote 579 keys and reproduced root's output SHA-256 `a5477a59ccc43cc3edad8b44904acda4fd2bcea7e67e97909f8cf683e06ba812` byte-for-byte. The entire output equals its prior version with one English default substitution for `shareCard.referenceTimeNote`. No translation-status, key, generator or unrelated prose changed. Source/copy semantics and actual image/privacy consumers remain in their separate reviews.

No root/author files, workflow or generated record were modified by this review. Actual commands and logs, unchanged guard/generator/driver, proposed before/after files, frozen identity and regenerated output are retained. No failure occurred in these metadata controls.
