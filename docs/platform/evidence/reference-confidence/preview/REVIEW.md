# C-015 exact preview acceptance — draft #437

Accepted within the bounded scope below for `9d180c9f1a2f66893ccd6d73fcda106cb3894674`, base `f803d2543ad81343b22d49326b8b46d0e2ea03a0`. This is independent preview acceptance, not merge, production deployment, SDK publication, comprehensive astronomical certification or human translation approval.

The actual provider identified preview `dpl_DCezqWrsukzWV3F7NotmTQCq4ENp` at `https://zodiacs-et5nq8c46-zodiacsofficial.vercel.app`, source Git, target null and READY before and after execution. The first listing was BUILDING; no access/browser execution occurred before READY. Twenty-eight selected immutable Git blobs match the snapshot, including all sixteen C-015 Freeze 2 files. An independent recomputation of the integrator's actual six-page GitHub file response verifies all 534 statuses and content-derived Git blob identities: 516 documentation paths and eighteen other paths. Pagination transport belongs to the integrator; this audit did not duplicate its remote request. `pr-identity/audit.json` records every independently checked file and SHA-256.

## Actual accepted browser scope

Eight distinct groups passed across two executions: the first run passed six of eight, and a corrected English-only run passed the two affected groups. Both English and Russian ran at 1280 and 390 pixels. Chrome was `152.0.7977.83`, driver Node `v22.23.2`.

Each of four full-chart sequences used the actual deployed offline city options and actual remote city index/shards, without substituting city rows:

- Toronto, `1919-03-31`, unknown time; Juneau, `1867-10-18`, unknown time. The hero Moon card is rendered as needing a birth time, with no candidate links/degree, the localized unverified notice appears, and the singular Registry bridge is absent.
- Toronto, `1990-06-15`, known `08:30`. The Moon uncertainty marker is absent, the existing Gemini Registry bridge/link returns, and the receipt identifies the supplied instant.
- Change the current result to Apia, unknown `2011-12-30`. Prior result/actions/Registry/context clear; submission produces the dedicated localized refusal with alert focus, no new computed event and no result. Editing to `2011-12-31` preserves Chart's existing error-until-retry behavior; retry recovers an unknown-time result and receipt.

Four other fresh contexts opened actual public positions-only links in both locales/viewports. The positions view appeared without receipt/Registry, full-chart computed events, or requests for the full/chart-adapter/calculator-receipt chunks. These are bounded observed-request checks; no native function call count was inferred from local symbols.

Sixteen native keyboard-triggered JSON downloads were parsed through the installed public `@zodiacs/engine/receipt` codec as rc.6 envelopes. For each of the four inputs, all locale/viewport downloads were byte-identical. Unknown-time envelopes have `timeKnown: false`, `reference: local-noon`, actual local date/zone/`12:00`, and absent angles/houses for unknown time. These receipts preserve a reference instant, not a whole-date result:

| Input | Actual receipt instant | Reference |
| --- | --- | --- |
| Toronto 1919-03-31 | 1919-03-31T16:00:00.000Z | local-noon |
| Juneau 1867-10-18 | 1867-10-17T20:57:41.000Z | local-noon |
| Toronto 1990-06-15 08:30 | 1990-06-15T12:30:00.000Z | supplied-instant |
| Apia 2011-12-31 | 2011-12-30T22:00:00.000Z | local-noon |

The Juneau receipt retains the historical `902.3166666666667` offset minutes. No endpoint completeness, historical-IANA coverage guarantee, independent ephemeris oracle or observed false Sun is claimed. Reference-Sun downstream save/daily behavior, the Moon tool and caption follow-ups remain outside this preview slice. No account/profile feature assumptions, local save activation or broad race rerun was introduced.

## Preserved failures and limits

The first English chart checks failed because my selector required exactly one `[data-moon-uncertain]` anywhere in the document. English also renders the existing ReadingPath uncertainty tile. The unchanged exact-source ReadingPath is preserved. The correction scopes only the hero check to `.calc__three [data-moon-uncertain]`; it does not change product bytes or weaken the no-links/degree/unverified/Registry assertions. Both original English failures and all first-run output remain. This is not one successful eight-of-eight execution.

The independent PR audit initially treated the preserved paginated API response as a flat array; the TypeError is retained, and the corrected audit explicitly validates page sizes `[100,100,100,100,100,34]` before flattening. The first evidence verifier also over-assumed every response body had been captured. Four status-200 image bodies were unavailable when contexts closed: three app icons in the already failed English desktop case, one guide avatar in the passed English mobile positions case. That failure is retained and the final verifier explicitly identifies those four images; no JS or city body is missing. A disabled-execution guard was intentionally exercised before access and failed closed. No product fix resulted from these review-harness issues.

There are 1,150 observed request events and 1,124 response events, of which 1,120 bodies were collected and hashed. All 68 distinct observed deployed JS payloads and four actual city data payloads are retained with identities. The remote archive has exactly 68 regular members, the selected source archive 28; both were written using neutral tar headers and reopened to verify each member, with no AppleDouble entries. This proves retained bytes, not that every potential dynamic path was visited.

Twenty-six native screenshots are retained, including the two initial failure captures. Download-stage images intentionally show the receipt area, rather than implying every screenshot depicts the hero. The reviewer visually inspected the English mobile initial Toronto result (correct unverified notice and hero cards) and Russian mobile receipt area. DOM/focus/overflow assertions cover the accepted sequences; this is not an exhaustive design or accessibility audit.

## Privacy, requests and cleanup

The driver uses a passive observer for chart-computed and data-free context-clear events. No product source, loader, resolver, clock, engine output or network response was injected. Every measured context starts fresh with service workers blocked. All non-GET, API and external requests are blocked by the owned browser route; actual measured cases attempted zero non-GET/API requests. Ten configured Plausible script GETs were blocked. Four expected generic calculation-refusal diagnostics, zero page errors and no other accepted-case console failures were observed. All 148 recorded accepted clear events were plain, data-free and observed the shared context already absent. The synthetic flows left `zodiacs.profile.v1` absent.

Preview authorization used a separate disposable, unobserved bootstrap context. There is no claim of zero bootstrap traffic, server-side token revocation or inspection of account endpoints. Temporary access remained outside the evidence directory with restrictive permissions. Before deletion, the exact generated private URL, nontrivial query values and encoded forms were scanned across 261 raw files and 96 decompressed archive members: zero matches. The private directory was removed, in-memory access reference cleared, and owned contexts/browser closed. Later reports/copies contain only previously scanned payloads and new nonsecret prose. `cleanup.json` records the exact scan's timing and scope.

## Separate evidence audit

A separate agent performed an actual read-only audit of these completed executions, independently recomputing the exact source hashes, full driver correction, eight-group accounting, deployed JS/city hashes and all sixteen downloaded envelopes. Its report and raw checks are retained under `preview-evidence-audit/`. It did not rerun the browser or read private authorization material; the earlier exact-secret scan remains attributed to the main reviewer. This audit is distinct from the native execution itself.
