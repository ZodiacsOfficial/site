# Independent audit of the captured C015 preview evidence

Accepted with the stated evidence boundaries. No blocker found. This is a read-only audit of two recorded native executions, not a third browser run or an independent remote deployment query.

Both runs record commit 9d180c9f1a2f66893ccd6d73fcda106cb3894674 and preview deployment dpl_DCezqWrsukzWV3F7NotmTQCq4ENp. Captured provider records before and after identify that same READY, non-production-target deployment and PR 437. All 28 selected source bytes independently match exact local Git blobs, including all 16 frozen product files. The source snapshot is not injected into the browser; the deployment identity and captured module hashes are the relevant remote linkage.

The initial execution remains **six accepted groups out of eight**, with both English chart cases failing `2 !== 1` at the uncertainty-marker selector. Corrected execution contains only the two English chart groups, both accepted. Their union is exactly eight distinct groups: English/Russian × chart/positions-only × widths 1280/390. Do not report the initial run as eight passes or the combined evidence as ten distinct accepted groups.

The complete driver diff is independently reconstructed and contains only (1) narrowing the Moon-card query to `.calc__three [data-moon-uncertain]`, (2) selecting a requested group prefix, and (3) setting the expected group count for that bounded rerun. Every numerical, receipt, Registry, input/refusal/recovery and request-boundary assertion is otherwise byte-identical. Exact ReadingPath source independently matches the commit and contains its existing second English uncertainty marker. The correction correctly tests the hero card without pretending that the reading-path marker is absent. It does not independently establish all reading-path behavior; the earlier frozen consumer review covers that separately.

All 144 run-specific JavaScript/city payload files rehash correctly. Both runs have the same 68 JavaScript and four city payload identities, with no basename collisions or missing recorded JS/city bodies. Four response events have status 200 but unavailable bodies at teardown: three for icon-512.png in the initial English desktop failure and one guide-avatar.webp in the initial English mobile positions case. Therefore the evidence does **not** preserve every HTTP response body. It does preserve every recorded successful JavaScript/city body needed for this source/data identity check. The initial verifier's overstrong all-body assertion and its correction remain captured.

Sixteen actual downloaded receipt files match their recorded hashes. The four input classes (Toronto, Juneau, known time, recovered Apia date) each have identical bytes across the two locales and two widths. An independent invocation of the existing rc.6 public receipt parser accepts all 16; their actual UTC instant plus recorded offset agrees with the requested local date/clock in these zero-gap cases. Unknown-time receipts describe a single local-noon reference, carry unknown-time house absence, and omit angles; known-time receipts describe the supplied 08:30 instant and include angles. These files do not contain a Moon-candidate set or establish date-wide certainty. No astronomical recomputation, independent accuracy oracle or numerical-call count is claimed.

The observed test contexts enforce the exact preview origin, GET-only requests, and no `/api/` requests. All 1,150 recorded request events are GETs; none target an API. The only ten blocked attempts are the configured external analytics script GET. The init script only records chart-computed and context-cleared events in a separate observer object; it does not override the engine, resolver, loader, date or fetched city data. Native UI input and city selection act on the actual page. Accepted results record no page errors, no saved profile, and closed contexts.

Authentication uses a separate disposable context before observers and request guards are installed. Its private URL/file was not read by this audit, and no claim is made about zero bootstrap network traffic, independent authentication-URL validation, or server-side credential revocation. Source review confirms that credential objects/cookies are not added to the report and the bootstrap failure is masked to a fixed message. The parent retained an exact-secret scan while those values existed, then removed access files; cleanup.json is captured as **attributed parent evidence**, not as a newly repeated exact-secret scan or independent deletion check.

The audit's 16 grouped reconciliation/source/body checks and 16 receipt parser checks passed. Original failures, both drivers, both raw results, provider/source records, cleanup qualification and receipt files are copied under captured/; raw module/city payloads were rehashed in place and their identities are recorded. No product/source/browser/private-auth changes were made. This acceptance remains limited to the one installed Chrome and these actual recorded flows.
