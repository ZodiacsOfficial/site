# Independent C-015 Freeze2 review

**Main preservation review accepted; no product defect found in the tested scope.** Exact identity `54cd5acbc7f69c7b3935943a9380be10bb18e8ca2b5a90c1d1766625d8bca55c`, patch `b13527901ee962d462b0ddd2bdbc16f82763ffa8ad7486d797358b46f76fd0a4`, sixteen files over `f803d2543ad81343b22d49326b8b46d0e2ea03a0`. The source and author copies were rehashed after execution. Root owns integration, full gates, normal-page captures, CI/scope metadata and release status.

The narrow implementation reuses the existing explicit empty Moon-candidate state for unknown-time Chart results. It removes the old local-date endpoint helper and its two direct body-position requests from this caller, and holds the singular Sun Registry bridge closed unless the time is known. A new localized notice says the possible Moon signs are unverified, without asserting an observed sign change. It does not infer a false Sun from a Moon counterexample. The legacy endpoint utility's executable code remains unchanged; its revised comments correctly describe samples and their limits.

## Independent native preservation controls

Nineteen frozen-candidate controls passed: the seventeen prepared controls and two additional ownership controls. The actual Chrome 152 / Node 22.23.2 fixture executes Preact, the complete Chart component, actual resolver, receipt helper and rc.6 ephemeris code from immutable copies. Synthetic city choices, module-loader gates, pass-through function-entry probes and share-artifact side effects are declared boundaries. There is no fabricated numerical result. The fixture's `portable` call label denotes a receipt-boundary invocation, not an unconditional native natal call; signed-zone and known-pole admission can fall back before natal execution. Direct `bodies` probes count the caller's endpoint requests, not primary ephemeris internals.

Fifteen comparisons preserve exact pre-augmentation and committed numerical JSON after omitting only the deliberately changed `moonSignCandidates`; twelve preserve the complete immutable portable envelope string. The numerical comparison includes the input instant/flags, coordinates, angles, houses, all bodies and aspects rather than a few selected signs. The matrix includes ordinary New York known/unknown time, Bangkok leap day, Paris seconds offsets, Manaus, a long Apia reference, unknown/known exact pole, signed-offset legacy fallback and Chart moon mode. Known-time charts keep their single Registry link; unknown-time charts have no endpoint confidence calls and no Registry bridge. This is finite same-reference compatibility evidence, not an independent accuracy oracle or complete-date certificate.

The native Blob receipt download exactly matches the captured immutable envelope and its baseline bytes. Two ordinary save controls execute the actual legacy local store once for known/unknown time; stable birth input, saved numerical summary and reference-Sun context match baseline. Random UUIDs and save timestamps are naturally generated and excluded from that stable-field comparison. A separate requested-save control delays the real store import, edits the form, then releases it: the captured old chart is written once, while the new input's cleared UI, receipt, Registry and context stay cleared. There is no rollback, retry or silent retargeting of that already requested write.

Replacement while receipt loading is pending computes only the latest input. Known-to-unknown editing clears the prior Registry link before the next run. The C-014 skipped-date refusal still precedes primary, receipt and endpoint work. Public positions-only initialization performs none of those operations and exposes no receipt/Registry link. The additional profile-derived primary control activates the actual conditional fail-closed reader boundary with no reader while engine loading is held; the form is scrubbed, calculation is suppressed and the synthetic stored bytes remain unchanged. This is a real source boundary with a synthetic legacy record, not a claim about a live account or authentication service.

## Source and test review

Exact reconstruction of the entire Chart component from the base allows only the reviewed import removal, confidence replacement, localized notice key and explanatory comment. All other component bytes match. Forty-two named functions and two large run spans—including primary resolution/calculation, ownership, receipt capture, share/context publication and error cleanup—are unchanged. The save function and its ownership/focus behavior are unchanged. A comment-stripped AST confirms no executable change in `chart-date-certainty.ts`.

Each catalog restores the preceding bytes when its one `moonUnverifiedNotice` line is removed. The two catalog-count tests advance 419 to 420 without dropping key/parity/interpolation checks. The Registry test now requires the known-time gate and absence of endpoint calls. Ownership and the existing C-014/T17 drivers replace only obsolete confidence expectations while retaining their numerical, refusal, sharing and focus checks. The new confidence test runs the actual caller statements; its native driver uses the existing browser helper and OUT_DIR convention and declares its controlled fixture boundaries. I read the full sixteen-file patch; I did not use the author's native test counts as independent proof or rerun that driver here.

The independent fixture has 211 baseline and 210 candidate inputs: only the endpoint helper disappears. It adds no input, and the 203 common unchanged inputs—including numerical, receipt, persistence and ownership dependencies—match exact hashes. Its other changed inputs are the Chart component and six catalogs. All dynamic import specifiers remain unchanged; the sole removed static module is the endpoint helper. This is source/fixture graph evidence, not a production bundle gate. SDK/package files, actual time resolver, portable helper, local store, positions serializer, MoonPhaseTool, fixed-noon captions and phase-at-birth surfaces are outside the frozen changes.

## Original failures and remaining scope

Before candidate review, the independent baseline passed fifteen controls and two saves failed because the reviewer's insecure `.invalid` HTTP origin lacked native `crypto.randomUUID`. A separate native two-origin probe confirms the failure's cause. Moving only the fixture origin to trusted loopback made both affected saves pass. Their original driver/result/log remain preserved; no product or authorization defect is inferred. The frozen candidate passed its seventeen controls on loopback, then the two additional ownership controls in a separate execution. Baseline and candidate extra ownership details match exactly.

The author's provisional Freeze1 failed two known-time Moon/Rising assertions because it compared legacy caller-input serialization with portable normalized-input serialization. The author preserved those logs; only that test comparator changed in Freeze2. I independently compared both sixteen-file identities and the test diff: product bytes did not change between those seals. The original failures are retained as author-attributed evidence, not concealed or counted as successful independent runs.

Unverified does not mean two possible signs, prove an empty date, or establish full-date membership/candidate completeness. The point calculation and portable receipt still identify a reference. Existing reference-Sun daily/save behavior is deliberately unchanged and remains an open product limitation. The optional saved-chart Registry Collection link is distinct from the singular computed-Sun bridge and unchanged. MoonPhaseTool's phase certainty, fixed 12:00 wording and Chart moon-mode phase-at-birth label remain separate follow-ups. No real account/backend, production layout/preview, publication, deployment or SDK release is certified or performed by this review.

The child's frozen consumer/counterexample review is still proceeding separately when this main preservation delivery is sealed. Its uncompleted checks are not counted here. The metadata proposal review is also a distinct record; neither becomes accepted by implication from this report.
