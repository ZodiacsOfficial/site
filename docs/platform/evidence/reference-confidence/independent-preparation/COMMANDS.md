# Bounded independent preparation ledger

1. Read the retained preparation delivery's manifest and `REVIEW.md.log`, then current immutable f803 source. The initial assumed filename `REVIEW.md` did not exist; the manifest identified the correct `.log` filename. Several exploratory guessed module filenames were likewise absent; subsequent reads followed the actual imports (`natal-context`, `PositionsShareSurface`, `share-positions`, `profile-chart-handoff`, `src/server/chart-preview-model`). These were read-path mistakes, not product or runtime failures.
2. `python3 capture.py > capture.log 2>&1` — exit 0. Read-only `git archive f803… src/lib src/data src/islands src/strings`; validate member paths and regular-file/directory types; physically extract into owned scratch. No root, author or shared documentation edit. Two server preview files were later captured by exact `git show f803:path` for source-only review.
3. `/private/tmp/zodiacs-platform-runtime/node_modules/node/bin/node build.mjs > build.log 2>&1` — exit 0. Bundle own exact source using an existing owned esbuild installation and dependency resolution path; no install or shared generated output. Explicit SSR catalog branch supports the non-DOM harness.
4. `/private/tmp/zodiacs-platform-runtime/node_modules/node/bin/node probe.cjs > node22-result.json 2> node22.stderr` — exit 0; stderr empty. Three admitted unknown-time controls, two known-time controls and one skipped-date control, plus two exact member/nonmember witness boundaries. The runtime, raw output, input coordinates and qualification are recorded in JSON. No browser or external request.
5. A display-only summary initially looked for signature field `headline` instead of its actual `title`, producing a KeyError while reading the already successful JSON. The full probe output was unaffected; the corrected display used the actual signature object. The exact probe and build themselves passed on their first execution.
6. The final identity step rehashed every consumed source/dependency input, recorded the existing engine package/version and committed rc.6 archive hash, and added the bundle identity. All consumed inputs remained unchanged. No numerical comparison tolerance was introduced.

This is independent preparation, not new production tests or author-freeze acceptance. No account, profile store, provider, publication, SDK rebuild or remote mutation occurred.
