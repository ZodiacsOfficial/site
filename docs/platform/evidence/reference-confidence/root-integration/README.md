# Root C-015 integrated local acceptance

The root checkout contains the exact sixteen-file Freeze2 candidate over
f803d2543ad81343b22d49326b8b46d0e2ea03a0, plus the separately reviewed two-file
CI/scope patch. All eighteen identities match final-source-identity.json. The
main independent preservation review accepts its scope; consumer review and
draft/remote acceptance remain separate from these completed root local gates.

Node22.23.2 / Chrome152 local gates pass: normal build and existing bundle budgets,
Astro check (1,045 files, zero errors/warnings,11 existing hints), all5,040 tests
in418 files,20 native confidence groups,35 existing date controls,17 chart
ownership groups and actual normal-build sharing. The engine static closure
remains23.3KB within25KB. Root's default confidence driver does not supply its
optional baseline directory: same-runtime baseline byte comparisons belong to
the separately retained author and independent runs. No test tolerance changes.

All eighteen fresh approved-page captures are byte-identical to f803d254. The
fresh build/capture receipt is
50d93d27c1409e48300103ba6608a24b7e5dd630debc5c8a66826420a6a73b26.
This closes the author's stale evidence-fingerprint failure rather than treating
it as a baseline exception. The root full suite has no linked-package failures
or skipped numerical suites and required no dependency change.

Actual normal-build chart pages pass twelve groups across six languages and
1280/390 widths. Each selects real offline Apia, verifies an ordinary unknown-time
result with the unverified notice, no singular Registry bridge or candidate-sign
links, edits to the skipped date, checks focused refusal and cleared old result/
actions/context, then recovers. Every group also verifies the known-time gap
positive: existing Registry link returns and the unknown-time notice disappears.
No saved profile, non-GET request, unhandled page error, unexpected console error
or overflow occurs in accepted states; each refusal retains its one sanitized
diagnostic. These are normal feature-off pages, not real-account/backend proof.

Ten groups passed initially. Two English groups failed an overly broad marker
count because the unchanged English ReadingPath also carries data-moon-uncertain.
The corrected hero-container selector passes both affected groups. Original
drivers, failures, screenshots and exact marker-source proof remain intact;
no product change was made. Twelve accepted groups therefore come from two
executions (fourteen groups executed), not a fictional single twelve-pass run.
All109 distinct served build files are rehashed and archived. Root visually
inspects the Russian phone recovery image. No numerical-call counts are inferred
from these uninstrumented pages. MoonPhaseTool is not covered by this claim.

The new native fixture and current ownership/date drivers retain their own
explicit controlled-component, loader/city and network boundaries. Actual T17
sharing retains the full/positions fragments, no birth details in positions
wire, native prepared-image sharing and uncertain-Moon presentation. Its 33%
sheet capture is retained separately; no broad baseline visual claim is inferred.

The known gaps remain explicit: reference Sun personalization/save naming,
fixed12:00 captions when resolution shifts, Chart Moon-mode phase-at-birth and
MoonPhaseTool/date-alone phase claims. Removing unsupported candidate confidence
does not prove complete civil-date coverage or astronomical accuracy. Local
acceptance is not human numerical/legal signoff, published/deployed software,
SDK release authority or external adoption. Astrofolio remains isolated.
