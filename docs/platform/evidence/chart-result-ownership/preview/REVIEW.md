# Chart ownership draft preview verification

**Accepted within the nine executed remote checks.** Draft PR [#434](https://github.com/ZodiacsOfficial/site/pull/434) remains open at `c7b9eb4a769e39a46aebc0a5ecedcb9fc40949e3`. The automatic [preview](https://zodiacs-280vhfvby-zodiacsofficial.vercel.app) reports deployment `dpl_2Z94CZozTG7aXCNiEEbCLz2GR27t`, READY, target null, source git, and that exact commit/PR both before and after browser execution. This is preview delivery verification, not publication, merge, production deployment or external adoption.

## Actual remote browser scope

Pinned Node 22.23.2 drove an owned disposable Chrome 152 browser. All rendered product modules came from the remote preview; no local product module or rewritten response was injected. Eleven source/test/workflow files were independently read from immutable Git objects at the exact commit for identity and interpretation. The original frozen driver informed the scoped controls; this is not a claim that all seventeen controlled local tests were reproduced remotely.

| Executed remote control | Result and measured native natal calls |
| --- | --- |
| English, 1280px and 390px | Both passed: shared synthetic inputs preserved, chart renders, no horizontal overflow at those widths, keyboard receipt download validates, share dialog opens/closes, editing clears result/actions, resubmission recovers. Two natal calls per viewport. |
| Russian, 1280px and 390px | Both passed with the same checks and localized receipt labels. Two natal calls per viewport. |
| Failed new run after success, then recovery | Passed: a scoped native `JSON.stringify` fault only for the natal envelope makes the remote portable calculation fail; current result/receipt/share/save/context clear, typed date remains, the sanitized alert receives focus, and recovery succeeds. Three natal calls (initial, failing serialization after calculation, recovery). This is explicit host-API fault injection, not a spontaneous preview failure. |
| Edit while actual receipt module is delayed | Passed: released remote completion cannot restore result/actions/context or emit a computed event. Zero natal calls. |
| Edit while actual engine module is delayed | Passed: released remote completion remains obsolete. Zero natal calls. |
| Replacement run while receipt module is delayed | Passed: only the replacement date commits a result/event. One natal call. |
| Public positions-only initialization | Passed: existing positions surface remains, no receipt or computed event, and no full/adapter/receipt module requests. Zero natal calls. |

The native function was identified by AST inspection of the **actual served** `/_astro/chart-adapter.K-QfJlbI.js`, SHA256 `20f5ee939b52b2c435ab12f4c3fd20e8a3195f1ec57a727d7933b9356fdbeb95`. Function `Et` is the one-argument function returning its exact input plus bodies and engineVersion. Per-case CDP precise coverage records are retained; their twelve total calls come from this remote identity, not a guessed local minified symbol. The delayed transport targets were selected from already observed remote module paths. Native numerical correctness across dates is not inferred from call counts.

Four actual browser-downloaded JSON files passed the public rc.6 envelope parser and contain engine `0.1.1-rc.6`, expected synthetic civil date/time and no persisted profile. Downloads, nine screenshots, remote module bytes/hashes, raw request/response observations, driver and source identity are retained. The mobile English actions and desktop Russian result were visually inspected; there is no baseline-diff or whole-site visual claim. The ordinary screenshots may include the site's fixed navigation because they capture a scrolled result element.

## Repository and provider identity

The separate independent PR review obtained fresh metadata and both paginated GitHub file pages (100+99). All 199 names, add/modify statuses and Git blob SHAs match the exact head/base comparison, including documentation. The four product/test files match the reviewed Freeze 2. The only other non-documentation file is the workflow with the exact four-line native-driver step, using the existing Node/browser setup, five-minute timeout and always-uploaded OUT_DIR tree. The child's raw API records, exact comparisons and sealed manifest are copied unchanged under `pr-identity/`; the parent independently rehashed every record. This is identity/wiring verification, not a fresh CI-run claim.

## Network, privacy and limits

Test contexts allowed same-origin GETs for the actual page/static city/engine assets and blocked all non-GET, `/api/*` and external requests. The only observed blocked attempts were nine GETs for the configured Plausible loader. No API request or non-GET request was observed in the test contexts. There were 1,023 observed request events and 905 captured same-origin response bodies; status counts are in `summary.json`. Request events without a captured response are not asserted to have completed successfully. No uncaught page error occurred. Console errors consist of the deliberately blocked loader and the deliberately triggered sanitized portable failure.

Protected-preview authentication occurred in a separate disposable bootstrap context before scoped request observation. That bootstrap was not a zero-network or zero-request assertion. Its temporary URL/cookies were never written to browser evidence. The auth directory was deleted, browser contexts closed, and all 138 then-existing evidence/source files scanned with zero matches for the exact private URL or nontrivial query secrets. The source and remote response bytes contain synthetic inputs only. No root source/dist, saved account/profile, production service, deployment, PR or publication state was changed by this work.

The preview checks do not repeat every local profile-access, committed-save, unmount, delayed-context or delayed-focus race. Those retain their separate frozen native/CI evidence and limitations. A missing real authentication reader is not replaced here by a claim of authority safety. This preview predates the newly authorized unknown-time date guard and makes no skipped-date, interval or whole-date candidate-completeness claim.

## Original setup failures retained

The first driver exited before opening a browser because CommonJS `require.resolve` cannot select the ESM-only public receipt export. `preview-drive-run1.mjs` and `preview-run1.log` are retained. The correction uses a public ESM import; a receipt-field assertion was also corrected against the actual codec shape before the successful browser run. These were harness setup issues, not failed product controls. An initial provider query used an unverified scope and returned 403; scope was then discovered using `list_teams`, after which both provider reads succeeded. The PR reviewer also retains its original sandbox network failure before approved read-only API retrieval.

Evidence is copy-ready and sealed. Root remains the accountable integrator.
