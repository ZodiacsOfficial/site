# Independent Freeze 2 profile/save review

**No further defect found in this bounded scope.** The save-focus finding from Freeze 1 is corrected, and all **18 independent native callback controls pass**. The identical driver against the pinned original passes 6/18, retaining twelve counterexamples. Parent/main review and root repository gates remain separate requirements.

Exact patch: `32b7413f9fcce17b192fa9f3fe28847d6b6ec7bfccc39b4437fee60df44a8e9f`. Exact ChartCalculator: `fea737333ea5df9a894dafa99de06cd1dcc8390f3bd43d9e627992b466dfa4d0`. Four frozen files and 39 unchanged committed dependency/reference files were verified. The entire sealed Freeze 1 delivery was rehashed and remains unchanged. No product, author or root file was edited.

The full corrected save path was inspected. `savePromptGenerationRef` advances when clearing a result, revoking profile access or opening another naming prompt. At lines 1550–1564, a closing prompt captures both access and prompt generations and verifies them again before touching focus or `saveReturnRef`. At 1587–1645, save UI completion checks result identity plus both generations after persistence, after matching, around the delayed A2HS claim and before errors/closing. This covers an independent primary whose profile-derived secondary was revoked, as well as a replacement prompt for the same chart. The `saveChart` call, including its full persisted birth/summary/name/relationship arguments, is byte-identical to the pinned base (SHA `f748909241176c0242d7c7670f324520439f4979fe5f2e1d0a6611a4be44f03b`). The store, account reader and existing push module loader are unchanged.

Native evidence is `native-freeze2-final/result.json`, run with Node 22.23.2 and Chrome 152.0.7977.83. It retains all original fourteen controls and adds:

- A replacement naming prompt opens before the earlier same-result close frame; its focus and return target survive.
- A replacement naming prompt opens while an earlier explicit save waits for its import; the earlier requested name is written once, while that callback does not close/relabel the replacement prompt or claim its install hint.
- Revoke followed by regrant cannot revive a queued committed-save close frame.
- An ordinary named-other save preserves the entered name, relationship, context, hint, confirmation and correct focus.

The prior native controls continue to pass: ordinary self save, pending-save edit/revoke/regrant, actual post-write event revocation/edit, delayed install hints, stale close frames after result edits, primary private-data scrubbing, mine-only preservation of an independent primary and public-input secondary preservation. Zero browser page errors and zero external requests occurred.

**Written saves remain committed.** The final driver observes calls to native `Storage.prototype.setItem` while delegating every write to the original method. Every committed test records exactly one actual profile write; denied/prompt-only controls record zero. A revoke inside the actual store's synchronous post-write event leaves the chart stored once. An explicitly requested captured save may also complete after form editing or opening another prompt, as the authorized contract retains; its obsolete UI callback is discarded. No rollback, retry, cancellation or cross-account persistence claim is made.

**Method and limits:** these are actual Chrome executions of exact AST-extracted component functions, the exact revoke/access callbacks, unchanged actual store/context/A2HS modules, native storage/events/animation frames and the actual frozen Base reader. Only two save dynamic-import expressions become controlled promises; surrounding state setters are observation probes. This is not a complete ChartCalculator render or hook lifecycle timing test, and does not assert what independent profile listeners may legitimately reread from committed storage. Parent independently owns full-component and production checks, public positions, the other corrections and portable driver/OUT_DIR review.

The synthetic reader control remains explicitly conditional: a custom `canRead` can dispatch revoke then return true after earlier equality checks. Both original and candidate retain that helper result. The actual Base reader has no such callback and passes native lease/matching-owner/mismatch/revoke controls. No ordinary auth bypass is inferred. This scope adds no numerical, date-interval, certainty, ownership SDK, account API, privacy-policy, style, locale, publication or deployment claim.

The first Freeze 2 run passed 18/18. A second bounded run added observation of the actual write count and again passed 18/18; both are retained. The esbuild warning concerns a tree-shaken unused locale import.meta branch; the native report has no runtime errors. All disposable browser contexts and the owned loopback server closed. Source identities, callback extraction identities, bundles, raw logs, driver and observations are retained for the accountable root integrator.
