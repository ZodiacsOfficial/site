# Independent frozen profile/save review — correction requested

Exact Freeze 1: patch `8a8f32d0c3eadeb68fffd282752c33e17fc273143955d5e93eebc397baba3738`; ChartCalculator `6d954dd0994e019408e5c23cae7ffab9853939d774eb0d8e7df9ca4a51aa920b`. All four frozen files were copied and verified. No author/root source edits were made. This is the bounded profile/save review; the parent owns full-component and remaining delayed-surface acceptance.

**One remaining focus defect:** `closeSavePrompt` at frozen lines 1532–1543 captures result ownership but not profile-access generation. A save on an independent primary commits and schedules its close frame. A mine-only revoke then clears the saved label/name/prompt and profile-derived secondary, while preserving that independent primary and result owner. The old frame still passes `resultIsCurrent(owner)`, focuses a connected save target and clears `saveReturnRef`. It needs the same access-generation ownership already applied to save completion. This fails both original and Freeze 1; the patch has not introduced it, but the scoped obsolete-save-completion requirement remains incomplete. Root and parent received the exact evidence before this report.

`native-run2/result.json` records actual Chrome 152.0.7977.83 execution: **13/14 candidate controls pass**, **5/14 original controls pass**, zero page errors and zero external browser requests. The remaining candidate failure is `queued-committed-save-close-after-mine-only-revoke`: access generation 2, primary/result retained, mine null, saved UI idle, one written chart, but focus changes from `typing` to `other`. This is focus ownership, not an account-read or storage rollback defect.

The corrected behaviors pass: an obsolete requested-save completion after edit cannot relabel the result; revoke and revoke/regrant during store import cannot publish stale errors; native post-write events that revoke or edit do not republish saved/name/context; delayed A2HS completion after edit/revoke cannot claim its one-time key or show a hint; old close frames after result invalidation do not steal focus. Ordinary save persistence, context, hint and focus still pass. Primary revoke scrubs private form/result state; mine-only revoke preserves an independent primary and removes its saved secondary; unmarked public-input secondary context remains independent.

**Persistence qualification:** these controls inspect real native localStorage directly. When the actual store's synchronous `zodiacs:profile` event revokes after `setItem`, the chart remains committed exactly once. Likewise, an explicitly requested captured chart still saves after input editing while its store import is pending, as the author contract intentionally retains; only its obsolete UI completion is suppressed. No rollback or cancellation claim is made.

**Reader qualification:** the exact `runIsCurrent` arrow still returns true if a synthetic `canRead` synchronously dispatches revoke and then returns true. The nested actual access callback clears state and advances generations, but equality was checked before the outer return. Both original and candidate share this conditional behavior. The actual frozen Base provider is a frozen object reading native lease/grant/owner state synchronously; it does not dispatch such a callback. Its actual browser controls reject missing lease, mismatched owner and revoked lease, while allowing the matching owner. No ordinary production auth bypass was established.

Method: TypeScript AST extracts exact component functions, onRevoke and hook onAccess callbacks. Only the two commitSave dynamic-import expressions are replaced with controlled promises. Unchanged store/read guard/post-chart-context/A2HS modules run in Chrome with native localStorage, sessionStorage, EventTarget and requestAnimationFrame. The actual Base reader is also extracted unchanged. Surrounding component state setters are observable probes; **this is not a full ChartCalculator render or a hook mount-timing test**. The parent independently owns those checks. Entry/callback/source/module hashes, the driver, complete JSON observations and raw logs are retained. Synthetic fixture records are the only data used. The legacy cloud-sync import is externalized and unreachable under the actual account-v2 enabled/empty-Supabase build settings.

The first sandbox attempt could not bind the owned loopback server (EPERM); approved local execution then succeeded. A retained esbuild warning concerns an unused locale import.meta branch; no locale function is invoked by these store controls, and there are no runtime errors. The first completed browser run passed all then-present 13 candidate cases; the additional focus control produces the retained 14-case run. No test or source budget was weakened. No frozen implementation approval is issued until the focus correction and the parent's independent findings are resolved.
