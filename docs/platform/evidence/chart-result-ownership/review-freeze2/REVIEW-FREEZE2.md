# Independent ChartCalculator ownership review — Freeze 2

Accepted within the bounded review scope for patch 32b7413f9fcce17b192fa9f3fe28847d6b6ec7bfccc39b4437fee60df44a8e9f, component fea737333ea5df9a894dafa99de06cd1dcc8390f3bd43d9e627992b466dfa4d0, exact base 30b41cd8f1a353cce0cee36bc76c1e9fa21b4c14. The complete four-file diff was read: component, updated locale source assertions, new ownership tests and browser driver. No product, root, source or shared documentation was edited.

## Executed results

- **22/22 independently designed native component groups pass.** The original controls retry focus and delayed revoked-mine failures now pass. Date, time, house and city edits fence pending engine/helper calculations and remove stale results, receipt/action/context state. Late signature, share artifact, share completion, dialog and context work cannot restore an old result. Current failures clear old output and recover; optional signature rejection is observed immediately; unmount blocks later calculation. Existing known/unknown paths still calculate as before. Valid, invalid and conflicting positions initialization cannot overwrite newer intent.
- **5/5 unmodified production-build groups pass.** EN/RU shared links retain expected birth fields and receipt availability. Positions-only initialization requests no full/chart-adapter/calculator-receipt chunk. Both revoke and revoke/regrant while positions loading preserve public positions, then permit a fresh anonymous chart without reattaching the obsolete saved-mine UUID. No source or HTTP response rewriting is used; only bounded local transport delays and a synthetic existing access reader/event.
- Positive native cases preserve unchanged admitted profile-mine and account-free mine ID/input context. This prevents an overly broad fix from erasing valid anonymous/public context.
- The exact frozen browser driver independently passes **17/17** from a checkout alias containing spaces, with an explicit Chrome path and an OUT_DIR containing spaces. OUT_DIR wins over the conflicting legacy evidence path, which is never created. These are re-executed author-driver cases for portability; they are not counted as independent test design.

## Source and architectural conclusions

The old controls load may cache its useful module; only its focus/error intent is bound to the captured run/revision/result. The optional saved-mine admission checks the captured access generation after calling the existing reader, so revoke/regrant does not revive the stale reference. Public positions remain account-free. Save UI additionally captures prompt generation; clear/revoke/reopen invalidates obsolete completion without undoing persistence. Its close frame captures access and prompt generation without advancing generation on ordinary close, so a legitimate committed-save hint can still appear. The separate child's native store/prompt/RAF evidence supplies independent validation of this save boundary and must be retained alongside this report.

All 62 static/dynamic module references match the original source. Numerical statements from effectiveTime through the existing unknown-time endpoint branch are identical after TypeScript AST printing (SHA-256 095c181031ef2ab158bfb5f254427fbc013bac67432adb7d3f60466fc029ce98). This preserves existing calculation behavior; it does not prove complete local-date coverage or whole-date Sun/Moon candidates. No interval activation, SDK/metadata, storage schema, account authority, richer saved-record feature, styles or new localization strings were introduced.

The existing profile-access callback contract remains unchanged. Synthetic arbitrary callbacks that revoke while returning true are conditional reentrancy probes, not proof of an ordinary authentication bypass or an authentication guarantee. Already requested saves may remain committed after edits; only obsolete UI completion is rejected, with no rollback or retry promise. Harmless ancillary module caching is allowed.

## Evidence and limitations

Execution uses Node22.23.2 and Chrome152.0.7977.83, actual Preact/ChartCalculator/BirthFields/PlaceSearch, actual rc.6 calculation/codecs/context store, and explicit bounded import/share/geosearch controls. The IIFE fixture is not import-graph proof; the separate untouched Astro build supplies native graph checks. The complete source hash is checked before and after; compressed exact fixture bundles and all input hashes are preserved. Browser/server contexts close; external test-page requests are blocked.

The portable driver imports the existing browser helper through pathToFileURL, honors explicit/managed/system browser selection and the shared stable args, and has no machine-specific executable/evidence path. Only macOS/Chrome was executed; no Linux/Windows runtime proof is claimed. It may require the repository's previously built dist and normal dependencies, as its existing contract does.

Freeze1 remains immutable at manifest cf4546294d6afc03c9ee26a7d6002278a2d8a5b8d93f79b7d2355a979b9eaaa9, preserving baseline counterexamples and failures. Its component-level controls-retry failure remains qualified; exploratory static transport attempts did not reproduce a successful retry in that older build. Freeze2 independently passes that same component schedule.

Root remains accountable for integrating the reviewed source, complete release gates and any release decision. This report claims no publication, production deployment or external adoption.
