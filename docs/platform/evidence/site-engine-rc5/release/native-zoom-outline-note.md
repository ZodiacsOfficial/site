The first new native-zoom harness incorrectly required a 2 CSS-pixel outline for both regions. Existing src/styles/base.css:79 specifies 1.5px for the developer code block; support explicitly overrides to 2px. The captured control was active, :focus-visible and solid, with the exact existing 1.5px width. The corrected harness checks those two exact declared widths rather than changing site styling or loosening a pre-existing gate. Original result, screenshot and harness are retained in native-zoom-initial-outline/.
