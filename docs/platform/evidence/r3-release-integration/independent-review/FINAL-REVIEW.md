# Independent R3 site integration review

Disposition: **no integration source blocker found** in the resolved candidate
at `/private/tmp/zodiacs-r3-site`. This accepts the bounded preservation review;
root owns final build, functional tests, captures, hosted CI and preview evidence.
No shared source, canonical document, Git ref or publication was changed by this reviewer.

## Source identity and scope

Reviewed main is `1d7d0d0e3baf06f813e9f164aa8ec5394bbd6af7`; the resolved
merge selects `60f9c779b69935e93dd14b7f8aac90dbbd4a6d35`, which includes accepted
draft #469 at `35301d24907b6eb7117a1f4b0ae690ca42c57de6`. At the review snapshot,
HEAD was main and MERGE_HEAD was the selected documentation closeout; no unresolved
paths remained. The candidate was not yet committed. `candidate-review.json`
records the exact composed source hashes so this review can be attached to the
final commit only if those source identities remain equal.

The main tree was refreshed directly from GitHub and is complete (9,397 entries,
not truncated). Its only repository instruction file is root `CLAUDE.md`; no
`AGENTS.md` exists in that tree. CLAUDE is byte-identical to the accepted instructions.

## Reviewed resolutions

- **Today:** main's `editionDate`/`bodies` props remain supplied by the Astro page.
  The client does not import the daily edition JSON. The accepted dynamic contact
  arithmetic and transit phrasing load together; all three contact calls use the
  composed module and current `bodies` props. Their exported names do not collide.
  The memo dependencies include `bodies`, and edition-dependent text/receipt IDs
  use `editionDate`. The loading fixture combines the main props with the accepted
  deferred-load/ownership controls. Source review found no lost main bundle fix
  or static arithmetic reintroduction.
- **Research:** the eleven tests retain actual committed-input generation and
  the R1 controlled market/pilot fixtures. Assertions still require market prose
  to change while sky evidence stays equal, empty pilot feed **and** publication,
  stale approval rejection, unchanged approved replay despite a changed candidate,
  and the first 25-hour observation despite a conflicting 49-hour snapshot.
  Main's exact automatic-template/publication-metadata and same-day published-item
  freeze tests remain. The bounded pilot is derived from the fixture edition.
  Production research logic is byte-identical to current main.
- **Horoscopes:** the isolated cloned same-sign fixture from #469 remains exact.
  It plants Venus in the anchor Moon sign with consistent longitude and retains
  the distinct-love-copy assertion. This preserves main's intended test condition
  without globally changing Venus in unrelated fixture days.
- **Dependencies:** the complete lockfile equals current main plus only the
  accepted engine declaration and engine package entry. Astro 7.3.2 and smol-toml
  1.8.0 remain; no older maintenance closure replaces them. The optional read-only
  SDK entry is unchanged. The rc.6 archive is exactly SHA256
  `09c3e63432f8ba2e9df05af137c42f65ab039740a207a89418d9e6470ea3db3e`.
- **Published evidence:** all 28 immutable research item files in current main
  are byte-identical in the candidate. This includes all fourteen conflicting
  identities and fourteen items missing from #469. Main's market archive,
  publication ledger/feed/index, sign output and canonical sources are retained.
  Of the 46 root-listed conflict paths, only the intentionally regenerated daily
  manifest differs: it updates engine version and generator hash, retaining
  publication content hashes and all other provenance values.
- **Concurrent fixes and gates:** strict provider-null handling, unpriced sign
  standings, their tests, Registry publication freeze, canonical identity and
  approval files, service worker, daily freshness, normal/widget performance gates
  and scope guard retain exact main bytes. All 33 frozen C-016 caption files retain
  their accepted hashes. Before fresh captures, 70 of the 77 main-changed paths
  remain exactly main; the seven others are the reviewed allowance, lock, research
  and horoscope tests, manifest, Today component and loading test.
- **Scope:** independently running the unchanged guard against exact main passes:
  4,437 changed paths, precisely 102 protected paths authorized by the changed
  one-time allowance. The actual Astrofolio #416 diff has 36 isolated added paths;
  every one is absent from this candidate. Its documentation integration patches
  remain unapplied and its release remains separate.

## Evidence reuse and rollback limits

The unchanged rc.6 artifact, caption sources and retained arithmetic/ownership
modules permit reuse of their existing bounded evidence. They do not certify the
new combination of current compiler, Today props and Registry data. Fresh required
tree checks, captures, hosted CI and exact-head preview remain root's acceptance
work. No broader numerical/date coverage, human signoff or production behavior
is inferred from this source review.

The original `vendor/zodiacs-engine-0.1.0.tgz` remains byte-identical to current
main. Engine pin and related adapters must be rolled back together, with the
selected immutable archives preserved. This review confirms rollback materials
and the no-migration boundary; it does not claim that a production rollback or
an npm correction has been executed. Root's final release packet must identify
the exact candidate and rollback baseline.

SDK #5's explicit publication hold remains an owner gate. No merge to main,
npm publication, production deployment or L1–L6 work is authorized or performed.

## Review execution notes

The initial checker correctly stopped on a tracked directory symlink. It was
fixed to hash the link target as Git does, then all preservation assertions passed.
An unscoped `git diff --check` included historical raw evidence logs and reported
their preserved whitespace plus an inherited final blank line in the accepted
contacts module. This is not a product verification result and does not justify
rewriting sealed evidence. No source correction was requested.
