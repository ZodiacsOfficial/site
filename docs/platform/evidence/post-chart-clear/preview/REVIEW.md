# Refreshed Chart ownership draft #434 — actual preview acceptance

The bounded actual preview acceptance passes all nine groups for immutable source 3845a04bcea822da3c30a18848ef3d8d133b7b66. This is the updated Chart ownership and downstream-context draft, before the separate C014 date guard. It is not a production deployment or publication claim.

## Source, PR and provider identity

Fresh provider reads before and after browser verification identify deployment dpl_9PyqUeR6weQFUdYnAREXYEQjUAMZ at https://zodiacs-ecj7uiwv8-zodiacsofficial.vercel.app as READY, target null, source git, PR #434 and exact commit 3845a04bcea822da3c30a18848ef3d8d133b7b66. Sixteen immutable Git source files were independently snapshotted for identity and driver interpretation; no local product module was injected into a remote page and no local site build was used for this run.

The genuinely independent child refreshed live PR metadata and all seven file pages: 100×6+23 = 623 paths, comprising 610 added and 13 modified. Before/after PR metadata remains open draft with head 3845a04bcea822da3c30a18848ef3d8d133b7b66 and base abcf1a44e52040db79a369b49bc9b55d78099b22. All paths/statuses/API blob identities match immutable local Git; each content blob SHA1 was independently recomputed and SHA256 retained. There are 612 documentation paths and the expected 11 nondocumentation paths. Ten frozen source/test files match the reviewed Chart Freeze2 and C012 Freeze2; workflow adds exactly eight lines. The child's raw records/report/manifest are copied unchanged under pr-identity/. Main rehashed its sealed records. This is source identity evidence, not remote CI or human review approval.

## Actual remote browser scope

Chrome 152.0.7977.83, launched by pinned Node 22.23.2, exercised ordinary shared chart calculation, keyboard JSON receipt download, share dialog, edit clearing and explicit recalculation on EN/RU at 1280px and 390px. Downloaded envelopes parse through the installed exact rc6 codec and preserve the synthetic 1990-06-15 14:30 local resolution. Failure/recovery uses an explicitly declared host JSON.stringify fault limited to envelope serialization; it is not product source rewriting. The current error is focused and private fault detail is absent from rendered copy.

Two delayed-loader controls gate actual previously observed remote calculator-receipt and full module responses. After a form edit, neither stale completion calls natal; a new run while the receipt response is held produces one replacement result. Positions-only input requests no civil/engine module and has no calculation receipt. All nine groups pass with zero unhandled page errors and no stored profile. This reuses the previously independent remote driver design with the new exact target/source and adds passive clear/capture observation; it is a fresh execution, not a reuse of old c7 results. It does not reproduce every local controlled import/auth/save race remotely.

Native natal counts come from CDP precise coverage of the actual served /_astro/chart-adapter.K-QfJlbI.js, SHA256 20f5ee939b52b2c435ab12f4c3fd20e8a3195f1ec57a727d7933b9356fdbeb95. The driver parses the response AST to derive symbol Et from its one-input/returned-input+bodies+engineVersion structure, then observes 12 total calls across the groups. No local symbol is assumed to identify remote code. All 69 unique served JavaScript files and their hashes are retained in an explicit regular-file archive.

Forty-three observed clear notifications are native Event objects with no detail and with global chart context already absent. This preview renders zero chart-linked capture shells. Therefore this remote run does not verify signed-in daily preference, capture submit/reset, or authenticated lifecycle behavior; those remain supported by the separately sealed exact-source native and combined actual-page fixtures. No feature flag is inferred merely from the missing markup.

## Requests, access and artifacts

There are 993 observed test request events, 874 response records and 871 complete captured 200 responses; three response bodies became unavailable. Unpaired or unavailable events are not counted as completed responses. All tested API, external and non-GET requests were blocked. Nine configured Plausible script GETs were blocked; no API or non-GET request was observed in the nine test contexts. This is not a claim that production never contacts third parties.

Protected preview access used a separate disposable bootstrap context before test request observation. That bootstrap is deliberately recorded as unobserved, so zero-network or zero-external-bootstrap claims do not follow. The private local access directory was deleted, browser contexts closed, and in-memory orchestration access values cleared. While the exact generated private URL and nontrivial query values were still available, the cleanup scanned 155 then-existing raw files and 69 decompressed module archive members, including encoded forms, with zero matches. This does not claim server-side token revocation or retroactive observation of bootstrap traffic. New delivery records contain only these sanitized artifacts and descriptive prose.

Nine screenshots, four downloaded synthetic receipts, actual coverage records, request/response ledgers, module archive, source snapshot and before/after provider records are preserved. Both archives use explicit neutral TarInfo records without filesystem xattrs/AppleDouble. No whole generated site, node_modules, private access file or real user profile is included.

There was no browser/driver failure in this refreshed run. Historical failed setup/probes remain in their earlier immutable deliveries; they are not mislabeled as failures of this new target. No production, merge, publication, spending or outreach action was performed. All records are SHA256 hashed/sealed, not cryptographically signed. Release-ready and deployed status remain separate root decisions and gates.
