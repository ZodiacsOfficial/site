# Independent PR #434 identity at 3845a04b

Accepted for this bounded source-identity review. Fresh GitHub reads before and after complete file pagination both report the open draft **“Prevent obsolete birth-chart results and delayed action updates”** at head `3845a04bcea822da3c30a18848ef3d8d133b7b66`, base `abcf1a44e52040db79a369b49bc9b55d78099b22`. The local merge base equals that exact declared base.

All **623** changed paths were retrieved in seven API pages (100, 100, 100, 100, 100, 100, 23), with no duplicates. Every path and addition/modification status matches the immutable local Git diff. Every returned Git blob SHA matches the local tree; the verifier independently recomputed each blob SHA-1 from its exact content and retained its byte length and SHA-256. There are **610 additions and 13 modifications**, comprising **612 paths under docs/** and **11 other paths**. This supersedes no earlier evidence: it is a fresh proof for this exact head, not the prior c7 inspection.

The entire non-documentation scope is the four accepted ChartCalculator ownership Freeze 2 files, the six accepted C-012 downstream Freeze 2 files, and `.github/workflows/site-check.yml`. All ten reviewed file byte lengths and SHA-256 hashes match their original frozen identities exactly. In particular, the ChartCalculator hash is `fea737333ea5df9a894dafa99de06cd1dcc8390f3bd43d9e627992b466dfa4d0`; the corrected downstream panel hash is `3dd16171ac8f19d47d380ed9ec22d51fcfc6f8284697c27f0418a68a1be66158`.

The workflow differs from the PR base by exactly eight added lines: the two previously reviewed native result-ownership steps, each with a five-minute timeout and output inside the existing always-uploaded visual-artifact directory. Its exact SHA-256 is `286ca48156fb294f2af629fdf973d921565c5601142396a4d31fe5f1d95633a9`. No trigger, permission, secret requirement, setup, dependency, or browser pin changes are included. The browser helper, package manifests, existing time resolver and Moon island have identical base/head Git blobs. C-014 date-reference activation is therefore outside this PR's product delta at this head.

The first sandboxed GitHub read failed to connect; its empty stdout and exact error are retained. The subsequent approved read-only API requests all completed successfully. No remote mutation, publication, production access, or authentication-secret capture was performed. The API results contain ordinary public PR/file metadata, not credential headers.

This is an exact-source and CI-wiring conclusion. It does not establish remote CI success, preview behavior, deployment, backend behavior, or numerical correctness. Documentation contents are identity-matched, not substantively reviewed in this subtask. Parent review owns preview/browser work; root remains the integrator. `identity-result.json`, `changed-file-identities.json`, the raw API responses, exact source extracts, diffs, verifier and command ledger are the evidence.
