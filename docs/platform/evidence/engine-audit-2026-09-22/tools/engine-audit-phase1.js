export const meta = {
  name: 'engine-audit-phase1',
  description: 'Audit the whole Zodiacs engine stack (production @zodiacs/engine + site wrappers + precision-alpha) across nine bounded dimensions, with fresh measurements against Swiss, DE440s and Horizons where they change a conclusion',
  phases: [{ title: 'Audit', detail: 'nine bounded finders, each returns evidenced findings, measurements and a Swiss gap list' }],
}

const ROOT = '<worktree>'
const SCRATCH = '<audit>'

const PREAMBLE = `
You are one of nine bounded auditors of the Zodiacs astrology engine stack. The owner's stated goal is an engine that is SUBSTANTIALLY MORE ACCURATE AND MORE ADVANCED THAN SWISS EPHEMERIS. Your job is to find what is wrong, unsound, unsupported, missing, or weaker than Swiss in your dimension, with evidence a sceptical engineer can reproduce. Vague code-smell impressions are worthless here; a finding is a specific claim + the file:line + a measurement or a reproduction. Where a measurement would change the conclusion, take it (the tools are below). Where an existing committed measurement already answers the question, cite it instead of re-running it.

REPOSITORY (read-only worktree, branch claude/eager-ramanujan-razak3 at 39b7a127, an open PR depends on it):
  ${ROOT}
Do NOT modify, stage, commit, stash or push anything in that tree. Do not run repo scripts that write into the tree (regenerators, build-*.mjs that emit into src/ or public/). Put every scratch script, data file and output under
  ${SCRATCH}/<your-area>/
and reference those paths in your output.

THE STACK, as it actually is (verified before you were spawned):
  - Production engine = the vendored npm package @zodiacs/engine 0.1.1-rc.6. Its SOURCE lives in a separate repository (ZodiacsOfficial/sdk, packages/engine) that is NOT in this checkout; its BUILT JS is readable at ${ROOT}/node_modules/@zodiacs/engine/dist/ (index.js = public API: natalChart/transits/synastry/saturnReturn/moonPhase/findLongitudeCrossings; chunk-GBH7JIYF.js = ephemeris.ts: planets via astronomy-engine GeoVector(body,time,true) rotated by Rotation_EQJ_ECT, Moon via EclipticGeoMoon, true node from GeoMoonState angular momentum, speed = central difference +-0.25 day; chunk-H7RJKCBO.js = houses.ts: GAST + MEAN obliquity angles, whole-sign and Placidus only, |lat|>66 -> whole-sign fallback; chunk-INI453VG.js = aspects (5 majors, fixed orbs) + signs; receipt.js = receipt CONVENTIONS block; geo.js = local time via Intl). astronomy-engine 2.1.19 is at ${ROOT}/node_modules/astronomy-engine (VSOP87 planets, NOVAS-derived Moon, custom Pluto integrator, its own Delta-T model, a nutation that keeps only 5 of the 77 IAU 2000B terms).
  - Site wrappers and site-side astronomy: ${ROOT}/src/lib/engine/*.ts (full.ts, houses.ts, aspects.ts, lite.ts, returns.ts, lunar-return.ts, solar-return.ts, progressions.ts, transit-scan-core.ts, transit-window-core.ts, longitude-crossings.ts, void-of-course.ts, year-scan.ts, synastry.ts, portable.ts, server-ephemeris.ts, engine.test.ts = the accuracy gate), ${ROOT}/src/lib/time/*.ts (localToUtc.ts etc.), ${ROOT}/src/lib/declination.ts, dignities.ts, wheel/ecliptic3d.ts, compare/*.ts, ${ROOT}/src/mcp/*.ts, scripts/build-transits.mjs, build-sky.mjs, build-eclipses.mjs, build-ingresses.mjs.
  - Research runtime = ${ROOT}/examples/precision-alpha (README.md, REPORT.md, the *-RESULTS.md ladder: EVALUATION, RETARDED, ABERRATED, OF-DATE, DEFLECTION (verdict FAIL), PARTITION (verdict PARTIAL); src/core/*.mjs = container, ephemeris (Chebyshev), nutation (IAU 2000B, 77 terms), frames (bias/precession/nutation matrices), reduce (apparent place: TDB-TT, light-time, solar deflection, relativistic aberration, BPN), interval, trig, interval-search, search, validated-search, retarded, aberration, retarded-search, frame-of-date, deflection, domain-partition, partitioned-search, result; tools/compiler (Chebyshev pack compiler from a DE440s SPK), tools/spk-backend.mjs, tools/measure/*, test/tier-a (no data) and test/tier-b (needs PRECISION_PACK and PRECISION_KERNEL env vars)).
  - Evidence already committed (cite rather than redo): ${ROOT}/docs/engine-validation/README.md (the honest summary of every measurement so far), docs/engine-validation/{swiss-node-polar,swiss-eight-cases,swiss-lunar-return,transit-windows}/, docs/platform/evidence/swiss-benchmark/ (RESULTS.md, CONFIGURATION.md, NEXT.md; 180-measurement distribution: shipped engine vs Swiss p50 1.88" p95 14.90" max 159.38", the max being a Delta-T convention difference of 109.5 s at 2100), docs/platform/evidence/precision-2026-09-20/ (README.md, CLOSEOUT.md, numerics/, compiler/, search/), docs/platform/evidence/precision-{deflection,partition}/, examples/precision-alpha/CHART-ADAPTER-CONTRACT.md (what a full-chart integration of the alpha would need; notes that production applies no light-time/aberration to the Moon and that 32.87% of days 1900-2100 have a body inside the 5-degree solar floor).

TOOLS AND DATA AVAILABLE ON THIS MACHINE (4 CPUs; keep any single measurement under ~15 minutes of compute and say how long it took):
  - node v22.22.2 (ESM). The worktree's node_modules has astronomy-engine, @zodiacs/engine, vitest. examples/precision-alpha has no dependencies; 'npm test' there runs tier A (node --test).
  - python3.11 with pyswisseph 2.10.03 (system python; a venv with numpy 2.4 + pyswisseph is at <swisslab>/venv/bin/python). Swiss Ephemeris is an INSTRUMENT here, never a fitting target, and nothing Swiss may be copied into the repo.
  - Swiss data: <swisslab>/ephe/{sepl_18.se1,semo_18.se1} (planets + Moon, 1800-2400 only; use swe.set_ephe_path and ALWAYS read the returned flag to confirm SWIEPH actually answered, not MOSEPH). swe.FLG_CENTER_BODY, FLG_JPLEPH, FLG_TOPOCTR, FLG_SIDEREAL, FLG_TRUEPOS, FLG_NOABERR, FLG_NOGDEFL, FLG_J2000, FLG_NONUT, FLG_SPEED, FLG_XYZ, FLG_EQUATORIAL exist; swe.set_jpl_file('de440s.bsp') with swe.set_ephe_path('<swisslab>') lets Swiss read DE440s directly (FLG_JPLEPH) so Swiss-with-DE440s vs Swiss-with-.se1 isolates the DE431->DE440 series difference, and Swiss-with-DE440s vs the alpha isolates reduction differences.
  - JPL kernels: <swisslab>/de440s.bsp (DE440s, 1849-2150; the alpha's tier-b PRECISION_KERNEL), satellite kernels <satkernels>/{jup348,sat480,nep097,plu060}.bsp (planet CENTRES relative to their system barycentres). A JS SPK reader exists at ${ROOT}/docs/platform/evidence/swiss-benchmark/prototype/spk.mjs and a compiler-side one at examples/precision-alpha/tools/compiler/spkref.mjs. 'pip download' and 'pip install' work (jplephem is installable into a fresh venv under ${SCRATCH} if you prefer it).
  - JPL Horizons API is reachable (https://ssd.jpl.nasa.gov/api/horizons.api). It is the closest thing to an arbiter available (DE441 + a full apparent-place reduction, independent of both implementations' code). Be frugal: at most ~30 requests total, cache every response under ${SCRATCH}/<area>/horizons/, and record the exact query strings. Use QUANTITIES='31' (observer ecliptic lon/lat, true-of-date) with CENTER='500@399' for geocentric apparent, or vector tables for geometric checks; note Horizons applies its own light-time/aberration/deflection settings — read what you asked for.
  - The precision alpha's own harness: examples/precision-alpha/tools/measure/swiss.py (matched-convention Swiss dump), four-configurations.mjs, cost-baseline.mjs; docs/platform/evidence/swiss-benchmark/tools/{corpus.mjs,dump-zodiacs.mjs,dump_swiss.py,compare.mjs} (the 180-case corpus and comparator). Copy them to scratch before adapting.
  - To run the alpha's tier-b tests you need a compiled pack: 'node examples/precision-alpha/tools/compiler/compile.mjs --help' (read tools/compiler/SOURCE.md first) can compile one from de440s.bsp into ${SCRATCH}; there is NO committed real pack. Synthetic packs: examples/precision-alpha/examples/synthetic-pack.mjs and docs/platform/evidence/precision-partition/cross-runtime/pack.bin.

SEVERITY SCALE (use exactly these words):
  blocking = a shipped or documented behaviour returns wrong answers, or a documented guarantee/claim is unsound or false.
  major    = an accuracy, completeness or capability gap that keeps the stack materially below Swiss Ephemeris, or a claim that is unsupported by the evidence in the tree.
  minor    = a real but small inaccuracy, inconsistency or ergonomic defect.
  note     = an observation or an opportunity to exceed Swiss, with evidence of its size.

For every finding give: a stable id ('<area>-N'), severity, a one-line title, the precise claim, the evidence (file:line references, numbers with units, the command that produced them, and the artifact path under scratch), a reproduction recipe, the impact on the goal of exceeding Swiss, a concrete proposed fix, and an effort estimate in engineer-days. A finding you could not confirm is labelled 'unconfirmed' inside its claim rather than dropped. Count things you did NOT examine in notExamined. Do not repeat what docs/engine-validation/README.md already concedes unless you can add a measurement or show the concession is itself wrong.

Your final message is data: return exactly the structured object requested, nothing else.
`

const FINDINGS = {
  type: 'object',
  required: ['area', 'summary', 'findings', 'measurements', 'swissGap', 'notExamined'],
  properties: {
    area: { type: 'string' },
    summary: { type: 'string', description: 'five to ten sentences: the state of this dimension and the two or three things that matter most' },
    findings: {
      type: 'array',
      items: {
        type: 'object',
        required: ['id', 'severity', 'title', 'claim', 'evidence', 'reproduction', 'impact', 'fix', 'effortDays'],
        properties: {
          id: { type: 'string' },
          severity: { type: 'string', enum: ['blocking', 'major', 'minor', 'note'] },
          title: { type: 'string' },
          claim: { type: 'string' },
          evidence: { type: 'string' },
          reproduction: { type: 'string' },
          impact: { type: 'string' },
          fix: { type: 'string' },
          effortDays: { type: 'number' },
          files: { type: 'array', items: { type: 'string' } },
        },
      },
    },
    measurements: {
      type: 'array',
      items: {
        type: 'object',
        required: ['name', 'method', 'result'],
        properties: {
          name: { type: 'string' },
          method: { type: 'string' },
          result: { type: 'string' },
          artifactPath: { type: 'string' },
          computeMinutes: { type: 'number' },
        },
      },
    },
    swissGap: {
      type: 'array',
      description: 'capability by capability in this dimension: what Swiss does, what Zodiacs does, and the concrete path (if any) to exceeding Swiss',
      items: {
        type: 'object',
        required: ['capability', 'swiss', 'zodiacs', 'exceedPath'],
        properties: {
          capability: { type: 'string' },
          swiss: { type: 'string' },
          zodiacs: { type: 'string' },
          exceedPath: { type: 'string' },
          evidence: { type: 'string' },
        },
      },
    },
    notExamined: { type: 'array', items: { type: 'string' } },
  },
}

const AREAS = [
  {
    key: 'production-positions',
    prompt: `AREA: production body positions — the physics of what @zodiacs/engine actually returns for the Sun, Moon, eight planets and the nodes.
Read ${ROOT}/node_modules/@zodiacs/engine/dist/chunk-GBH7JIYF.js end to end, then the astronomy-engine code paths it calls (${ROOT}/node_modules/astronomy-engine/astronomy.js: GeoVector, EclipticGeoMoon, GeoMoonState, Rotation_EQJ_ECT, e_tilt/iau2000b, CorrectLightTravelTime, the Pluto model, MakeTime/DeltaT). Establish, with measurements where the committed evidence does not already, each of the following, and report every one as a finding with its measured size:
 1. The reduction chain per body class (planets vs Moon vs nodes): which corrections are applied (light-time, annual aberration, gravitational deflection, nutation, precession, frame bias) and which are silently absent. The Moon route (EclipticGeoMoon) applies no explicit light-time/aberration — quantify what that costs in arcseconds against Swiss with matched conventions and against Swiss with those effects toggled off, so the cause is attributed and not assumed.
 2. Nutation: astronomy-engine's iau2000b keeps only the first 5 luni-solar terms. Reproduce the size of the resulting longitude error over a modern span (the committed numerics say up to 0.2677"); confirm it is present in the shipped package's output, not just in astronomy-engine.
 3. Gravitational deflection: absent in the production chain. Measure its size on the shipped output (Swiss FLG_NOGDEFL toggle) for bodies near the Sun.
 4. Speed: central difference at +-6 h. Measure the error of that derivative against Swiss FLG_SPEED for the Moon (whose rate changes fastest) and near planetary stations; establish whether 'retrograde' can be wrong near a station and by how many hours.
 5. True node: computed from the instantaneous geocentric Moon orbit plane (angular momentum of GeoMoonState). Compare with Swiss TRUE_NODE and MEAN_NODE over a year: residual distribution, and whether its definition matches Swiss's osculating node (it should be the same geometric object — show it, or show it is not). Node latitude is reported as 0: correct by definition, note it.
 6. Series accuracy far from J2000: VSOP87 planets, the analytic Moon, and the custom Pluto integrator. The site accepts 1800-2199 but the package computes any year without refusal or flag. Measure residuals vs Swiss(.se1) at 1800, 1850, 1900, 2000, 2100, 2150, 2200, 2400 for every body (Swiss data covers 1800-2400), and additionally vs Swiss reading DE440s directly (FLG_JPLEPH, 1850-2150) so that the DE431-vs-DE440 series difference is separated from the astronomy-engine-vs-DE difference. Also measure Swiss(.se1) vs Swiss(DE440s) itself at a few epochs: this is the size of the improvement available merely by moving to DE440/441, which is a key number for the owner's goal.
 7. Delta-T: which model astronomy-engine applies, its divergence from Swiss's (the committed benchmark found 109.5 s at 2100) across 1800-2200, and the resulting Moon displacement. State whether the package exposes or documents Delta-T at all.
 8. Anything else you find in that chunk (e.g. obliquity input to the ecliptic rotation, use of UTC where TT/UT1 is meant, the order of the Rotation_EQJ_ECT application, latitude computation).
Use the committed 180-case corpus (docs/platform/evidence/swiss-benchmark/tools/corpus.mjs) where a distribution is needed, and the alpha's tools/measure/swiss.py as the Swiss dump. Take at most ~10 Horizons queries if you want an arbiter for one or two epochs (e.g. Neptune 2020-01-01, Moon 2100-01-01) — record what Horizons's reduction settings were.`,
  },
  {
    key: 'angles-houses-aspects',
    prompt: `AREA: angles, house systems, aspects and chart-level astrological computation.
Read ${ROOT}/node_modules/@zodiacs/engine/dist/chunk-H7RJKCBO.js (houses.ts) and chunk-INI453VG.js (aspects/signs) end to end, the site wrappers ${ROOT}/src/lib/engine/{houses,aspects,synastry}.ts, ${ROOT}/src/lib/{declination,dignities,compat,composite,chart-shape,aspect-pattern-model}.ts and ${ROOT}/src/lib/wheel/ecliptic3d.ts, and the frozen Swiss house fixtures in ${ROOT}/src/lib/engine/fixtures/swiss-node-polar*.json with docs/engine-validation/swiss-node-polar/README.md. Establish, with measurements against Swiss swe.houses_ex / swe.houses_ex2 where the committed evidence does not already:
 1. Angles: the MC/ASC use apparent sidereal time (GAST, which carries nutation in right ascension) but MEAN obliquity — an inconsistent pairing; Swiss uses true obliquity with apparent sidereal time. Measure the resulting ASC/MC error across latitudes 0-66 and dates 1800-2200 (nutation in obliquity is up to ~9.2"). Also check the obliquity's time argument (centuriesSinceJ2000 uses the UTC Date, not TT) and the ASC quadrant logic 'if normalize(asc - mc) >= 180 asc += 180' for correctness at all latitudes including where the ecliptic's ascending point is degenerate.
 2. Placidus: verify the iteration (offsets 30/60/120/150, factors 1/3, 2/3, 64 iterations, 1e-9 convergence, 'argument >= 1' bail-out) against Swiss 'P' cusps over a grid of latitudes including 55-66 degrees, both hemispheres, and epochs; report max cusp residual and any failure to converge; establish what happens exactly at 66 and just above (the |lat|>66 refusal is a hard cut; Swiss computes Placidus up to the polar circle 66.56 and falls back to Porphyry). Confirm the fallback is to whole sign and is flagged.
 3. House placement: houseOf boundary semantics (a body exactly on a cusp; cusp spans of 0; wrap-around), and consistency with Swiss house_pos.
 4. Aspects: orb table (8/4/7/7/8 with luminary 10/5/8/8/10), the 'applying' test (a 0.02-day step using both speeds; check it is correct for retrograde pairs, for pairs straddling the 180-degree wrap, and for bodies near stations), aspects excluded for nodes, no minor aspects, no angle aspects, no declination aspects in the package (the site has declination.ts using a FIXED J2000 obliquity 23.4392911 — measure the declination error that constant causes at 1900, 1950, 2050 for the Moon and Pluto; and it derives declination from lon/lat with what formula — check it).
 5. Site astrology tables: dignities (classical table correctness: verify each row against the standard Ptolemaic scheme and name any deviation), composite/midpoint computation (midpoint of longitudes across the 0-degree wrap), chart-shape and aspect-pattern detection thresholds (are they defined and sane, are they tested).
 6. What Swiss offers here that this does not: ~24 house systems (Koch, Regiomontanus, Campanus, Equal, Porphyry, Alcabitius, Morinus, Meridian, Vehlow, Gauquelin sectors, APC, Sunshine, Krusinski, topocentric...), house_pos for any system, Gauquelin, the polar handling per system (list Swiss's per-system polar behaviour from the Swiss documentation you know; verify one or two empirically with houses_ex), ARMC-based functions, azimuth/altitude. Be precise about which of these matter astrologically (Koch/Equal/Porphyry/Regiomontanus/Campanus dominate practice) and give the path to exceeding Swiss (e.g. exact closed forms where Swiss iterates, documented convergence guarantees, an explicit polar policy per system with flags instead of silent fallback).`,
  },
  {
    key: 'time',
    prompt: `AREA: time — local wall time to UTC, UTC to the astronomical time scales (UT1, TT, TDB), Delta-T, calendars, and how each layer of the stack treats them.
Read ${ROOT}/src/lib/time/*.ts (localToUtc.ts, local-date-intervals.ts, local-date-reference.ts, civil-date.ts, technical-locales.ts, and their tests), the SDK's ${ROOT}/node_modules/@zodiacs/engine/dist/geo.js (resolveBirth / timezone.ts), astronomy-engine's MakeTime/DeltaT (${ROOT}/node_modules/astronomy-engine/astronomy.js: search for DeltaT and TerrestrialTime), the receipt conventions in dist/receipt.js, and in the alpha ${ROOT}/examples/precision-alpha/src/core/reduce.mjs (TDB-TT) and src/core/frame-of-date.mjs (time arguments). Establish:
 1. resolveLocalToUtc samples the offset at wall-36h, wall, wall+36h and treats the set of distinct values as the candidate offsets. Construct or search for a real IANA case where that misses a transition (two transitions within 72 h; a transition of exactly the same offset value going and returning; zones with historical double shifts; the 'GMT' regex fallback returning 0 for an unparseable name) using the host ICU data (node 22): write a scan over all Intl.supportedValuesOf('timeZone') zones and every transition instant in 1850-2100 (find transitions by scanning offsetAt daily then bisecting) and check that resolveLocalToUtc round-trips every local minute around each transition; report every failure with the zone and date. Also check the gap policy (shift forward by the pre-transition offset) and fold policy (earlier instant) against the documented intent, and behaviour for negative DST and 'lmt' flagging.
 2. Calendar: everything is proleptic Gregorian. Birth data before 1582 (and before each country's adoption, e.g. Britain 1752, Russia 1918) are recorded in the Julian calendar; Swiss provides swe_julday with a calendar flag and swe_date_conversion. Establish that the stack has no Julian-calendar input path and what a user entering a 1500-06-15 Julian date actually gets (a different day). Check year 0 / negative years / years > 9999 through Date, Intl and astronomy-engine.
 3. UT1 vs UTC vs TT: the JS Date is UTC; astronomy-engine treats it as UT (no leap-second or UT1-UTC treatment) and converts to TT with its own Delta-T table/extrapolation. Quantify: the UT1-UTC neglect (<0.9 s, i.e. <0.5" for the Moon) and the Delta-T model vs Swiss's swe_deltat across 1600-2200 (Swiss data covers 1800-2400 but swe.deltat needs no data); tabulate the difference per 50 years and its Moon-longitude effect. Establish what model Swiss uses (Stephenson/Morrison/Hohenkerk 2016 + tidal acceleration adjustment per ephemeris in 2.10) and what astronomy-engine uses, from the code. Establish whether either the package or the site documents or exposes Delta-T or lets a caller pin it; the alpha requires the caller to supply TT and never sees Delta-T at all — state what a chart adapter would need.
 4. Sidereal time: SiderealTime(MakeTime(utc)) — GAST from a UT that is really UTC; obliquity from UTC centuries; house angles therefore have a time-scale inconsistency of order Delta-T in the MC (69 s of time = 17 arcmin of RAMC?? no: 69 s of TIME is 0.29 degrees of RAMC) — check whether the package converts UT->sidereal correctly (astronomy-engine's SiderealTime takes an AstroTime whose ut is used, good) and quantify any error introduced by feeding UTC where UT1 is meant (<1 s -> <15" in RAMC) and by the obliquity epoch.
 5. The alpha: TDB-TT two-term form (error <30 us — confirm the constants), TT handling in the Chebyshev evaluation (integer ms TT since J2000: confirm no precision loss across 1850-2150 and how the pack's epochs are referenced (TDB vs TT for the SPK time argument — SPK kernels are in TDB; check the compiler and the runtime agree)).
 6. Swiss gap: swe_deltat/deltat_ex with model selection and tidal acceleration, swe_set_delta_t_userdef, UT1 handling, Julian/Gregorian conversions, sidereal time functions, time-zone conversion helpers; what it would take to exceed Swiss here (e.g. a documented Delta-T with an UNCERTAINTY BAND carried into the result, IERS Bulletin A/C integration for UT1-UTC and leap seconds, an explicit calendar model, tzdb version pinning inside receipts).`,
  },
  {
    key: 'production-event-search',
    prompt: `AREA: production event finding — returns, crossings, transits, void-of-course, progressions, year scans, eclipses/ingresses data builders.
Read ${ROOT}/node_modules/@zodiacs/engine/dist/index.js (findLongitudeCrossings, groupIntoSeasons, computeSaturnReturns) and the site's ${ROOT}/src/lib/engine/{returns,lunar-return,solar-return,progressions,longitude-crossings,transit-scan-core,transit-scan-shared,transit-scan-server,transit-window-core,void-of-course,year-scan}.ts with their tests and fixtures (fixtures/swiss-lunar-return*.json, solar-return-crossings.fixture.json, fixtures/transit-window-independent.json), ${ROOT}/docs/engine-validation/{swiss-lunar-return,transit-windows}/README.md, and the data builders ${ROOT}/scripts/{build-transits,build-sky,build-eclipses,build-ingresses,build-birthdays}.mjs (what astronomy they call, what they claim). Establish, with constructed counterexamples run against the real functions where possible:
 1. findLongitudeCrossings: step sampling (default 5 days; who passes what), the 'abs(delta) < 90' guards, the exact-zero handling, 24 bisection iterations (resolution vs the ms floor), the 10,000-sample budget as a hard throw. Construct cases where an event is missed: a tangency (body reaches the target and turns back inside one step: e.g. a station within 0.05 degrees of the target), two crossings inside one step (Moon with a 5-day step, or Mercury near a station), and an event exactly at a sample. Report which are missed and which throw.
 2. Saturn return: window 26-92 years, gap grouping 400 days; check the natal speed (finite difference +-1 day) and whether a birth with Saturn stationary is handled; check what happens outside astronomy-engine's comfortable range.
 3. Solar return / lunar return: which longitude convention (of-date apparent, but the natal was computed at a different epoch — precession is irrelevant for tropical returns, note it); root precision; the Moon route without light-time/aberration (the lunar-return README concedes the two correction paths are not identical); whether the returned chart is recomputed at the found instant with full angles.
 4. Progressions: secondary progressions day-for-a-year — which year length (365.25 vs 365.2422 tropical vs 365.2564 sidereal; the mainstream convention is the tropical year, Solar Fire uses 365.2422, Swiss users usually 365.2422 or 365.25 by choice), which day length (mean solar day), whether progressed angles use solar-arc or naibod; check the code and name the convention it implements and whether it says so.
 5. transit-window-core and transit-scan-core: the half-day sampling with a 0.05 degree budget, the 'exactTopologyStatus' semantics, the Uranus D failed-incomplete contract; establish whether the 3-degree orb windows can lose an entry/exit at a half-day step for the slow planets (they cannot move 0.1 degree in 12 h, so show the bound), and whether the Moon/fast bodies are ever scanned with these tools.
 6. Void-of-course Moon: which definition (last Ptolemaic aspect to the seven classical planets before sign change; whether outer planets are included; whether applying-only; the orb rule; whether the sign-change instant is found precisely) and its test coverage; compare to the mainstream definitions.
 7. The data builders: build-eclipses.mjs, build-sky.mjs, build-ingresses.mjs, build-transits.mjs — what astronomy-engine searches they use (SearchLunarEclipse etc.), what they get right, what they cannot (local circumstances, contact times, magnitude), and their refresh cadence.
 8. Swiss gap: swe_solcross/mooncross/helio_cross, swe_sol_eclipse_when_loc/glob, lun_eclipse_when, lun_occult_when, rise_trans, heliacal, nod_aps (nodes/apsides), pheno (phase/elongation/magnitude), station finding (users do it by speed sign), and the fact that Swiss offers no completeness guarantee anywhere — which is exactly where Zodiacs can exceed it (the alpha's proven-complete validated search). Give the path.`,
  },
  {
    key: 'alpha-reduction-math',
    prompt: `AREA: the precision alpha's numerical core — the pointwise apparent-place reduction and its interval enclosures.
Read ${ROOT}/examples/precision-alpha/README.md, then src/core/{cheb,ephemeris,container,nutation,nutation-series-2000b,frames,reduce,trig,interval,retarded,aberration,deflection,frame-of-date}.mjs end to end, the ERFA test vectors in test/tier-a/_erfa-ld-vectors.json and tools/measure/{aberration-vs-erfa.mjs,erfa-ab.py,erfa-deflection/}, and the committed numerics track docs/platform/evidence/precision-2026-09-20/numerics/ (RESULTS.md, verify/). The reduction is documented at 0.0107" max against Swiss under matched conventions. Your job is to find what would stop this core from being the most accurate apparent-place reduction available, and anything unsound in the enclosures. Establish:
 1. Order and conventions of the reduction versus the ERFA/SOFA apparent-place chain (eraApcg/eraAtciq: BCRS geometric -> light-time -> gravitational deflection -> aberration -> bias-precession-nutation). Confirm the light-time solution uses the BARYCENTRIC positions at emission and the observer at reception, that the deflection uses the observer-Sun vector at reception and the emitter position at emission (eraLd's arguments), that aberration uses the observer's BARYCENTRIC velocity (not heliocentric), and that the frame bias is applied once and in the right direction. Name any deviation with its size (compute it on a few epochs against ERFA-derived vectors you generate with an independent implementation: pyerfa is pip-installable into a scratch venv — do that, and compare the alpha's pointwise output with an ERFA chain you write from a DE440s state read with jplephem; report max residual across ~200 epochs x 10 bodies; keep it under 15 minutes).
 2. TDB-TT: the two-term form; confirm the constants and that the error claim (<30 us) holds against the ERFA eraDtdb series on a sample.
 3. Nutation and precession: IAU 2000B 77 terms 'adjusted to P03', IAU 2006 precession (Fukushima-Williams); compare with pyerfa eraNut06a/eraNut00b, eraPfw06/eraFw2m, eraPnm06a on a sample; report the size of the 2000B-vs-2000A difference (the tree says <=0.0025") and whether the P03 adjustment is applied correctly.
 4. The Moon: DE440s stores EMB and the Moon geocentric; Earth = EMB - Moon/(1+EMRAT). Confirm the pack's EMRAT and the derivation, and that geocentric errors are not built from correlated terms as if independent (the README says so; verify the bound arithmetic in code).
 5. Outer planets are planetary-SYSTEM barycentres (DE440s has no body centres for Mars outward). Measure the centre-vs-barycentre offset in apparent longitude for Jupiter, Saturn, Neptune and Pluto using the satellite kernels at <satkernels> (jup348 has 599 relative to 5; sat480 699 relative to 6; nep097 899 relative to 8; plu060 999 relative to 9) — and state whether Swiss returns centres (test swe.FLG_CENTER_BODY on Jupiter and Pluto with .se1 and with DE440s via JPLEPH, and compare against your kernel-derived centre). This is a concrete accuracy item where the alpha may currently be WORSE than Swiss; quantify it in arcseconds per body.
 6. Interval enclosures (interval.mjs, trig.mjs, retarded.mjs, aberration.mjs, deflection.mjs, frame-of-date.mjs): JavaScript has no directed rounding. Establish how floating-point rounding is accounted for (ulp padding constants like ABS_ERR, outward rounding helpers) and whether the padding is JUSTIFIED (a documented error analysis) or asserted; look for any interval operation whose result can be narrower than the true image (division near zero, atan2/asin near branch cuts, sqrt near 0, Chebyshev derivative bounds Sum|c_k|k^2, the mean-value enclosure using an unbounded derivative); try to construct a counterexample (an input interval where the enclosure excludes the true value) and report it with numbers, or report that you tried N families and failed, naming them.
 7. Chebyshev evaluation: Clenshaw vs direct recurrence, error growth, the analytic derivative, the record boundary handling (t exactly at a boundary, the last record's inclusive end), and the pack's coefficient scaling.
 8. Swiss gap in this dimension: Swiss applies (per its documentation) light-time, aberration, solar deflection, IAU 2006 precession/Vondrak 2011 for long spans, IAU 2000B nutation, and offers topocentric (geodetic WGS84 observer with altitude), heliocentric, barycentric, J2000/astrometric variants, and speeds consistent with the apparent chain. Give the path to exceeding Swiss: planetary deflection (Jupiter up to ~16 mas), IAU 2000A nutation, Vondrak precession for long spans, topocentric with a proper geodetic model and diurnal aberration, rigorous proven error bounds on every output (which Swiss cannot offer), and DE441 for the full historical range.`,
  },
  {
    key: 'alpha-search-partition',
    prompt: `AREA: the precision alpha's event search, validated search, restricted-domain modes, domain partition, budgets and result contracts.
Read ${ROOT}/examples/precision-alpha/{MIGRATION,EVALUATION-PLAN,EVALUATION-RESULTS,RETARDED-RESULTS,ABERRATED-RESULTS,OF-DATE-RESULTS,DEFLECTION-EVALUATION,DEFLECTION-RESULTS,PARTITION-EVALUATION,PARTITION-RESULTS,CHART-ADAPTER-CONTRACT}.md, then src/core/{interval-search,search,validated-search,retarded-search,domain-partition,partitioned-search,result,instrument}.mjs and src/experimental.mjs end to end, plus test/tier-a/{validated-search,retarded-search,domain-partition,partitioned-search,partition-enclosure-*}.nodetest.mjs and docs/platform/evidence/precision-partition/review/*.md. The ladder's own verdicts are: empirical-apparent = conditional completeness; validated-geometric = proven; retarded/aberrated/of-date = holdouts run; deflected = FAIL (6/20); partition = PARTIAL. Establish:
 1. Soundness of each completeness claim: for validated-geometric, check the exclusion test and monotonicity test really are statements about the stored polynomial (Sum|c_k| bounds, derivative bounds) and that the root isolation is complete (no interval is dropped, tangencies handled, window edges handled, the ms integer time grid cannot hide a root between adjacent ms). For the retarded/aberrated/of-date/deflected modes, check that every enclosure feeding the verdict is sound end to end (the enclosure of the whole reduced longitude, not just the position), and that 'unresolved' is emitted whenever a proof fails rather than a verdict being guessed. Try to construct a counterexample on a synthetic pack (examples/synthetic-pack.mjs lets you build exact polynomial motion with known roots: build a double root, a root at the window edge, a root within 1 ms of another, a body that grazes the target without crossing, a retrograde loop crossing the target three times within one cell) and run each mode; report any missed, extra or misclassified event with numbers.
 2. Budget and cost accounting: confirm that evaluations are counted at exactly one place, that budgets are whole-request (not per subinterval), that cancellation is a result state, and that the partition + subsearches + verification share one budget. Look for any path that spends evaluations outside the counted region (e.g. plan construction, robustness probes, verification passes) and report what fraction is uncounted on a real case.
 3. The domain partition: the five-degree solar floor, the two guards (admit vs exclude), the boundary spans (never exact instants), the cache identity (pack digest + structure, profile, tolerances, boundary policy) and the refusal of imported plans; check the key includes everything the partition's truth depends on (e.g. the light-time tolerance, the relight ratio, EMRAT, the nutation model, the interval padding constants — anything whose change would change spans without changing the key is a defect). Report any omission.
 4. The FAIL and PARTIAL verdicts: state, from the numbers in the results files, why the deflected mode fails (cost 47-831x the rung below, 6/20 completeness) and whether the partition's remaining failures (P-3 boundary width 5.27% on the Moon; P-4 ratios 3.17-3.95x vs 4x) are structural (mean-value enclosure conservatism, 25-72x wider than true variation per the tree) or fixable — and by what (higher-order Taylor/Chebyshev enclosures of the elongation, a dedicated conjunction locator with a proved bracket, adaptive tolerance). Estimate the gain with a small experiment if you can (e.g. measure the enclosure width ratio on ten cells and what a second-order term would recover).
 5. Result contract (result.mjs, zodiacs-precision-search/2): check buildResult's invariants really prevent a self-contradictory result, the 'support' vocabulary (none/conditional/proven), the unverified-assumption list, the not-decided list; check the TypeScript declarations in types/ match the runtime; check experimental.mjs's gating is real (cannot reach experimental modes from the main entry).
 6. Missing capabilities the goal needs: stations/retrograde turns (a root of the derivative), latitude/declination/distance/elongation events, rise/set (topocentric), eclipses, occultations, and proven-complete versions of each; and a chart adapter (what CHART-ADAPTER-CONTRACT.md says is needed, and what it gets wrong or misses).
 7. Swiss gap: Swiss has iterative crossing finders (swe_solcross, mooncross, helio_cross since 2.10), eclipse/occultation finders and rise/set with no completeness or error-bound semantics at all. The alpha's typed verdicts and proven completeness are the strongest thing in this stack; give the concrete path from 'proven for geometric longitude' to 'proven for apparent longitude of date' (what enclosure is missing, at what cost), and to stations and eclipses.`,
  },
  {
    key: 'data-toolchain-packaging',
    prompt: `AREA: ephemeris data, the pack toolchain, provenance, integrity, licensing, packaging, and the consumer/API surface.
Read ${ROOT}/examples/precision-alpha/tools/compiler/{SOURCE.md,compile.mjs,cheb.mjs,format.mjs,runtime.mjs,sources.mjs,spkref.mjs}, tools/seal.mjs, tools/spk-backend.mjs, src/core/{container,source}.mjs, src/{index,node,browser}.mjs, types/*.d.ts, NOTICE.md, LICENSE-erfa, package.json, examples/*.mjs; docs/platform/evidence/precision-2026-09-20/compiler/RESULTS.md and RIGHTS.md; docs/platform/evidence/swiss-benchmark/LICENSING.md; ${ROOT}/vendor/README.md and the vendored @zodiacs/engine tarballs (the SOURCE of the production engine is in a separate repository not in this checkout — establish what that means for auditability and reproducible builds: can the tarball be rebuilt from a pinned commit? is the commit recorded? compare the tarball's sha256 with vendor/*.sha256 and with what package.json pins); ${ROOT}/scripts/{report-bundles,measure-engine-bundle,build-mcp-server,platform-engine-candidate,verify-platform-starter}.mjs; ${ROOT}/src/lib/engine/{portable,calculator-receipt,server-ephemeris}.ts; ${ROOT}/src/mcp/{server,tools,bounds}.ts; ${ROOT}/examples/{platform,mcp-server}/README.md; docs/platform/evidence/precision-partition/{consumer,cross-runtime}/. Establish:
 1. Pack format ZODEPH02: header layout, what the SHA-256 covers (everything before the digest — confirm by reading the code and by flipping one header byte of a synthetic pack: it must fail to open), bounds-checking before allocation, the v1 refusal, the seal tool. Authenticity is explicitly not established — state what a signed manifest (e.g. Ed25519 over the digest with a published key, or SLSA-style provenance) would add and cost.
 2. Compile a REAL pack from <swisslab>/de440s.bsp into ${SCRATCH}/data-toolchain-packaging/ following SOURCE.md (record the exact command, wall time, output size, and the proven error bounds the compiler prints), then run the alpha's tier-b tests with PRECISION_PACK=<that pack> PRECISION_KERNEL=<swisslab>/de440s.bsp ('npm run test:data' in examples/precision-alpha; ~ minutes). Report pass/fail per test and the numbers. This is the first time in this session the data-dependent tier has been exercised.
 3. Coverage and range: DE440s is 1849-2150; the site accepts 1800-2199 and the package computes any year. Establish exactly how the alpha refuses outside coverage (typed error, no silent fallback) and what a production integration would need for 1800-1849 and 2150-2199 (DE441 is ~2.6 GB for -13200..17191; DE440 full is 114 MB for 1550-2650; a Chebyshev pack over 1550-2650 at the same fidelity would be ~how many MiB by the compiler's own scaling — estimate from the committed 1.66 MiB / 300 y figure).
 4. Licensing and redistribution: JPL ephemerides (public domain, attribution), ERFA (LICENSE-erfa: derived series and vectors, the ERFA licence's conditions), Swiss Ephemeris (AGPL/commercial: confirm nothing Swiss-derived is in src/ or in any pack; grep for Swiss-derived constants), astronomy-engine (MIT), MIT for @zodiacs/engine. Name any gap in NOTICE.md.
 5. The production package: @zodiacs/engine is 'release candidate ... not published' (README says npm lookups 404). Establish the actual distribution state, the receipt CONVENTIONS strings' truthfulness against the code (e.g. 'planetPositions: apparent-geocentric-ecliptic-of-date' while the Moon route applies no light-time/aberration; 'moonNodes: instantaneous-geocentric-moon-orbit-plane'; 'angles: gast-and-mean-obliquity'), the ENGINE_VERSION pin tests, and the bundle-size gate (report-bundles.mjs: what it enforces).
 6. API surface for a Swiss-class engine: compare the public API (natalChart/transits/synastry/saturnReturn/moonPhase/positions/findLongitudeCrossings, plus the alpha's apparent()/search()/experimental modes) with what an engine that exceeds Swiss must expose: a uniform calc(body, time, flags)-style call returning lon/lat/dist/speeds with error bounds and provenance; body enumeration; coordinate-frame selection (of-date / J2000 / ICRS, ecliptic / equatorial / cartesian, geocentric / topocentric / heliocentric / barycentric); sidereal modes; time-scale inputs (UTC/UT1/TT/TDB) and Delta-T control; house systems; fixed stars; asteroids; a deterministic version/identity string on every result. Report the gap as findings with a proposed API shape (short, concrete), not a design essay.
 7. Cross-runtime determinism (the tree claims bit-identical plans/keys across Node/Chromium/Firefox on one digest-verified file): check the mechanism (no Math.hypot, no fused ops, integer ms) and whether any remaining source of runtime divergence exists (Math.sin/cos/atan2 differ across engines by ulps — how are those absorbed? if enclosures pad them, where is the padding justified?).`,
  },
  {
    key: 'verification-honesty',
    prompt: `AREA: verification, test infrastructure and the honesty of the claims in the tree — what is actually tested, against what, and what is asserted without a test or a receipt.
Read ${ROOT}/src/lib/engine/engine.test.ts and every *.test.ts in src/lib/engine and src/lib/time, the fixtures and their policies (src/lib/engine/fixtures/*), docs/engine-validation/README.md and each subdirectory README, docs/platform/evidence/swiss-benchmark/{RESULTS,CONFIGURATION}.md, examples/precision-alpha/test/tier-a/*.nodetest.mjs (list every family), test/tier-b/README.md, ACCEPTANCE*.md, the CI workflow ${ROOT}/.github/workflows/site-check.yml (which steps exercise the engine and the alpha, which do not), and the mutation-testing records under docs/platform/evidence/precision-partition/review/. Establish:
 1. The accuracy gate in engine.test.ts: JPL Horizons vectors as bare literals with an unrecorded provider version and query date 2026-07-05; gates of 0.05 degree / 0.15 degree / 0.2 degree that are 10-100x looser than the measured residuals (so a regression up to ~3 arcminutes would pass). Re-fetch two of those Horizons rows now (record the query and the Horizons version banner) and report whether the literals still match, and by how much; propose gate tightening with the margin justified.
 2. Frozen Swiss fixtures: verify the committed sha256s of the fixture files match (recompute), check that the policies were declared before comparison (dates in the READMEs), and that every fixture is actually executed by 'npm test' (run the vitest engine suite in the worktree: 'npx vitest run src/lib/engine src/lib/time' — report the counts and time; do not run the whole site suite).
 3. What is NOT tested: far dates (only 1800/2000/2199 point epochs), polar edge cases, exact poles, tangencies in crossing search, Moon speed near perigee, retrograde flags at stations, Placidus at 60-66 degrees, negative years, leap-second boundaries, DST gap/fold combined with LMT, unknown-time charts across a date boundary, synastry with identical charts, transits at the natal instant, the SDK's receipt round-trip against hostile JSON (there is a custom parser: is it fuzzed?). Produce a concrete list with a one-line test proposal each; where a proposal is cheap, run it now against the real code and report the outcome.
 4. Observational anchors: nothing in the tree is checked against an observation — every oracle is another JPL-descended program. Identify observational or independent anchors that CAN be used offline and cheaply: published solar-eclipse contact times (Espenak/NASA canon, e.g. 2017-08-21 and 2024-04-08 greatest eclipse instants), transits of Venus 2004/2012 contact times, lunar occultation timings (IOTA), USNO/HMNAO Astronomical Almanac tabulated apparent places, IERS Delta-T/UT1 tables, and JPL Horizons as a semi-independent reduction. For at least two of them, run the shipped engine and the alpha (with a compiled pack if one is available under ${SCRATCH}/data-toolchain-packaging/, otherwise the shipped engine only) against the published value and report the residual. This is the first observational check in the project; keep it honest about what it establishes.
 5. Mutation evidence: the tree claims '42 families ... each confirmed by a mutation that makes it fail'. Sample five families, apply the recorded mutation in a scratch COPY of the alpha (copy examples/precision-alpha to ${SCRATCH}/verification-honesty/alpha-copy and mutate there, never in the worktree), and confirm the family fails. Report which you sampled and the outcome.
 6. CI: which engine claims are gated in CI and which are documentation only (tier-b never runs in CI because there is no kernel; the Swiss packs are frozen fixtures; the benchmark is a one-off). Propose the minimal CI additions that would make the accuracy story enforceable (a committed small reference set with recorded provider versions; a compiled synthetic pack gate; a Delta-T table gate).
 7. Claims audit: grep the docs and READMEs for accuracy/precision claims (arcsecond figures, 'proven', 'complete', 'exact', 'bit-identical', 'independent') and check each against its receipt; list any claim without a committed receipt or with a stale number.`,
  },
  {
    key: 'swiss-parity',
    prompt: `AREA: Swiss Ephemeris parity and the superiority gap — a capability-by-capability and accuracy head-to-head, measured where it matters.
You know Swiss Ephemeris 2.10 (swisseph/pyswisseph) well: introspect the local binding ('python3 -c "import swisseph as s; print(sorted(dir(s)))"') to list its functions and flags, and use your knowledge of the Swiss Ephemeris programmer's documentation for semantics. Read ${ROOT}/docs/engine-validation/README.md, docs/platform/evidence/swiss-benchmark/{RESULTS,CONFIGURATION,NEXT}.md, examples/precision-alpha/README.md and REPORT.md sections 1-4, and the receipt conventions in ${ROOT}/node_modules/@zodiacs/engine/dist/receipt.js, so you know what Zodiacs claims today. Then produce:
 1. A FEATURE MATRIX (as swissGap entries): bodies (Sun..Pluto, mean/true node, mean/osculating apogee (Lilith), Chiron, Pholus, Ceres/Pallas/Juno/Vesta, numbered asteroids, fictitious bodies, Earth heliocentric, fixed stars with proper motion); frames (tropical/sidereal with ~40 ayanamsas, J2000/of-date/ICRS, ecliptic/equatorial/cartesian, geocentric/topocentric/heliocentric/barycentric, astrometric, true vs apparent); time (Delta-T models, UT1, Julian/Gregorian, sidereal time); houses (~24 systems, house_pos, Gauquelin); events (solcross/mooncross/helio_cross, solar and lunar eclipses global and local with contacts and magnitudes, occultations, rise/set/transit with refraction, heliacal, nod_aps, pheno, orbital elements, max/min distance); data (DE431 .se1 13201 BCE-17191 CE, direct JPL files, Moshier fallback, ~0.001" fidelity claims); engineering (C, WASM ports, GPL/commercial licence, no integrity verification, no error bounds, no completeness semantics, thread-safety caveats). For each: Zodiacs today (production / alpha / absent), and the exceedPath with a realistic effort in engineer-days. Rank the astrological importance honestly (Chiron, Lilith, mean node, Koch/Equal/Porphyry/Regiomontanus/Campanus, Lahiri/Fagan-Bradley sidereal, declinations, eclipses and stations are in daily use; heliacal risings and Gauquelin sectors are niche).
 2. HEAD-TO-HEAD ACCURACY, measured now on a small preregistered corpus you write first (say 24 instants across 1850-2150 stratified by epoch, and all ten bodies): (a) Swiss with .se1 (DE431-derived), (b) Swiss with DE440s via FLG_JPLEPH (swe.set_jpl_file('de440s.bsp'), swe.set_ephe_path('<swisslab>')), both apparent geocentric of date tropical with FLG_SPEED and with FLG_CENTER_BODY tested separately, (c) the shipped @zodiacs/engine, (d) the precision alpha's apparent() — use a compiled pack if one exists at ${SCRATCH}/data-toolchain-packaging/*.zeph (another auditor is compiling one; wait up to 10 minutes for it, and if absent compile your own into ${SCRATCH}/swiss-parity/ per examples/precision-alpha/tools/compiler/SOURCE.md — note the alpha takes TT days; convert with Swiss's own Delta-T so the clock is held fixed, and say so), and (e) JPL Horizons for up to 12 of the rows as the arbiter (record queries; QUANTITIES='31', CENTER='500@399', apparent). Report, per body, the residuals (a)-(e), (b)-(e), (c)-(e), (d)-(e), and (a)-(b). The numbers that matter for the owner's goal: how far Swiss's shipped .se1 answer is from DE440-based truth, how far the alpha is from Horizons, and whether any Zodiacs configuration is measurably CLOSER to Horizons than Swiss's default is. Be exact about conventions and about what Horizons itself does (it is DE441 with its own apparent reduction; agreement with it is still consistency, not observation).
 3. WHERE ZODIACS CAN BE BETTER, with evidence: (i) ephemeris generation DE440/441 vs Swiss's DE431 .se1 (size the difference from your (a)-(b) rows); (ii) planet centres via satellite kernels for outer planets if Swiss's centre correction is absent or approximate (test FLG_CENTER_BODY); (iii) nutation 2000A + full deflection incl. Jupiter/Saturn; (iv) rigorous error bounds and proven completeness (Swiss has none); (v) integrity-verified, deterministic cross-runtime, browser-native; (vi) Delta-T with uncertainty; (vii) topocentric with proper geodesy. And WHERE IT CANNOT for a long time: breadth (asteroids, fixed stars, eclipses, houses), historical range, and the maturity of 25 years of use. Say plainly what 'substantially better than Swiss' can truthfully mean and what it cannot.
 4. Findings: anything you discover in the process that is wrong in the Zodiacs stack (e.g. a convention mismatch, a body where the shipped engine is far from both Swiss and Horizons, a place where a documented claim is not supported by your measurement).`,
  },
]

phase('Audit')
const results = await parallel(AREAS.map(a => () =>
  agent(PREAMBLE + '\n' + a.prompt + `\n\nWrite your scratch under ${SCRATCH}/${a.key}/ and set area to '${a.key}'.`,
    { label: `audit:${a.key}`, phase: 'Audit', schema: FINDINGS, effort: 'high' })
    .then(r => r ? { key: a.key, ...r } : null)
))

const done = results.filter(Boolean)
log(`${done.length}/${AREAS.length} auditors returned; ${done.reduce((n, r) => n + r.findings.length, 0)} findings, ${done.reduce((n, r) => n + r.measurements.length, 0)} measurements`)
const missing = AREAS.map(a => a.key).filter(k => !done.some(r => r.key === k))
if (missing.length) log(`MISSING areas (agent returned null): ${missing.join(', ')}`)
return { areas: done, missing }