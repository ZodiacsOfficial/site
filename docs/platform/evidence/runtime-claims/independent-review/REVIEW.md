# Runtime wording review

**No blocking finding in the exact proposed two-text-change scope.** Reviewed patch SHA-256 `2a59e5e721a748178f96e5d653434c0515cc5795d7c37d82cd25338dbe83e581`, resulting support page SHA-256 `7598fca770c5f5e9c26f92603ee37e48c4cc3d087d744ee1ac3783b068a49cea`, against current rc.6 metadata, its four immutable site evidence-carrier records, and SDK rc.6 documentation/package evidence. No source was edited.

The old row ties Node 20/22 tests and clean installation claims to the current-candidate table caption. The current linked consumer instead records Node 22.23.2/24.19.0, TypeScript 5.9.3, public root/geo/receipt examples and exact archive declarations. Its setup explicitly describes manual extraction of 23 archive files, copied dependencies and no npm install. The proposed candidate-relative row avoids carrying old runtime results into each new version, and “public-download consumer check” accurately labels this linked record.

The change retains material limits: the adjacent language/runtime row declares Node ≥18 rather than certification; the evidence paragraph still names and links Node 22 and Node 24 comparisons; ESM/dependency setup and browser requirements remain visible; host Intl/ICU history, finite date/accuracy scope, house/pole limits, and candidate/publication status remain explicit. The two text edits neither promise Node 18/20 testing nor suggest every browser is certified. The inactive site's optional interval primitive creates no new runtime requirement for the delivered SDK and needs no additional support-table claim here.

I also challenged the unchanged introductory “installed in a clean consumer.” It is supported separately: SDK rc.6 `consumer-node22.log.log` and `consumer-node24.log.log` identify actual isolated consumers and the same artifact hash `09c3e63432f8ba2e9df05af137c42f65ab039740a207a89418d9e6470ea3db3e`; their smoke implementation invokes npm install with lifecycle scripts disabled. Thus that introductory statement does not need to be inferred from the manually unpacked public-consumer record. Those are distinct evidence records and should remain described distinctly. This review did not run a new installation.

`RESULTS.json` records the exact prepared identity, current metadata, all four evidence hashes compared with immutable site commit `68412f16140f9986b11767d271eda6e14b6aba48`, rc.6 archive manifest identity and the separate SDK consumer records. All four current evidence files matched their carrier bytes; the local candidate archive matched metadata. Raw supporting text and the npm-install smoke implementation are copied as inert records. This is a read-only evidence/wording review, not a new runtime test, hosted availability check, publication approval or whole-release signoff.
