# Runtime wording exact preview acceptance

Draft #431, source abcf1a44e52040db79a369b49bc9b55d78099b22, has 183 changed paths matching the local #430-based diff: one support source page plus durable platform records. The exact provider preview dpl_7TVmbdjnFTVjT1rZrVj5a8Kyj26C is READY before/after the actual Chrome run.

All 13 developer/support/examples, synthetic shared-chart and dated sky API checks pass at 1,280/390/320 widths. The new runtime row and precise current-carrier consumer link pass along with artifact/caption, standard footer, no horizontal overflow, reduced motion, table keyboard focus and no saved profile controls. Two fresh developer screenshots are retained. Source adaptation and per-page diagnostics are preserved. Local product code is not injected into the preview.

The observer blocks external requests on scoped pages; existing Plausible attempts are recorded in preview-summary.json. Authentication bootstrap is outside the observer. All temporary access is deleted and browser contexts close. The original overbroad literal-marker scan failed on a public historical scanner source string; diagnostic and failure are retained. The corrected credential-like URL scan passes. It is not represented as an exact-secret scan after deletion. No browser assertion failed, no product or tolerance changed.

This is preview acceptance, not production deployment or human numerical certification. Existing Phase1 captures remain valid under the unchanged covered source fingerprint; no fresh Phase1 capture run is claimed.
