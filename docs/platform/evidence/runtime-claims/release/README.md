# Runtime wording release records

Draft #431 is exact source abcf1a44e52040db79a369b49bc9b55d78099b22, above #430.
The READY preview passes13 recorded browser/API groups. The browser request
observer excludes both the separate sky APIRequestContext check and isolated
authentication bootstrap. The earlier root report and independent audit retain
that distinction and the original literal-marker scan failure. No exact-secret
scan after deletion, fresh Phase1 capture or production deployment is claimed.

Unchanged prior #428/#429 CI and Moon initial draft records remain in
../integration/release/ rather than being duplicated here. The independent
read-only audit is ../preview-audit/REVIEW.md.log. The current exact-source CI
snapshot in runs.json was in progress; refresh before claiming completion.
