# September 6 recovery status

The historical checkpoint below describes the earlier local work, not a current
release or browser pass. This recovery uses the separately prepared Solar Return
checkpoint and inherits the full Wave 14 collision/leader prerequisite. The
cumulative archive also contained Wave 21 aspect-pattern integration in two
composite files; those later feature imports, rendering hook and four pattern-only
tests are withheld for Wave 21. Actual composite uncertainty checks remain.

Current preparation: Node 22 build and every postbuild gate pass, including the
unchanged budgets and source receipt. Type check passes: 936 files, zero errors
and warnings, ten existing hints. The integrated focused suite passes 108 tests
in six files. Initial focused runs identified the missing Wave 14 prerequisite
and later-wave imports; those failed logs are retained. Source fingerprint is
`c0a64e10364fb2ebe1ff24b6d2dcf890276d8e53cc51be35ff861b8d48db6041`.
No current production, baseline, screenshot or full-suite pass is claimed.

---

# Wave 17 composite acceptance checkpoint

Initial base: `9096764df2d805948da387375ec04d81d1ca2f7b` (approved crowded-wheel checkpoint). The full frozen Solar checkpoint `a395549a36991c031b48a1d104b706e9df935f41` merged without conflicts at `0be5380eab8cefed63a32affc4e7fbf4d58b8f8f`; its eighteen files remain inherited intact.

This checkpoint completes the composite result with native placement/contact selection, a relationship-specific reading, and a dedicated image prepared only after an export request. Its authored corpus was reviewed in full and approved on 2026-09-05: twelve body roles, forty-five unordered pair themes and five separate aspect prompts.

## Source ownership

The eight production paths are:

- `src/islands/synastry/RelationshipWheel.tsx`
- `src/islands/synastry/relationshipData.ts`
- `src/islands/synastry/CompositePanel.tsx`
- `src/islands/synastry/CompositeWheel.tsx`
- `src/islands/synastry/compositeCopy.ts`
- `src/islands/synastry/compositeReadings.ts`
- `src/islands/synastry/relationship.css`
- `src/lib/composite-card.ts`

The original midpoint calculator, aspect math, shared Wheel serializer, natal types, codecs, locale catalogs and profile storage remain unchanged in this Wave 17 patch. Exact opposites retain the existing eastward-from-chart-A convention. Only marker draw positions use the existing 14-degree fan; source longitudes and chord endpoints remain exact. All twelve placements have native buttons; the wheel retains the existing eleven-marker convention with South Node available in the list.

Selection is independent of the synastry view, survives tab changes and presentation-only ring swaps, and is invalidated by replacement source positions or certainty, including replacement pairs that happen to yield identical midpoints. Export preparation/delivery belongs to that same input identity; late work cannot overwrite a replacement result. Closing or unmounting revokes the preview URL.

A missing birth time preserves reference Moon facts and contacts but withholds their substantive interpretation and prompts. No Moon candidates, houses, angles, speeds, retrogrades, applying/separating state or certainty bounds are manufactured. The full image includes the reference facts and explicit localized uncertainty note. Five non-English interfaces explicitly identify the English narrative.

The image module dynamically loads on export intent and returns a 1080-by-1350 PNG with the canonical brand lockup. Once the renderer starts, the entire preparation has a fifteen-second deadline. Cached artwork requests cover headers and body consumption with AbortController cancellation, and only the failed cache entry is evicted. Native font, SVG and PNG waits also respect the preparation deadline; SVG URLs and handlers are released. The shared brand helper has no abort API, so its wait is bounded and a late callback cannot paint after expiry. Text that cannot fit at the minimum size fails preparation instead of overflowing. Separate native Share and Download controls report the actual shared/downloaded/cancelled outcome. They do not write birth/profile storage or include private birth inputs in the image.

## Local evidence so far

Node 22 cumulative focused suite after the Solar merge: **126 tests pass across ten files**, including the shared transport and Solar action tests. Wave 17 coverage includes model/corpus/geometry, existing midpoint and shared-Wheel serialization contracts, component selection and export ownership, painter receipts/cleanup, and browser-observer counterexamples. The painter unit fixture uses deterministic font metrics; it is not evidence of actual font layout.

Consumer boundary passes (1,357 source files checked). The exact scope check passes for sixteen changed paths relative to the frozen Solar checkpoint with no protected-path allowance. JavaScript syntax and whitespace checks pass.

No local browser has run. No screenshot baseline or Phase 1 receipt has been refreshed. The existing compatibility CI entrypoint invokes `runCompositeBrowserChecks` in twelve contexts (six locales, 390 and 1440 widths), with timed/unknown-time cases, real native selection and SVG hit tests, tab/swap/replacement checks, PNG downloads, native canvas-ink observations, cancellation/fallback/encode-retry coverage and unchanged-storage checks. One English 390px case holds a genuine artwork request of resource type fetch, leaving normal DOM images untouched; it checks a 14–22 second observed abort window, the exact failed-request identity, failed-entry refetch on retry, retained reading/selection, and two additional screenshots. If the browser does not report the controlled abort while the route is held, that is a pending CI failure to investigate, not an error predicate to weaken. Planned outputs live under the existing `OUT_DIR/composite`: twenty-four downloaded PNGs, twenty-six panel captures and `measurements.json`.

## Pending release gates

The approved shared export transport belongs to Wave 16. The full frozen Solar checkpoint has now merged, and Wave 17 consumes its `downloadPreparedChartCard` API without editing or copying `share-card.ts`. Image preparation also rejects a missing font face instead of silently exporting fallback typography. Cumulative Node 22 check passes: 931 files, zero errors, zero warnings and ten existing hints. Full build is queued and no browser result is claimed.

Remaining: run full build/unit/bundle gates and reconfirm scope/source against the exact source, report the cumulative Today margin without raising its cap, and obtain genuine existing-CI browser evidence. Full source review, publication and merging remain with root. Local focused tests establish contracts, not screen-reader verification or a visually approved PNG.
