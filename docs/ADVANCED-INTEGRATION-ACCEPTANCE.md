# Advanced astrology cumulative acceptance

Prepared September 6, 2026 in an isolated checkout. Not released.

## Delivery decision and source ownership

The owner asked to retain the advanced features, build independent work in
parallel, and then explicitly instructed this session to manage both website
sessions and finish. This candidate deliberately replaces the recovered audit's
one-bounded-wave-per-PR sequence for prepared Waves 15–23 with one cumulative
integration and release review. It reduces repeated release sweeps without
removing any feature's acceptance, source review or required CI job.

The integration starts from actually released Wave 14 main
`2738713a6787d84d05bd3b8e2f401adbc42d7ea7`, retains current Wave 15
`6cbd8dcd48997bf61f7d3c1c37cbd0195de6a506`, and merges cumulative Wave 23
`b2860def3a00f849ebcb7b4fc105cd06509eceab`. Those merges had no conflicts.
Wave 23 contains the current prepared Waves 16–22, including the larger type
on short chart-context images. Wave 14's final sky-overlay marker-tap fix and
tests remain inherited from main. The original worktrees are not modified.

The coordinating session subsequently consolidated the separately prepared
personal-chart continuity PR 393 and mobile RaceRamp correction PR 380 into this
same final candidate before the broad validation sequence. Exact source commits
`bd05582fdf4ab56bf1239bbeceff2557d1bb04f5` and
`f2b5d4a1342aa34bc78ef48527e3531b76fa3edf` are inherited. The sole merge conflict
was the expected one-time scope allowance; its complete twelve-path protected
union is pinned to actual released main `2738713a`, without widening the guard.
Product source merged without conflicts. The original two PRs stay open until
this candidate passes and merges, then can be closed as superseded. Their
intermediate captures remain change-review evidence, not this candidate's
source-matching receipt. This avoids three successive release recaptures.

Wave 24 is excluded. Its unresolved Uranus 2020 conditioning case is not dropped,
retuned, or represented as a pass. All historical wave notes and failed logs
remain historical evidence, not acceptance of the final integrated source.

## Required feature evidence

| Wave | Existing owning browser or calculation checks | Required evidence |
| --- | --- | --- |
| 15 | `tests/event-transit-checks.mjs` | Five no-JS articles, truthful metadata, exact UTC handoff, saved-chart source, Now/Event date, keyboard scrub, same-event reset, unknown-time restrictions; 390/1440 captures. |
| 16 | `tests/solar-return-drive.mjs` | Real PNG/ICS, dimensions/fonts/ink, private-field omissions, activation/cancellation/fallback, relocation, unknown time, stale ownership and renderer recovery. |
| 17 | `tests/composite-browser-checks.mjs` via compatibility drive | Six locales at 390/1440; native placement/contact and SVG selection, tab/swap/replacement, uncertainty, real images, cancellation/fallback/encode retry, controlled abort and recovery, unchanged storage. |
| 18 | `tests/editorial-metadata-drive.mjs` and completed-build validator | All 70 owner/date/schema/sitemap contracts; no-JS collection and localized Chinese hub/animal captures; retained editorial disclosure and visible prose. |
| 19 | `engine`, `longitude-crossings`, `progressions`, `returns`, `solar-return`, `transit-scan` engine tests | Immutable independent fixtures/policies, endpoint correction, nodes/polar/epochs/stations/returns and exact returned-chart applicability, unchanged limits. |
| 20 | `tests/lunar-return-drive.mjs` | Complete timed input, strict next return, retries/source changes, relocation, invalid/unknown clocks, genuine private PNG/ICS, lifecycle failures, canonical/schema/sitemap and English-only discovery. |
| 21 | `tests/aspect-pattern-browser-checks.mjs` | Native natal/composite selection, exact graph edges, contained patterns, uncertainty, selected-pattern image and owned lifecycle at 390/1440. |
| 22 | `tests/chart-context-checks.mjs` | Four disclosures/images, six-locale labels, actual glyph bounds, uncertainty, input invalidation, load recovery and reviewed new typography at 390/1440. |
| 23 | `tests/learning-practice-checks.mjs` | Saved-chart handoff, answer/reflection/separate completion, source and uncertainty changes, keyboard/layout, genuine second-tab Web Lock, bounded fallback, drained callbacks, unavailable locks and blocked storage. |

Explorer already invokes Waves 15, 16, 18, 20, 21, 22 and 23. The existing
compatibility drive owns Wave 17. Do not substitute a new reduced browser suite.
All new screenshots and downloaded images require independent visual review;
mocked canvas tests and historical desktop demonstrations are insufficient.

## Evidence limitations

Wave 19's larger eight-case raw oracle, 101,718-row provider journal and returned-
chart supplement were not recovered locally. The committed compact fixture,
immutable hashes, source maps and historical review are preserved. Offline
regressions validate those recorded expectations; they do not constitute a new
raw-source audit or source acquisition. Raw acquisition is intentionally outside
offline CI. Recover the immutable bundle if further source-audit attestation is
needed; do not change its assertions or acquire new references merely to remove
this limitation.

Wave 20's primary lunar oracle was recovered and its projection reproduced, but
the later returned-chart raw supplement remains unrecovered. Its committed
expectations and applicability limits are retained with the same qualification.

## Final release checklist

- Integrate the coordinated exact concurrent changes, inspect source and protected
  scope, and keep independent facts/copy/product review distinct from generation.
- Use focused checks while resolving source issues; then freeze the source and
  run a clean Node 22 build/postbuild, check and complete serial unit suite.
  Preserve existing timeout limits; avoid overlapping resource-intensive builds.
- Obtain genuine Phase 1 360/1280 captures and matching receipt. Inspect the
  390/1440 visual comparisons and every new feature's screenshots and real PNGs.
  Candidate baselines require the existing explicit reviewed-head procedure.
- Run the unchanged Site Check and Browser Evidence workflows on the final
  candidate. Keep every SQL, flag-on, existing browser, drift, schema, dist,
  dependency, bundle and engine-isolation gate. Neither workflow substitutes for
  the other, and neither a failed step nor stale receipt counts as passing.
- The unchanged Lighthouse route policy now includes the new lunar-return route:
  27 routes, three samples each. Existing budgets stay fixed; lunar return retains
  its approved 30 KB initial-JS budget. Each required workflow performs its own
  sweep; this is one cumulative release, not one literal execution of every tool.
- Merge only after root coordination, green final checks and independent review;
  verify the exact production commit, affected routes and native interactions.
  A merged but unverified deployment is not completion.

Local preparation results will be appended with their actual source identity.
No final build, complete-suite, browser, visual or production pass is claimed yet.

## Initial integrated preparation

Source head `504e3b11` passes 881 focused tests in 56 files with Node 22.23.2,
one worker and file parallelism disabled (54.94 seconds). The inventory includes
all changed owning test files plus released crowded-wheel and technical-time
regressions. Ten owning browser entrypoints parse successfully. The scope guard
passes 188 changed paths with the exact eight protected paths, and the complete
committed diff passes whitespace checks after removing one inherited trailing
blank line. The original focused log is `advanced-focused-tests.log` in the
coordinating workspace, with its inventory in `advanced-focused-inventory.txt`.

The released Wave 14 wheel source, crowded-wheel unit tests and crowded-wheel
browser helper remain byte-identical to the main base. No workflow changes or
Wave 24 implementation paths are present. All eight Swiss policy/fixture hashes
retain their recorded identities. The sole budget delta adds the approved
30 KB lunar-return route; no existing limit changes.

This checkpoint precedes the separately owned PR 393/380 release integration.
It is not the frozen final-source gate, full-suite or browser acceptance.

## Independent personal-chart visual review

The initial PR 393 Browser Evidence run `34040390003` belongs to head
`009cb3a2f1054c9b3251657e13ca31361652ceb1`, not this final candidate. GitHub
artifact `9991780923` is 101,281,048 bytes with ZIP SHA-256
`e9a540728827586485906689d248a1a91699c88e49605fd035645d79efdd81a2`.
An agent separate from that feature's implementer verified the ZIP, all 453
provenance-listed byte counts/hashes, exact recorded head/run and pinned Chromium
149.0.7827.55. Every recorded compare/capture/receipt/Lighthouse/Explorer outcome
passes. No candidate baseline generation was requested.

The reviewer inspected `personal-chart-home-390.png`,
`personal-chart-home-1440.png` and `personal-chart-choice-390.png`: the personal
identity and Big Three are readable, controls and chart-list links are clear,
and the unresolved identity has an explicit Choose my chart action. No clipping,
collisions or other visible defect was observed within those bounded cards.
This is initial-source visual review only; the integrated source still requires
all final acceptance and genuine capture evidence.

The coordinating reviewer also inspected the lunar calculation, learning-source
and progress ownership, and chart-context model/copy. Known-time and DST guards,
fresh original-input calculation, strict-next return and location separation,
profile access checks, bounded lock failure, progress migration semantics and
unknown-time interpretation exclusions were retained. No blocking issue was
found in that bounded source review. It is not a raw-oracle attestation or a
substitute for the remaining numerical, browser and visual gates.

## First complete candidate: preserved failures and bounded corrections

Draft PR 394 publishes remote head `c351bb64efcab790c7eb714690206ec6c6c28dab`,
exactly matching local frozen head `7b6b40d5` at tree
`113de1537bb4fe713cc4e5c5fa757ec185a30185`. Complete Node 22 build/postbuild
passes (203.3 seconds), with source fingerprint
`11d720654113e801c502e1246eb37093a28c88c7fcc8ed1fb35b1445ae06181f`.
Check passes 996 files, zero errors/warnings and eleven existing hints. The
full serial suite passes 4,173 tests and fails one of 4,174 across 396 files
(447.32 seconds): the committed Phase 1 receipt still describes the old source.
The monthly regeneration passes under its unchanged 120-second limit.
Site Check `34042475957` independently records the same sole receipt failure;
all thirteen supporting jobs pass. Neither suite is described as fully green.

Browser Evidence `34042475962` preserves artifact `9992371542`, 92,175,926 bytes,
ZIP SHA-256 `fdaf74e91c3bdce0f6057d148f48e8f121282dad135ddf647e12e09501ea6f0f`.
ZIP integrity and all 362 provenance-listed file sizes/hashes were verified
against exact head `c351bb64`, source `11d72065` and pinned Chromium. Build,
fresh Phase 1 capture and capture-receipt validation pass. Visual comparison,
Lighthouse and Explorer fail, as follows; the original artifact remains intact.

| Visual case | Expected size | Captured size |
| --- | --- | --- |
| Birth chart, desktop and reduced motion | 1440 × 8887 | 1440 × 9502 |
| Birth chart, mobile | 390 × 13414 | 390 × 14010 |
| Full-moon article, desktop and reduced motion | 1440 × 4726 | 1440 × 4785 |
| Full-moon article, mobile | 390 × 6814 | 390 × 6887 |

The other nine visual cases pass. Dimension changes produce six actual-image
artifacts, without pixel-diff images. Root independently reviewed all six
against committed baselines, including original-resolution details and reduced
motion: the new optional chart disclosures and event handoff explain the
additional height, with readable controls and no observed overlap. Root then
requested baseline candidates for that exact unchanged `c351bb64` head through
the existing reviewed-head marker. No baseline is silently accepted or generated
from a different source. Candidate-image review/import remains separate.

The complete Lighthouse sweep fails lunar return at 2.64-second LCP and Russian
Home at 2.56 seconds, over their unchanged 2.50-second limit. Both show performance
96, accessibility/SEO 100, CLS zero and TBT zero; those scores do not waive LCP.
The lunar route now opts into Base's existing `inlineCriticalCss` path after
the saved report identifies blocking stylesheets. Russian Home's empty returning
chart island uses the existing interaction directive, including its automatic
600ms post-load fallback. The owner-card still appears without requiring input,
while profile modules leave the first poster paint clear. No hero, font, asset,
directive implementation, numerical algorithm or performance budget changes.
Fresh unchanged-limit Lighthouse verification remains required for both routes.

Explorer reaches the existing learning regression at 390px, then times out at
`tests/search-learning-checks.mjs:195`. Control/Meta-click opens the houses lesson
in a new tab, but the reconstructed LearningPath handler exits before submitting
the start transaction. This is a product regression; its browser assertion is
retained. Modified primary clicks now submit the same owned start while leaving
default browser navigation untouched. Ordinary clicks alone prevent default and
navigate after acceptance. Seven component-handler tests cover Control, Meta,
Shift, Alt, pending ordinary navigation and rejected transactions. The four-file
owning suite passes 36 tests; it does not claim real-browser completion.

Explorer stopped before the advanced feature helpers. Their pending status must
not be replaced with passing claims from the earlier successful checks. These
three bounded corrections will be built and checked together, with the reviewed
candidate pixels included only after their separate review. Final capture,
feature browsers, native image review and all required workflows remain gates.

## Reviewed reference images and postbuild scope

Head-bound candidate run 34043890167 on c351bb64 succeeded. Its ZIP digest is d794316b2d7e15d71f60620396743e88fd5c5d934713e51e7ca2b1700716edf2; all 35 artifact hashes verify the original source fingerprint. The six approved chart/full-moon candidates are byte-identical to the independently reviewed actual comparison images. Only those six reference images are imported. The older capture receipt is not imported as evidence of the corrected source.

The first corrected build reached postbuild and rejected the lunar-return inline marker because that new route was absent from the explicit delivery allowlist. Added only lunar-return/index.html to the existing CSS delivery pass, retaining its strict exact counts at 18 pages and 34 stylesheets. No CSS contents, font treatment or performance threshold change. Resuming the postbuild over the completed corrected build; final clean CI remains required.

Corrected local postbuild completed successfully: all distribution, schema, bundle-budget and engine-isolation gates pass. The lunar HTML contains two inline stylesheet blocks and no blocking stylesheet links. Corrected source fingerprint: `5b8ad6cc481429cea121454dae7b6a18d7917f5c349411e66076f3ffbb683943`. The five focused files pass all 45 tests, and exact scope passes with 213 changed paths and 12 protected paths. Fresh corrected-source browser evidence and required CI remain pending.

## Corrected comparison results and remaining fixes

Browser run 34045111424 captured exact head `3b392ad9c4afd7d3dc92a5f083873f57ed4163c1`. ZIP SHA-256 `d49f80e526931c45370b01fcf1b87bda77808eecf67e8abaf8e9aa30727be479`; all 405 provenance file hashes and ZIP CRCs verify. All 15 visual comparisons pass, and all 18 Phase 1 PNGs are byte-identical to the committed images. Capture/receipt validation passes for source `5b8ad6cc481429cea121454dae7b6a18d7917f5c349411e66076f3ffbb683943`; that receipt is not imported because the following source fixes invalidate it.

Site Check 34045111423: all 13 supporting jobs pass. Main Build & Check passes 4,180 tests with the sole stale committed receipt failure. Root separately used native Control-click on the exact ready preview: houses opens in a new tab, Learn stays open, and one started / zero completed persists after reload; no horizontal overflow at 1353px.

Lighthouse keeps all 81 samples and unchanged gates. Russian Home now passes all three samples (maximum LCP 2407ms). Lunar return still misses at 2582ms and 2570ms (third sample 2179ms); its blocking stylesheets are gone, but calculator dependencies begin before text LCP. The route now uses the existing interaction directive, which preserves pre-hydration form input and automatically hydrates 600ms after load; no new directive, content, font or layout changes.

Three other routes have isolated TBT failures: tomorrow 937ms, weekly 602ms, and Russian birth-chart 407ms. Their other two samples each have TBT zero. Traces attribute the long tasks to browser startup / unassigned work: tomorrow has an isolated 368ms RunTask; weekly spends 344ms in ScriptCatchup during context creation; Russian birth-chart spends 64ms there. These observations do not justify changing product code or budgets, and do not convert failures into passes. The next materially corrected source requires fresh measurements on every route.

Explorer now passes modified-click learning, all five event readings and exact transit handoffs at both widths, and the editorial metadata browser checks. Solar Return then stops on the exact accessible submit name: its decorative arrow was exposed. Hide only the decorative arrows from assistive technology in both return calculators, retaining the exact existing test locator. The return/practice fixture init scripts now skip opaque about:blank documents before accessing storage; genuine page and storage errors are still asserted. The five independent advanced helpers now collect each exception as a failed check and continue to collect the others' evidence; the final nonzero failure exit remains unchanged. No feature assertion is removed or relaxed.

The 13 focused return/observation/first-paint test files pass all 85 tests. Full corrected build and fresh browser evidence remain pending; no overall advanced-feature browser or image-review pass is claimed.

Root reviewed all ten authored-event page captures (390/1440) and four known/unknown transit result captures from that artifact. Article text, exact-UTC cards, prompts and personal handoffs are readable with no observed article clipping. The mobile transit crop exposes an internal minimum-width overflow that document-level overflow alone missed: the date/next-contact controls can extend outside the ring. Bound the ring grid track and scrubber minimum width, allow exact-contact buttons to wrap, and strengthen the existing viewport assertion to check internal control bounds and scroll widths. Desktop and unknown-time notices were independently inspected; corrected mobile pixels still require review.

The preceding return/accessibility build completed all build/postbuild/bundle gates at fingerprint `b8da85d53159071aca416c54481db3a242c19d1d3113e03c4c7eb8dd00fb2f37`. The subsequent bounded transit CSS correction changes the final source fingerprint to `111636cb69f8be2b6eda1b2a6af57714adbbc0ce66728b253b8b6c0ff8f7d494`; the earlier local build is not presented as a build of that later CSS. Clean CI must build and capture this final source. Scope remains 213 changed paths / 12 explicitly allowed protected paths. No performance limits, numerical fixtures, workflow definitions or visual baselines changed in these follow-up corrections.

## Complete follow-up evidence, with failures retained

Run 34046736345 captured `cfe63879b85513b13707f5beae36aca7571641a6` / source `111636cb69f8be2b6eda1b2a6af57714adbbc0ce66728b253b8b6c0ff8f7d494`. Artifact ZIP SHA-256 `40556025eba53e89906b3f9a8b413a439e833fdb1bdc5ae26bdfde383850a547`; all 690 file hashes and CRCs verify. All 15 visual comparisons pass; all 18 Phase 1 PNGs remain byte-identical. All 81 Lighthouse samples pass unchanged budgets: minimum performance 97, maximum LCP 2486.48ms, maximum TBT 16ms, CLS zero. Site Check 34046736347 has 4,180 test passes, only the old receipt failure, and 13 supporting job passes.

Root additionally used native controls on the exact preview to calculate a synthetic 1990-01-01 12:00 London solar return. Changing to unknown birth time immediately removed stale reading/export controls; recomputation gave the noon-chart notice, approximate calendar wording, and omitted the house-based reading.

Explorer completed with 28 failed checks rather than stopping after its first advanced helper. Ten static event failures came from root's newly strengthened fit helper incorrectly demanding a chart on article pages; restore the static document check while explicitly requiring the ring for dynamic checks. The four dynamic failures expose the range input's native 2px inline margins: native preview DOM measured a 520px scrubber with 522px scroll width and range bounds 418.5–938.5 against scrubber bounds 416.5–936.5. Remove only those native inline margins, retaining the strengthened internal-bound assertion.

Solar Return's complete owning browser checks now pass. Lunar return's relocated selected-place chip has a 26px clear button and 22.39px read-only field; give only that calculator's selected controls 44px targets and bound the chip width. Wait for genuine wheel image requests to settle before subsequent deliberate form edits, retaining strict request-error checks instead of allowing all aborted requests.

The pattern/context reload tests incorrectly expected unsaved input to survive the documented warned reload. They now assert that input was cleared, then explicitly re-enter the synthetic chart link in a fresh document before testing the recovered real module. The learning handoff intentionally consumes the opaque identifier; capture the original navigation and assert both its ID-only form and subsequent URL clearing. Give the learning selects explicit accessible labels, preserving the exact existing locators. Russian context layout failures still need the newly expanded geometry diagnostics; no passing claim or threshold change is made.

The seven focused component, observation, and first-paint files pass all 48 tests. New source changes require another genuine capture before receipt import. No existing failure is silently waived; all numerical and performance gates remain unchanged.
