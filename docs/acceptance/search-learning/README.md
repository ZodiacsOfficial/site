# Search and deliberate learning progress

Plural chart queries use the same narrow normalization in site search and the consumer search interface. Big Three and Solar Return are classified as tools. The five existing lessons remain available without JavaScript. Opening a lesson marks it started; explicit reflection marks it complete. Legacy recorded visits migrate to started, and restarting learning preserves saved charts. Storage failure leaves progress usable for the current visit.

Initial Browser Evidence33981701766, attempt1, exact headffe426a85d7c7be573d6bf7b6f28d1dc3f70787d, passed Phase1 and all15 visual comparisons. Its archive9974170643 is93,821,040 bytes, SHA-256d9f586b4d3ae143f9e29573f231ae86f2ce07f20a1510e2c180401355c046837. The checksum and all387 provenance members were independently verified. The release reviewer inspected all nine genuine learning captures using contact sheets. These show the exercised started/completed/no-JavaScript states; the run remains failed and no receipt is imported from it.

Three Explorer assertions failed. At390px the fixed Guide launcher covered part of the second reflection label. A Learn-only responsive right gutter now reserves room for Guide beside the actual in-flow controls, with the smaller circle allowance below560px. Guide remains available; no global styling, programmatic scrolling or weakened hit-ownership checks are introduced. Two saved-chart continuation assertions incorrectly required the self-chart marker for the unchanged other-person fixture. The corrected test requires its named other-person result, original birth date/time, consumed opaque fragment, unchanged private profile, and exactly one fresh full-chart computation event with the expected Cancer Sun. Nine new observation regressions, existing chart-locale tests and eight progress/SSR tests pass; actual browser verification of these repairs remains pending.

The same capture failed the unchanged birth-chart Lighthouse LCP gate in one of three samples:2410.2804ms,2884.194ms and2561.93625ms against2800ms. All have accessibility100, CLS0 and TBT0; the other25 routes passed. The three samples have identical request identities and the same hero-paragraph LCP element. The failed sample has the shortest observed LCP and lowest task time, but different pre-LCP hydration scheduling. Simulation variability is a hypothesis, not a confirmed external flake; the84.194ms gate miss remains recorded. No performance threshold or unrelated product code is changed without evidence. The next exact-head run must pass.

The current source includes the complete Moon54b7a35ea16bff03ab7f77e57b0908f1076b4e0d update, including the corrected Russian stylesheet check and Today shift diagnostics. The protected allowance names only the changed Learn page and is pinned to that actual base. Phase1 source remains858e9e6024f8692a32f8fa4d720bac85a26113186f378307aabcf626cdb2bb42; Learn geometry and helper changes outside that existing boundary are bound by the exact commit/tree and fresh browser captures. The boundary is unchanged. Full cumulative gates, personal review of new captures, genuine receipt import and final green Site Check are still required before release.
