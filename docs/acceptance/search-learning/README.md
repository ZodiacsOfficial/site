# Search and deliberate learning progress

Plural chart queries use the same narrow normalization in site search and the consumer search interface. Big Three and Solar Return are classified as tools. The five existing lessons remain available without JavaScript. Opening a lesson marks it started; explicit reflection marks it complete. Legacy recorded visits migrate to started, and restarting learning preserves saved charts. Storage failure leaves progress usable for the current visit.

Initial Browser Evidence33981701766, attempt1, exact headffe426a85d7c7be573d6bf7b6f28d1dc3f70787d, passed Phase1 and all15 visual comparisons. Its archive9974170643 is93,821,040 bytes, SHA-256d9f586b4d3ae143f9e29573f231ae86f2ce07f20a1510e2c180401355c046837. The checksum and all387 provenance members were independently verified. The release reviewer inspected all nine genuine learning captures using contact sheets. These show the exercised started/completed/no-JavaScript states; the run remains failed and no receipt is imported from it.

Three Explorer assertions failed. At390px the fixed Guide launcher covered part of the second reflection label. A Learn-only responsive right gutter now reserves room for Guide beside the actual in-flow controls, with the smaller circle allowance below560px. Guide remains available; no global styling, programmatic scrolling or weakened hit-ownership checks are introduced. Two saved-chart continuation assertions incorrectly required the self-chart marker for the unchanged other-person fixture. The corrected test requires its named other-person result, original birth date/time, consumed opaque fragment, unchanged private profile, and exactly one fresh full-chart computation event with the expected Cancer Sun. Nine new observation regressions, existing chart-locale tests and eight progress/SSR tests pass; actual browser verification of these repairs remains pending.

The same capture failed the unchanged birth-chart Lighthouse LCP gate in one of three samples:2410.2804ms,2884.194ms and2561.93625ms against2800ms. All have accessibility100, CLS0 and TBT0; the other25 routes passed. The three samples have identical request identities and the same hero-paragraph LCP element. The failed sample has the shortest observed LCP and lowest task time, but different pre-LCP hydration scheduling. Simulation variability is a hypothesis, not a confirmed external flake; the84.194ms gate miss remains recorded. No performance threshold or unrelated product code is changed without evidence. The next exact-head run must pass.

The current source includes the complete Moon54b7a35ea16bff03ab7f77e57b0908f1076b4e0d update, including the corrected Russian stylesheet check and Today shift diagnostics. The protected allowance names only the changed Learn page and is pinned to that actual base. Phase1 source remains858e9e6024f8692a32f8fa4d720bac85a26113186f378307aabcf626cdb2bb42; Learn geometry and helper changes outside that existing boundary are bound by the exact commit/tree and fresh browser captures. The boundary is unchanged. Full cumulative gates, personal review of new captures, genuine receipt import and final green Site Check are still required before release.

Browser Evidence33983506731, attempt1 on exact4313fdbe2eb4bba50e7cbb8dbb763d02331daae9, passed all steps: Phase1 capture,15 visual comparisons,all78 Lighthouse samples and repaired Explorer interactions. Verified artifact9974725180 is94,266,605 bytes with SHA-256284633d67fed161720654dce944e66bb9690a60eb105aea726173e44e68268e9; all387 provenance members, exacthead/base/run/source and receipt linkage pass. Every rawCLS is0 and accessibility100. Birth worstLCP2563.91495ms,TBT37ms,performance96; all original limits remain. No terminal exception,timeout orFAIL row remains.

The release reviewer personally inspected all9 genuine Learn captures and the original390px started state. Fourdesktop images remainbyte-identical; five mobile images retain350px elementcapturewidth and gain145–164pxheight from the scoped gutter. Text and reflection controls remain readable with clear spacing; browser hit-ownership checks confirm the fixed Guide no longer overlaps the actual labels. These element captures do not establish a manual screen-reader or native-device session. All18 Phase1 screenshots match the prior Search captures; import only the genuine current19-member Phase1 receipt set, with changed bytes recorded by git. No visual baseline is changed.

This receipt update includes actual Moon main reconciliation33f80a6be0a6556d1bd66bfdbda16d44a6f65809 and pins the same one protected Learn page to that parent. Product and browser test sources remain exactly the successful4313 capture. Final local gates, eventual actual-main reconciliation and a green exact-head Site Check remain required before merge.

Moon PR378 has merged as actual main67360f35ec48337be1e68a560dc09dcf80bc0673 with the exact green Moon tree. This integration inherits that actual main and re-pins only the same protected Learn path. Product, tests and genuine evidence remain unchanged from bd34e405. Final stacked Site Check33985525130 passed all3434 units and functional/visual checks but failed four Lighthouse performance/TBT routes: horoscopes91/368ms, eclipse88/439ms, Ada92/284ms and Russian home87/471ms. These failures remain preserved; raw trace diagnosis is pending, and no cap or fixture is changed. The required actual-main integration needs a new final all-green Site Check before merge.

The official bd34 final archive9975395947 (78,533,522 bytes; SHA256592573f68177dc32fd3b5d5924d301976888b8ed42d611d9ef9cdeb836b7bb90) has 78 valid raw reports and every CLS exactly0, but no trace/devtools assets. Each failing route has two other passing samples; all response signatures match the prior successful Search431 comparison. This supports intermittent execution behavior, but cannot distinguish CPU work from waits. The existing Lighthouse driver now saves its already-computed assets when CI=true, after each audit and browser shutdown. This adds diagnostic files only; workflow files, browser flags, retries, fixture selection and all pass thresholds remain unchanged. No unchanged-head rerun was used.

Actual-main local gates pass: check918/zero errors or warnings; all3434 tests/351 files; dist/footer/boundary/bundles; exact16 paths with only Learn protected and pinned to67360f35. All8815 built files remain byte-identical to the fresh Search build. Twelve existing Lighthouse asset integrity tests also pass after the diagnostic export change. Final exact-head CI is still required.
