# Registry Aura V2 acceptance proofs

These production-rendered proofs use a 375 CSS-pixel viewport, one locally saved natal summary, a deterministic sky instant (`2026-07-16T12:00:00Z`), and a deliberately different wallet-check date (`2026-07-15`). Each proof includes the identifier-free source docket, held-only Carry Ring, persistent legend, factual Aura sentence, and symbolic reading.

- `one-375.png` — one official Zodiac found at the public address.
- `four-375.png` — four official Zodiacs, using the same settled-state geometry as the other count proofs.
- `twelve-375.png` — twelve official Zodiacs, with no completion or prestige treatment.
- `social-card-250.png` — the 1080×1350 focal-sign social card rendered at a 250 CSS-pixel feed width.
- `social-preview-375.png` — the on-device review step in a 375 CSS-pixel mobile viewport before download or Web Share.
- `wallet-basics-375.png` — the static beginner guide with every native disclosure open, proving installation, privacy, scam, and clearing guidance remains readable without client JavaScript.

The browser acceptance run verifies that:

- the central natal wheel keeps all twelve zodiac coordinates while the outer wallet layer draws only held signs;
- open-corner record frames keep identical geometry in the one-, four-, and twelve-sign cases;
- chart evidence is limited to two neutral glyph chips plus `+N`, and dated sky evidence uses a white sector arc;
- no connector, focus marker, glow, pulse, ranked state, denominator, or completion language returns;
- the source order remains Chart → Record → Sky, and the wallet and sky facts retain their own dates;
- unknown birth time omits Moon, ASC, MC, and exact natal degrees from Aura evidence;
- the social card is private by default, keeps chart disclosure session-only, paints generated copy only, requires an exact on-device preview before sharing or downloading, and sends Web Share a constant-name PNG with no caption, URL, or identifier;
- the example Aura is explicitly illustrative, says no public address was checked, and exposes neither sharing nor meaningfulness feedback as if it were a real lookup;
- reduced motion receives one finished-state fade, and record/chart marks remain visible in forced colors.
- wallet education remains available without JavaScript; opening it calls no wallet or holdings API, its disclosures remain keyboard-operable, and its official external links carry no tracking query.

These images are geometry and privacy proofs. The separate five-second comprehension test with ordinary, crypto-literate, and astrology-literate viewers is still a launch gate rather than a claim made by this automated run.
