# Widget presentation acceptance

The builder uses native, visible controls with 46px fields and explicit focus treatment. Widget type and embed mode now span the full control column so their selected names remain readable on desktop; Theme and Accent retain their paired row. The four approved page changes also improve mobile code type and inline spacing.

Browser comparison33984938740 on658d316d0935f3b201dfadd1507adea7f592768c passed actual controls, iframe/script settings, all Explorer checks, Phase1 and15 visuals. The official archive9975109776 is94,141,551 bytes, SHA25666999e82df6a75cad9211bd853ccbad3b5f3e74bb581a5ffc7b34e379eccbe80. All391 provenance hashes and392 CRCs were verified. Root personally inspected allfour genuine Widget PNGs; that review found the desktop name truncation addressed here. Updated captures remain required.

That comparison remains failed because monthly horoscope sample3 scored76 with939.4483ms TBT. Its other two samples scored97/TBT0; all78 raw CLS values were exactly0. The failed trace shows445.208ms renderer wall time but1.003ms thread CPU in document loading, with no nested JavaScript task. All17 response signatures match the two passing samples and nine previous passing samples. This strongly supports a browser/runner wait or descheduling episode; its precise cause is unproven. No gate waiver, threshold change, or unchanged-head rerun was used.

The native combobox lookup repair follows the actual accessible role/name; it does not change production ARIA. The original label-lookup timeout and prior inherited Search failures remain preserved. Current source inherits complete Searchbd34, including its genuine Phase1 receipt. Fresh cumulative gates and a genuine comparison of the widened fields are required. This remains a draft stacked on Search; release must integrate the actual preceding main and pass all final gates.

Fresh full local build/check pass after the full-width controls change; all3434 tests/351files pass, including genuine inherited evidence. Dist, footer, boundary, schema, engine isolation, unchanged bundle caps and scope pass. The complete Search87eba8b4 main-based integration and CI trace retention are then inherited without product changes. All built files remain byte-identical to that fresh Widget build; exact comparison scope is seven paths with zero protected changes. A new genuine browser comparison must verify the wider fields before release.

## September 6 continuation

The required comparison completed on Widget head `78a688107771ced9e63d9430554989f32aa00c19` against Search `87eba8b4f80cffb15002a53b7603ea43f771e143`. Site Check [33987702828](https://github.com/ZodiacsOfficial/site/actions/runs/33987702828) passed all 14 jobs. Browser Evidence [33987702831](https://github.com/ZodiacsOfficial/site/actions/runs/33987702831), attempt 1, passed the required compare steps. Artifact `9975934427` is 94,736,926 bytes with SHA256 `ee8fa1ac3674bf164c585f97dcdcf3cd0cb2853d56b8b52bcd79ad4fc4f73179`; all 392 ZIP members passed CRC verification, and all 391 provenance inventory entries matched their byte counts and hashes.

The four genuine Moon/iframe and chart/script captures at 390 and 1440 pixels were inspected. The widened controls display their selected names in full, with readable mobile controls, code and focus treatment. Element-capture scrolling places the sticky header across part of some builder or preview headings, and the inherited fixed Guide overlaps the mobile preview edge; these images therefore establish the selected-value correction, not unobstructed full-page placement. All 78 raw Lighthouse reports have accessibility 100 and CLS 0, with minimum performance 97, maximum LCP 2490.9396 ms and maximum TBT 23 ms. These results cover the fixed CI routes only.

The continuation inherits the current daily edition through Search integration `0f367be5f378b7a3b1dcb862c8c7ed05ae0662b3`, whose main parent is `cf6b0edf7b4ff6c58c01a685e1ebd48e69d0d1e1`. Widget product code and browser assertions remain unchanged from the verified head above. The Widget delta remains seven paths, with no protected product changes and an unchanged inherited Search allowance. The final exact-head Site Check is still required; release must use the actual preceding main after Search merges. The earlier failed runs remain evidence and are not waived by the later passing comparison.

Fresh integrated Node 22 checks pass: full build, Astro check (918 files, zero errors or warnings, 10 hints), all 3,434 tests across 351 files, dist integrity (4,302 HTML files, 1,119 search entries, nine feed items), canonical footer, consumer boundary (1,349 files), scope and unchanged bundle budgets. The generated Phase 1 source receipt remains `858e9e6024f8692a32f8fa4d720bac85a26113186f378307aabcf626cdb2bb42`. The first test attempt passed 350 suites but could not collect the 39-test engine suite through a dependency-directory symlink; replacing only that ignored symlink with a local dependency copy resolved module loading. The focused engine check and the complete suite then passed without changing product code, tests, configuration or thresholds.

Search PR #371 subsequently merged as actual main `feed4a135e14561335ef357e07497418ce622b63`, with tree `817930bdb2ee16c707b6b1e500d662a4b2135e17`. This Widget release now contains that main commit as an ancestor. The merge produced the exact already-validated Widget tree `84468de7aee0c6e4f0efb72017fbb18974d296f5`, also published as `66d32a0ec3078f88e13ccc90354f849cf4c24e2c`; the only subsequent edit is this acceptance note. Every build and test input therefore remains byte-identical to the passing local run above. Scope against actual main still passes with seven paths and no protected changes. Final exact-head Site Check against main remains a release gate.
