# Wave 22 — chart context reconstruction

The original implementation was not recoverable. This candidate follows the
recovered complete September 5 plan and approved amendment. Its preparation
base is Wave 21 `ce76b29b`; Locale/Moon prerequisites are already released.
The release will still integrate actual main after every preceding wave.

Commit A restores the additive dignity API, explicit six-shape millidegree
convention with 2° exclusion bands, classical seven-body dispositor graph,
actual Ascendant/cusp rulers, six-locale native controls and the separate
calculator-owned input revision. Legacy dignity results, scene/wheel math,
People data, storage, codecs and global catalogs are unchanged.

The original-longitude model preserves independent sections, explicit Moon
certainty and unknown-time reference scope. An incomplete graph cannot claim
a final dispositor. Input revision advances synchronously on field edits,
source runs and profile revocation, independently of the retained chart.

Local model/native tests: 90 pass in four files, including six-locale receipts,
classical ruler controls, shape fixtures, exclusion boundaries and uncertainty.
Initial check found nine reversed localized-name arguments; corrected through
the existing helper contract. Final check: 978 files, zero errors/warnings,
eleven hints. No broad build, full suite or real browser run yet.

Commit B implements all four private section PNGs through the same visible
facts, explicit preparation intent, a 15-second whole-operation bound and real
share/download/cancelled outcomes. Source revision, section, locale, Close,
unmount and live disclosure state invalidate late work. Rejected imports offer
the existing explicit reload control. Every generated bitmap, canvas and
object URL has cleanup; failed assets/fonts/encoding remain retryable.

The existing Explorer drive now includes genuine 390/1440 computed charts,
all six locale labels and all four image types, actual glyph ink bounds,
unknown-time scope, immediate input-edit cancellation and renderer-load
recovery. These browser checks have not run. Real layout and PNG review remain
required; unit canvas metrics are not visual evidence.

Focused model, UI, export and observation coverage initially passes132 tests;
the expanded independent table/context file passes105 tests (all84 classical
planet/sign pairs plus cusp/uncertainty cases). Four malformed metadata cases
were added afterward. Astro check981 passes before those final pure-model
additions. An initial complete build/postbuild passes bundle gates, but two
late source additions (localized dignity definitions and the metadata array
guard) occurred after compilation; that build is not final-source acceptance.
A fresh frozen-source build/check/full unit sequence follows this commit.

Frozen candidate `9aaecbc164802c7a3dda06575b306ea88bcba39e` now passes
223 focused tests, the complete build/postbuild and every unchanged bundle
budget. Its build receipt is
`0fea190d6c93e8442be7b7050ba030ba8736b3d282e2feb010f7760ffb22da11`.
Astro check: 981 files, zero errors/warnings, eleven hints. The full serial
unit suite passes 4,108 tests in 389 files; its sole failure is the required
Phase 1 source/capture receipt, still bearing the earlier Wave 13 fingerprint.
This is not a green acceptance claim: genuine fresh browser captures and
their verified receipt must satisfy that gate at release integration. No
assertion, timeout, budget or stored receipt was weakened or rewritten.

Actual-main scope, fresh receipt/captures, real browser/PNG review, final CI
and production acceptance remain pending. No release approval is claimed.

## First native image review and type adjustment

The cumulative prepared Wave23 preview at b1b614c4 rendered the four native
context disclosures and a genuine English shape image for the synthetic
1999-08-11 chart. The short shape card was legible but used small text with
excess empty space. Its adaptive type now starts at42px instead of28px and
retains the same22px minimum, line wrapping, spacing and overflow failure.
Dense cards can fall through the identical previous sizes; facts, convention,
private-data exclusions, fonts, dimensions and resource deadline are unchanged.
All31 renderer tests pass. New real-image review and the complete six-locale
390/1440 browser suite remain required; this updates a preparation candidate,
not an approved release or the earlier frozen-source test claim.
