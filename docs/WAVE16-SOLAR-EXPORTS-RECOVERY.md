# Wave 16 — recovered Solar Return exports

This isolates the Solar Return source from the cumulative recovered archive.
It is prepared on local Wave 15 fc77061; actual released main must be integrated
before publication. No cumulative advanced packet is being published as one wave.

The eighteen implementation/test paths add return-specific Save image, Share
image and a private one-minute ICS marker. The export model contains the returned
chart, reading and uncertainty, without birth inputs, names or cast coordinates.
Image data is prepared before native sharing to retain activation; cancellation
is neutral and unsupported/refused sharing retains a separate download path.
Resource cleanup and stale-request ownership protect replacement calculations.
The shared image transport is updated once, preserving existing natal variants.

Result revisions invalidate exports after birth, year, relocation or relevant
saved-chart changes, including same-instant relocation. Unknown-time and missing
place modes omit unsupported houses/angles and explicitly qualify the returned
instant. Original ephemeris and return computation remain unchanged.

Only the Solar export CSS and Explorer hook are taken from the shared cumulative
files. The shared recovery-test harness retains all six Wave 15 event ownership
tests while adding the five recovered Solar ownership cases. Eighteen source/test
paths plus this recovery note form the local wave.

Focused Node 22 tests pass: 106 tests in ten files. This includes five external
longitude-crossing vectors under their predeclared limits, not comprehensive
independent natal-to-return reports. Wave 19's separate Swiss fixtures and engine
changes are deliberately not imported into this wave. Type check passes across
927 files with zero errors/warnings and ten existing hints. Compilation passed, but postbuild found the inherited Wave 15 event publication
date contract conflict. The subsequent Wave 15 receipt-bound validator correction
is inherited intact. Postbuild is being rerun; no full build, full-suite or browser
pass is claimed here.

The recovered browser helper exercises real PNG/ICS downloads, image dimensions,
font/ink observations, private-field omissions, user activation, relocation,
unknown-time modes and renderer recovery. Browser evidence, actual fonts/pixels,
matching Phase 1 receipt, final local/CI gates, separate PR, merge and production
verification remain pending. No baseline or budget is changed.
