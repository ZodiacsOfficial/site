# Wave 21 — recovered aspect patterns

Separate candidate; not released. Base is the recovered Wave 20 candidate
`dea45454`, with earlier waves retained pending sequential main integration.

The sixteen dedicated recovered files restore all-match grand trine, T-square,
grand cross and kite detection from original positions and the owning inclusive
aspect graph. Extra edges do not remove matches; contained patterns remain
selectable. Invalid geometry fails closed. No aspect thresholds or numerical
engine behavior change.

English natal and composite results gain an optional disclosure, native body
selection, exact edge receipts, diagrams, symbolic reflection and a private
1080×1350 selected-pattern image. The panel loads on disclosure intent; image
code loads on image intent with bounded preparation and stale-result ownership.
Unknown-time Moon geometry is excluded and interpretation/export are withheld.
Composite certainty comes from both original input times, independently of
whether a Moon midpoint is present. Existing reading cards remain intact.

The existing Explorer drive invokes the recovered 390/1440 browser checks.
These have not run on this candidate; image layout is not yet approved.

Validation: all 96 focused tests in seven files pass. Initial Astro check found
one recovered test-only assertion cast error; the explicit unknown conversion
repairs that cast without changing runtime assertions. Final check, complete
build, full units, actual-main scope, fresh receipts, reviewed browser evidence,
release CI and production verification remain pending. Existing budgets,
baselines and Phase 1 checks are unchanged.

Final local build/postbuild passes unchanged bundle gates, source receipt
`15754e64894ad5a7112a88c090af530350236a580886c1401e7db9d2a0d1cee7`.
Final Astro check: 971 files, zero errors/warnings, eleven hints. Full units:
3,885 pass, six failures (stale Phase1 receipt plus five existing test timeouts).
Serial diagnostic of the five affected files, with original time limits,
passes all 25 other tests and preserves the sole stale receipt failure.
No final all-green full-suite claim. Logs are wave21-full-tests.log and
wave21-timeout-diagnostic.log in the recovery workspace.
