{
  "schemaVersion": 1,
  "package": "zodiacs-mcp-server",
  "version": "0.1.0-rc.7",
  "status": "Unpublished release candidate. Not on npm, under this or any other name. Install from the versioned archive at https://zodiacs.org/examples/, whose SHA-256 is published beside it.",
  "sourceRepository": "https://github.com/ZodiacsOfficial/site",
  "sourcePaths": [
    "src/mcp/server.ts",
    "src/mcp/tools.ts",
    "src/mcp/bounds.ts",
    "src/lib/compare/diff.ts",
    "src/lib/compare/angles.ts",
    "src/lib/compare/replay.ts",
    "scripts/build-mcp-server.mjs"
  ],
  "bundled": {
    "note": "Inlined into server.mjs by esbuild because neither is published: the engine is an unpublished candidate, and the comparison is TypeScript in the site repository. Bundling is what keeps one copy of the comparison rules rather than two.",
    "engine": {
      "package": "@zodiacs/engine",
      "version": "0.1.1-rc.7",
      "releaseStatus": "unpublished-candidate",
      "sourceRepository": "https://github.com/zodiacs-org/engine",
      "sourcePackagePath": "",
      "artifactSha256": "49b2b03f50fea8a625d443d4fd0f6d03ffc22831e54009fd09c49d07c8698f90"
    },
    "ephemeris": {
      "name": "astronomy-engine",
      "version": "2.1.19"
    },
    "comparison": {
      "module": "src/lib/compare/diff.ts",
      "note": "The same module the browser tool at https://zodiacs.org/developers/compare/ runs."
    }
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "@modelcontextprotocol/core": "2.0.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0"
  },
  "protocol": {
    "transport": "stdio",
    "sdkLatest": "2025-11-25",
    "negotiatedInTesting": [
      "2025-11-25",
      "2025-06-18",
      "2025-03-26",
      "2024-11-05",
      "2024-10-07"
    ],
    "note": "Revisions observed to negotiate against this server on the wire, one probe each. The official SDK client offers 2025-11-25."
  },
  "builtWith": {
    "esbuild": "0.28.1",
    "target": "node22",
    "format": "esm",
    "node": "v22.22.2"
  },
  "schemas": {
    "record": "zodiacs.natal-envelope.draft-v1",
    "receipt": "zodiacs.calculation-receipt.draft-v1",
    "note": "Zodiacs-owned draft vocabulary, not an industry interoperability standard."
  },
  "supersedes": "0.1.0-rc.6",
  "changedSince": "rc.1 through rc.6 are unchanged and still downloadable; this is a separate immutable archive. The adapter code is the same as rc.6; the bundle differs in the version it reports and the engine inside it, which moves from 0.1.1-rc.6 to 0.1.1-rc.7. With it the ascendant and midheaven use the true obliquity of date, Placidus falls back to whole sign at 90\u00b0 minus the obliquity (about 66.56\u00b0) instead of 66\u00b0, an aspect is applying while its orb is shrinking at that instant, and speeds are central differences over \u00b10.001 day (\u00b10.25 day for the true node). Planetary longitudes are unchanged. Records written with rc.6 still parse and compare; one naming rc.6 is not re-run on this engine, so its differences stay hypotheses."
}
