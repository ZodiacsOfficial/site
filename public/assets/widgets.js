/* Generated by scripts/build-widget-loader.mjs from src/strings/widgets.ts. */
(function () {
  'use strict';

  var kinds = Object.freeze({ moon: [360, 250], sky: [480, 250], chart: [400, 610] });
  var titles = Object.freeze({"moon":"Moon phase today — Zodiacs.org","sky":"The sky today — Zodiacs.org","chart":"Mini birth chart — Zodiacs.org"});
  var sandbox = 'allow-scripts allow-forms allow-same-origin allow-popups allow-popups-to-escape-sandbox';

  function validAccent(value) {
    return /^#[0-9a-f]{6}$/i.test(value || '') ? value.toUpperCase() : '';
  }

  function mount(script) {
    if (!script || script.dataset.zodiacsMounted === 'true') return null;
    var kind = script.dataset.zodiacsWidget;
    if (!Object.prototype.hasOwnProperty.call(kinds, kind)) return null;
    var theme = script.dataset.theme === 'light' ? 'light' : 'dark';
    var source = new URL(script.src);
    var frameUrl = new URL('/embed/' + kind + '/', source.origin);
    frameUrl.searchParams.set('theme', theme);
    var accent = validAccent(script.dataset.accent);
    if (accent) frameUrl.searchParams.set('accent', accent);

    var dimensions = kinds[kind];
    var frame = document.createElement('iframe');
    frame.src = frameUrl.toString();
    frame.title = script.dataset.title || titles[kind];
    frame.width = String(dimensions[0]);
    frame.height = String(dimensions[1]);
    frame.loading = 'lazy';
    frame.referrerPolicy = 'strict-origin-when-cross-origin';
    frame.setAttribute('sandbox', sandbox);
    frame.style.cssText = 'display:block;border:0;border-radius:14px;overflow:hidden;max-width:100%';
    script.dataset.zodiacsMounted = 'true';
    script.parentNode.insertBefore(frame, script.nextSibling);
    return frame;
  }

  var current = document.currentScript;
  if (current && current.dataset.zodiacsWidget) mount(current);
  document.querySelectorAll('script[data-zodiacs-widget]:not([data-zodiacs-mounted])').forEach(mount);

  window.ZodiacsWidgets = Object.freeze({ mount: mount });
}());
