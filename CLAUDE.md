# zodiacs.org Landing Page

Build the landing page for `zodiacs.org`.

Use the Claude frontend design skill/plugin:

```json
{
  "enabledPlugins": {
    "frontend-design@anthropics": true
  }
}
```

Keep design decisions with Claude. This starter repo only establishes the project name and plugin configuration.
